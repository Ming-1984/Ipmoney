# Ipmoney Full Quality Remediation TODO (Complete)

> Updated: 2026-03-13
> Scope: `apps/api`, `apps/client`, `apps/admin-web`, `scripts`, CI/CD, engineering docs
> Constraint: Real login/payment integrations are out of scope for this phase.

## 1. Current Baseline (for planning)

### 1.1 Quality gates status
- `typecheck`: pass (api/client/admin-web).
- `build`: pass (api/admin-web/client h5/weapp); WeApp severe regression has been fixed in this batch, and bundle gate is now enforced.
- `smoke`: latest full rerun pass on `2026-03-12-r189/r190/r191` (UI HTTP `86/86`, UI Render full `83/83`, UI DOM full-83 `83/83`); previous API full baseline remains green on `2026-03-12-r185` (`1754/1754`), including announcement-tag and patent-map industry-breakdown/top-assignee compatibility regressions.
- `verify`: pass on `2026-03-13-r228` (openapi/lint/typecheck/build/api-smoke `1754/1754` + coverage `238/238` + quality-floor `violations=[]` + db/ui-http/ui-render(core)/ui-dom(core) all green); port/process hardening has been applied to core smoke scripts.
- `api test` (new minimal gate): pass on `2026-03-13` with `pnpm -C apps/api test` (`468/468`, Vitest + Supertest health e2e), and CI now executes it as a dedicated step.
- `weapp-route-smoke`: pass on `2026-03-12-r187` (11/11 routes, no runtime exceptions, `--kill-stale-devtools` enabled); script now includes launch retry + stale DevTools diagnostics + success/failure report output.

### 1.2 Coverage and test capability
- OpenAPI operations: 243 (GET 108 / POST 93 / PUT 12 / PATCH 21 / DELETE 9).
- API smoke covers 1754 checks (including semantic read-back/state assertions).
- API Vitest suite now passes `468/468` (`rbac` read/update strictness `5`, `favorites` list filter strictness `5`, `orders` list filter strictness `5`, `patent-maintenance` list filter strictness `5`, `demands` search/list strict filters `5`, `achievements` search/list strict filters `5`, `artworks` search/list strict filters `5`, `conversations` pagination/id strictness `4`, `patents` admin-list filter strictness `4`, `alerts` list-filter strictness `4`, `alerts` acknowledge strictness `4`, `cases` list-filter strictness `4`, `notifications` filter/id strictness `4`, `notifications` write-path strictness `4`, `user-verifications` admin-list filter strictness `2`, `admin-user-verifications` controller strictness `5`, `admin-config` controller strictness `3`, `config.service` behavior strictness `5`, `public-config` controller strictness `2`, `files.service` temp-token strictness `5`, `users` profile+verification strictness `6`, `auth.service` strictness `11`, `auth.controller` delegation `4`, `bearer-auth.guard` strictness `7`, `verified-user.guard` strictness `4`, `permissions` utilities `4`, `all-exceptions.filter` strictness `4`, `rate-limit.guard` strictness `3`, `file-access.guard` strictness `3`, `request-id.middleware` strictness `4`, `request-logger.middleware` strictness `4`, `content-audit.service` strictness `5`, `listings.controller` delegation strictness `6`, `demands.controller` delegation strictness `7`, `achievements.controller` delegation strictness `7`, `artworks.controller` delegation strictness `7`, `webhooks.service` strictness `12`, `webhooks.controller` delegation `1`, `demo.config` strictness `4`, `addresses.list` strictness `3`, `addresses.controller` delegation `4`, `reports` range-filter strictness `4`, `reports.controller` delegation strictness `2`, `contracts` list-filter strictness `4`, `contracts` upload strictness `7`, `contracts.controller` delegation strictness `2`, `announcements` filter/sanitization strictness `4`, `announcements.controller` delegation strictness `7`, `admin-regions.controller` delegation strictness `5`, `admin-patent-map.controller` delegation strictness `5`, `alerts.controller` delegation strictness `3`, `audit-logs.controller` delegation strictness `3`, `rbac.controller` delegation strictness `6`, `favorites.controller` delegation strictness `5`, `cases.controller` delegation strictness `6`, `comments.controller` delegation strictness `3`, `conversations.controller` delegation strictness `4`, `my-verification.controller` delegation strictness `3`, `industry-tags.controller` delegation strictness `3`, `organizations.controller` delegation strictness `2`, `organizations` detail strictness `3`, `inventors.controller` delegation strictness `1`, `inventors` ranking-query branch strictness `2`, `notifications.controller` delegation strictness `2`, `regions.controller` delegation strictness `2`, `users.controller` delegation strictness `2`, `patents.controller` delegation strictness `4`, `patent-maintenance.controller` delegation strictness `2`, `patent-clusters.controller` delegation strictness `3`, `files.controller` delegation strictness `4`, `ai.controller` delegation strictness `2`, `admin-ai.controller` delegation strictness `3`, `tech-managers.controller` delegation strictness `3`, `patent-map.controller` delegation strictness `3`, `ai.query` filter/sanitization strictness `4`, `ai` feedback/parse-update strictness `6`, `patent-map` filter/import strictness `5`, `patent-clusters` public filter strictness `3`, `files` temporary-access strictness `4`, `tech-managers` filter/sanitization `4`, `tech-managers` update/public strictness `5`, `content-utils` tag sanitization `5`, `regions` filter/sanitization `6`, `organizations+inventors` filter strictness `6`, `listings` search-filter strictness `6`, `audit-logs` strict-filter `5`, `orders` write-first `24`, `orders.controller` delegation strictness `8`, `comments` write-first `28`, `favorites` write-first `29`, `addresses` write-first `9`, `health` e2e `2`).
- OpenAPI smoke alignment (excluding login/payment integrations): 238/238 operations covered, and this is now regression-gated in `verify`.
- API smoke quality floor gate (coverage intensity): active in `verify` with minimum thresholds on total assertions, write/read counts, negative-path count, key error-code distribution, and admin negative density.
- Write operations total 135; smoke now executes 1297 write assertions plus 127 read-back semantic checks (state transitions, persistence, cross-module link integrity, same-key replay invariants, selected concurrency race outcomes, percentile-based chaos stability guard, cross-run trend-threshold capability, and AI/industry-tag/region/featured/file-temporary-access/admin-listings-write/user-listings-write/admin-cases-create-enum/patent-strict-negative/patent-maintenance-create-status/ai-query-strict-enum/order-payment-intent-strict-pay-type/RBAC-401-403-write-boundary/user-write-401-boundary-expanded/admin-settlement-refund-full-lifecycle-auth-boundary/admin-case-maintenance-verification-reject-auth-boundary/admin-config-write-401-403-convergence/admin-domain-write-401-403-convergence-round2/admin-content-write-401-403-convergence-round3/admin-residual-write-401-403-convergence-round4/admin-alert-comment-write-convergence-round5/admin-patent-map-entry-upsert-and-patents-normalize-write-convergence-round6/patent-map-year-strict-integer-round7/reports-days-strict-round8/ai-feedback-score-strict-round9/file-temp-ttl-strict-round10/contract-signed-deal-amount-strict-round11/search-listings-numeric-filter-strict-round12/listing-featured-empty-rank-strict-round13/maintenance-schedule-year-strict-round14/tech-manager-featured-rank-strict-round15/listing-transfer-count-strict-round16/demands-artworks-int-filter-strict-round17/search-demand-artwork-empty-filter-strict-round18/patent-map-import-strict-round19/demands-empty-budget-string-strict-round20/artworks-empty-price-deposit-string-strict-round21/listings-empty-price-deposit-string-strict-round22/listings-empty-negotiable-range-percent-strict-round23/orders-empty-datetime-string-strict-round24/patents-empty-date-datetime-string-strict-round25/artworks-empty-creation-date-string-strict-round26/listings-empty-date-and-filter-string-strict-round27/alerts-maintenance-empty-date-filter-string-strict-round28/cases-empty-due-at-string-strict-round29/reports-empty-start-end-string-strict-round30/listing-featured-tech-manager-empty-featured-until-string-strict-round31/maintenance-schedule-empty-date-string-strict-round32/listings-empty-nullable-enum-string-strict-round33/content-user-admin-nullable-enum-empty-string-and-user-invalid-strict-round34/patents-empty-legal-status-string-strict-round35/listings-patent-legal-status-source-primary-strict-round36/patents-upsert-null-legal-status-clear-round37/patents-upsert-null-filing-date-clear-round38/listings-patent-null-filing-date-clear-round39/alerts-maintenance-empty-query-id-string-strict-round40/audit-logs-empty-query-id-string-strict-round41/listings-region-code-null-clear-empty-string-strict-round42/demands-achievements-artworks-region-code-null-clear-empty-string-strict-round43/me-addresses-region-code-null-clear-empty-string-strict-round44/me-patch-region-code-null-clear-empty-string-strict-round45/admin-region-update-null-empty-name-level-strict-round46/regions-patent-map-empty-filter-strict-round47/ai-agent-query-region-code-strict-round48/search-and-recommendations-empty-region-code-strict-round49/inventor-org-tech-manager-empty-region-code-strict-round50/listing-search-empty-seller-cluster-filter-strict-round51/content-create-empty-region-code-strict-round52/listing-create-path-empty-region-code-strict-round53/listing-cluster-id-empty-string-strict-round54/cases-create-empty-order-assignee-id-strict-round55/maintenance-task-create-empty-assigned-cs-user-id-strict-round56/maintenance-task-update-empty-assigned-evidence-id-strict-round57/order-milestones-empty-evidence-file-id-strict-round58/content-cover-file-id-empty-string-strict-round59/comment-create-empty-parent-comment-id-strict-round60/admin-listing-update-empty-seller-user-id-strict-round61/admin-content-owner-user-id-empty-string-strict-round62/admin-comments-empty-content-id-strict-round63/admin-listing-create-empty-seller-user-id-strict-round64/me-verification-empty-id-code-fields-strict-round65/me-verification-empty-evidence-file-id-strict-round66/admin-order-upsert-invoice-empty-invoice-no-strict-round67/admin-announcement-empty-issue-no-strict-round68/admin-patent-empty-application-no-display-strict-round69/admin-order-manual-payment-empty-trade-no-strict-round70/admin-case-create-empty-title-requester-name-strict-round71/listings-negotiable-note-empty-string-strict-round72/listings-encumbrance-note-empty-string-strict-round73/listings-title-empty-string-strict-round74/listings-summary-empty-string-strict-round75/demands-contact-name-empty-string-strict-round76/demands-contact-title-empty-string-strict-round77/demands-contact-phone-masked-empty-string-strict-round78/demands-summary-empty-string-strict-round79/demands-description-empty-string-strict-round80/achievements-summary-empty-string-strict-round81/achievements-description-empty-string-strict-round82/artworks-description-empty-string-strict-round83/artworks-certificate-no-empty-string-strict-round84/artworks-material-empty-string-strict-round85/artworks-size-empty-string-strict-round86/artworks-title-empty-string-strict-round87/artworks-creator-name-empty-string-strict-round88/artworks-update-omit-fields-preserve-round89/artworks-update-null-title-creator-name-strict-round90/achievements-title-empty-null-update-strict-round91/demands-title-empty-null-update-strict-round92/demand-achievement-create-empty-title-regression-round93/announcement-patent-title-strict-regression-round94/patents-upsert-abstract-omit-preserve-round95/patents-upsert-application-no-display-omit-preserve-round96/patents-upsert-source-meta-omit-preserve-round97/listing-validation-no-patent-side-effect-round98/case-evidence-invalid-file-id-strict-round99/case-note-empty-required-strict-round100/case-uuid-format-strict-round101/order-upsert-invoice-file-id-format-strict-round102/invoice-admin-order-id-format-strict-round103/user-invoice-order-id-format-strict-round104/order-detail-payment-intent-order-id-format-strict-round105/order-case-refund-order-id-format-strict-round106/admin-order-core-order-id-format-strict-round107/admin-refund-request-id-format-strict-round108/order-listing-id-and-payout-evidence-file-id-format-strict-round109/file-route-file-id-format-strict-round110/contract-upload-id-format-strict-round111/conversation-route-id-format-strict-round112/comment-route-id-format-strict-round113/alert-notification-id-format-strict-round114/favorite-route-id-format-strict-round115/address-route-id-format-strict-round116/organization-tech-manager-route-id-format-strict-round117/announcement-route-id-format-strict-round118/ai-parse-result-route-id-format-strict-round119/rbac-role-user-route-id-format-strict-round120/patent-maintenance-route-id-format-strict-round121/admin-user-verification-route-id-format-strict-round122/patent-route-id-format-strict-round123/achievement-route-id-format-strict-round124/artwork-route-id-format-strict-round125/demand-route-id-format-strict-round126/listing-route-id-format-strict-round127/case-route-id-format-strict-round128/contract-upload-contract-file-id-format-regression-round129/contract-upload-empty-contract-file-id-regression-round130/contract-upload-missing-contract-file-id-regression-round131/contract-upload-contract-file-id-not-found-regression-round132/contract-upload-missing-contract-id-regression-round133/contract-upload-non-pdf-file-regression-round134/contract-upload-forbidden-not-seller-regression-round135/api-smoke-response-body-capacity-round135/contract-upload-happy-path-round136/contract-list-wait-confirm-readback-round137/contract-list-auth-page-zero-guard-round138/contract-list-page-size-zero-guard-round139/contract-prefixed-upload-and-page-size-format-round140/contract-list-page-size-cap-and-decimal-round141/contract-list-status-case-normalization-round142/contract-list-page-negative-decimal-guard-round143/contract-list-empty-status-and-negative-page-size-round144/contract-list-empty-page-round145/contract-list-page-size-slight-over-cap-round146/contract-list-page-one-round147/contract-list-page-size-one-round148/contract-list-page-size-fifty-round149/contract-list-page-one-page-size-fifty-round150/contract-list-unauthorized-pagination-and-smoke-filter-artifact-cleanup-round151/search-filter-smoke-artifact-frontend-guard-and-preflight-round152/public-filter-artifact-backend-guard-and-smoke-cleanup-round153/public-regions-smoke-query-anti-leak-round154/public-regions-industry-tags-anti-leak-round155/client-search-industry-tags-sanitize-round156/publish-flow-industry-tags-sanitize-round157/client-industry-tags-visible-surface-sanitize-round158/api-public-industry-tags-sanitize-round159/api-public-industry-tags-smoke-regression-round160/api-smoke-dist-freshness-guard-round161/search-public-industry-tags-smoke-regression-round162/search-hidden-industry-filter-sanitize-round163/ai-hidden-industry-filter-sanitize-round163/hidden-industry-tag-variant-sanitize-round164/hidden-industry-tag-space-variant-sanitize-round167/tech-manager-public-service-tag-sanitize-round168/announcements-hidden-tag-sanitize-round169 assertions), while unique write-operation coverage remains at least prior 132/135 baseline.
- Non-admin secured write-path 401 boundary: fully covered in smoke except `/auth/wechat/phone-bind` (login-scope, intentionally out of current phase).
- Admin secured config write-path boundary (`PUT /admin/config/*`): all 8 endpoints now include unauthorized/custom-role/after-clear forbidden convergence checks.
- Additional admin write-path boundary closure: `industry-tags / regions / region-industry-tags / listings-featured / tech-managers / patent-map-import / rbac-role-create / order-issue-invoice` now include unauthorized/custom-role/after-clear forbidden checks.
- Additional admin content write-path boundary closure: `listings / demands / achievements / artworks` create+update+approve+publish+off-shelf+reject now include unauthorized/custom-role/after-clear forbidden checks.
- Remaining admin write-path closure completed: `announcements / patents / cases-notes / rbac role-user mutation(custom-role)`, and `admin ai parse-result update` now uses unauthorized/forbidden checks with missing-id `404` compatibility (resource check may precede auth).
- Highest uncovered write concentration remains in `/admin` (77 write operations; still the largest uncovered write domain).
- `apps/api` Vitest/Supertest suite has expanded to `92` files (`468` assertions), including strictness/delegation coverage for `auth`, `webhooks`, auth guards, permission utilities, request middlewares, exception normalization, rate limiting, file access guard fallback, content-audit aggregation behavior, listings/demands/achievements/artworks admin-audit controller boundaries, announcements/orders delegation boundaries, admin regions/patent-map config boundaries, admin alerts/audit/reports controller boundaries, and rbac/favorites/cases/comments/conversations/my-verification/industry-tags/organizations/inventors/notifications/regions/ai/admin-ai/tech-managers/patent-map/contracts/patents/patent-maintenance/users/patent-clusters/files controller boundaries, plus notifications write-path, alerts acknowledge behavior, inventors ranking-query branch, organizations detail behavior, contracts upload behavior, tech-managers update/public detail behavior, and ai feedback/admin parse-update behavior; remaining gaps are deeper cross-module integration semantics beyond current unit/controller strictness scope.

### 1.3 Code rigor
- `apps/api/src`: ~750 `any` usages (74 files).
- `apps/client/src`: ~302 `any` usages (56 files).
- `apps/admin-web/src`: ~125 `any` usages (24 files).
- `@Body() ...: any` appears ~65 times.
- `apps/api/src/main.ts` lacks global `ValidationPipe`.

### 1.4 WeApp bundle/perf risk
- `apps/client/dist/weapp/app-origin.wxss`: 286,492 bytes (~279.8 KiB) after fix (was 2,690,716 bytes).
- `apps/client/dist/weapp/pages/home/index.wxss`: 118,115 bytes (~115.3 KiB) after fix (was 2,586,679 bytes).
- `apps/client/dist/weapp/pages/me/index.wxss`: 69,236 bytes (~67.6 KiB) after fix (was 1,271,348 bytes).
- `apps/client/dist/weapp/subpackages/login/index.wxss`: 64,541 bytes (~63.0 KiB) after fix (was 1,266,653 bytes).
- Current values are back under the phase budget guard (`app-origin` < 500 KB, key pages < 200 KB).

### 1.5 UI coverage blind spots
- Client configured pages: 58 (`apps/client/src/app.config.ts`); render smoke full covers 58 (100%), gap 0.
- Admin routes: 25 (including `/login` and `/`); render smoke full covers 25 (100%), gap 0.
- Page/API/test matrix has been established for all 83 pages: `docs/engineering/page-api-test-matrix-2026-03-05.md`.
- DOM assertions now cover all 83 pages (`ui-dom-smoke` mode `full-83`), including all client/admin routes in current matrix.
- Remaining blind spots: `ui-http-smoke` route reachability now covers all 83 configured page routes plus app root (86 checks including 2 mock endpoints), and now also checks content-type/body-length semantics, but still needs deeper page-business semantics; DOM assertions also need continued semantic hardening on some pages.
- E2E path automation is still pending for critical client/admin journeys.

### 1.6 Dependency/security baseline
- `pnpm audit --prod`: 42 vulns (critical 2 / high 21 / moderate 14 / low 5).
- Notable hot packages: `axios`(5), `follow-redirects`(4), `fast-xml-parser`(3), `multer`(3), `swiper`(1), `xlsx`(2).
- CI currently has no vulnerability gate.

### 1.7 Engineering consistency
- Tracked files with UTF-8 BOM: ~145 (`apps` 116 / `docs` 25 / `packages` 3 / root 1).
- BOM in generated JSON artifacts causes cross-tool parsing noise.

### 1.8 Script robustness
- `verify` port fallback was fixed in this batch; WeApp route smoke now also has launch retry/backoff + optional stale DevTools cleanup + per-attempt diagnostics.
- `api-real-smoke` / `ui-http-smoke` / `ui-render-smoke` / `ui-dom-smoke` now avoid kill-by-port and use child-process cleanup + dynamic port fallback.
- `api-real-smoke` now disables local rate-limit (`RATE_LIMIT_ENABLED=false`) during run to reduce false 429 flakiness while validating business paths.
- `api-real-smoke` now keeps a larger response truncation ceiling (512 KB) to prevent false JSON-assert failures on large list payloads.
- `verify` now appends `NODE_OPTIONS=--max-old-space-size=4096` and retries transient `client:build:h5` crash exit codes once.
- `verify` now passes configurable chaos history path into `api-real-smoke` and writes per-report snapshots (`.tmp/api-real-smoke-chaos-history-<ReportDate>.json`) for trend reproducibility.
- `verify` now runs `api-smoke-openapi-coverage` immediately after `api-real-smoke` to block non-auth/non-payment OpenAPI coverage regressions.
- `verify` now runs `api-smoke-quality-floor` immediately after coverage gate to block major regressions in negative/semantic assertion density.

---

## 2. Execution strategy

- **P0 close-out first**: A + D + H + N.
- **P1 hardening**: B/C/E/F/G/J/M first-stage items.
- **P2 institutionalize**: K/L and long-term process controls.
- Every task must have acceptance criteria, artifacts, and script-reproducible steps.

---

## 3. Full TODO

## A. Quality gates and build stability (P0)
- [x] A01 Fix current lint blocker (Inventors unused variable).
  - Acceptance: full repo `pnpm lint` passes.
- [x] A02 Run `scripts/verify.ps1` to full green baseline (excluding real login/payment).
  - Acceptance: openapi lint, lint, typecheck, build, api smoke, api smoke openapi coverage, api smoke quality floor, db preflight, ui http smoke, ui render smoke(core) all pass.
- [x] A03 Improve `verify` port resilience for OS reserved-port scenarios.
  - Acceptance: auto fallback works even when `ApiPort..ApiPort+30` is unavailable.
- [x] A04 Fix admin config write-path 500 regression (`/admin/config/*`).
  - Acceptance: `PUT /admin/config/trade-rules|customer-service|recommendation|alerts|banner|taxonomy|sensitive-words|hot-search` all return 200 and create audit logs successfully.

## B. Test system completion (P0-P1)
- [x] B01 Introduce API unit/integration test framework (Vitest/Jest + Supertest + test DB).
  - Acceptance: executable `test` scripts in `apps/api`; CI can run minimal set. (done: `apps/api` now has `vitest + supertest + @nestjs/testing`, `pnpm -C apps/api test` green `2/2`; CI adds `API minimal tests` step; first e2e at `apps/api/test/health.e2e-spec.ts`)
- [ ] B02 Build write-first API test inventory (orders/refunds/invoices/comments/favorites/addresses/audit flow).
  - Acceptance: first batch covers >=30 key write APIs with success/failure/idempotency assertions.
  - Incremental progress (Vitest): added write-first suites for `favorites`, `addresses`, `comments`, and `orders`, plus strict-filter/query-path suites for `audit-logs`, `listings.searchPublic`, `organizations/inventors` filters, `tech-managers` filters/sanitization, `patent-map` filter/import strictness, `patent-clusters` public pagination/tag sanitization strictness, `files` temporary-access scope/ttl strictness, `files.service` temp-token strictness, `users` profile+verification strictness, `auth.service` strictness, `auth.controller` delegation, `bearer-auth.guard` strictness, `verified-user.guard` strictness, `permissions` utility strictness, `all-exceptions.filter` strictness, `rate-limit.guard` strictness, `file-access.guard` strictness, `request-id.middleware` strictness, `request-logger.middleware` strictness, `content-audit.service` strictness, `listings.controller` delegation strictness, `demands.controller` delegation strictness, `achievements.controller` delegation strictness, `artworks.controller` delegation strictness, `webhooks.service` strictness, `webhooks.controller` delegation, `demo.config` strictness, `addresses.list` strictness, `addresses.controller` delegation, `admin-user-verifications` controller strictness, `admin-config` controller strictness, `config.service` behavior strictness, `public-config` controller strictness, `ai.query` filter sanitization/strictness, `announcements` filters/tag sanitization, `admin-regions.controller` delegation strictness, `admin-patent-map.controller` delegation strictness, `alerts.controller` delegation strictness, `alerts` acknowledge strictness, `audit-logs.controller` delegation strictness, `reports.controller` delegation strictness, `rbac.controller` delegation strictness, `favorites.controller` delegation strictness, `contracts` list-filter strictness, `contracts` upload strictness, `reports` range-filter strictness, `cases` list-filter strictness, `notifications` filter/id strictness, `notifications` write-path strictness, `user-verifications` admin-list strictness, `alerts` list-filter strictness, `patents` admin-list strictness, `conversations` pagination/id strictness, `demands` search/list strictness, `achievements` search/list strictness, `artworks` search/list strictness, `orders` list/invoice strictness, `patent-maintenance` list strictness, `favorites` list strictness, `rbac` read/update strictness, `regions` sanitization, and shared tag-sanitizer guards, plus `cases.controller`/`comments.controller`/`conversations.controller`, `my-verification.controller`/`industry-tags.controller`/`organizations.controller`/`inventors.controller`/`notifications.controller`/`regions.controller`, `ai.controller`/`admin-ai.controller`/`tech-managers.controller`/`patent-map.controller`, and `contracts.controller`/`patents.controller`/`patent-maintenance.controller`/`users.controller`/`patent-clusters.controller`/`files.controller` delegation strictness, plus `inventors` ranking-query branch, `organizations` detail coverage, `tech-managers` update/public coverage, and `ai` feedback/admin parse-update coverage (`466` assertions total), plus existing health e2e (`2` assertions); `pnpm -C apps/api test` now passes `468/468`.
  - Progress: smoke batch now executes 1297 write assertions (favorites/comments/addresses/conversations/consultations + auth + admin-config writes + admin order/refund negative paths + order/admin happy-path state transitions + file-dependent payout/invoice paths + refund approve/complete/reject lifecycle checks + admin case workflows + admin case-create strict invalid type/status/priority negatives + patent-maintenance schedules/tasks workflows + rbac role/user workflows + reports export + patent-map import + patent-map entry upsert + patents normalize + AI parse/feedback + admin industry-tag create/duplicate-invalid + admin patents create/update happy-path + patents strict-validation negatives (including invalid sourcePrimary/legalStatus create/update) + admin announcements create/update/delete happy-path + announcements strict-validation negatives + admin demands create/update/publish/off-shelf/approve/reject happy-path + strict invalid-enum negatives + admin achievements create/update/publish/off-shelf/approve/reject happy-path + strict invalid-enum negatives + admin artworks create/update/publish/off-shelf/approve/reject happy-path + strict invalid-enum negatives + admin listings create/update/publish/off-shelf/approve/reject happy-path + strict invalid-enum-and-amount negatives + user listings create/update happy-path + strict invalid-enum-and-amount negatives + region create/update happy-path + region create/update strict-validation negatives + region industry-tags success/invalid-body/missing-field/invalid-code/missing paths + listing featured CITY/NONE success + listing featured validation negatives (missing region/invalid level/rank/until/missing listing) + listing featured empty-rank strict negatives + listing/demand/achievement/artwork approve/reject missing-id 404 negatives + admin listing/demand/achievement/artwork/announcement publish/off-shelf missing-id 404 negatives + admin tech-manager update missing-id/invalid-rank/empty-rank/invalid-until/invalid-service-tags negatives + tech-manager update happy-path assertion + admin order manual-payment/contract/transfer/payout/invoice invalid-datetime negatives + admin order contract-signed strict deal-amount negatives + admin maintenance schedule year strict negatives + file temporary-access create + comment empty-text create/update strict-400 negatives + user-verification approve/reject missing-id 404 + reject-missing-reason 400 negatives + same-key replay probes for order/payment/invoice/refund/AI feedback + concurrency race matrices including mixed payout/invoice/refund overlap + repeated mixed bursts on same aggregate + cross-order parallel payouts + staggered mixed-tail overlap + jittered repeated overlap loops + randomized multi-iteration seeded overlap harness + randomized multi-order/multi-aggregate overlap harness + larger-seed chaos overlap harness with percentile + trend guards + order-flow precondition hardening + maintenance create-status strict negatives + ai-query strict negatives + order-payment-intent payType strict negatives + search-enum strict negatives + search-listings numeric-filter strict negatives + public detail read-path assertions + rbac privilege-boundary assertions (including minimal-permission `report.read` allow + multi-admin-forbidden matrix + critical admin write-forbidden checks + expanded admin read-surface forbidden checks + unauthenticated 401 boundary checks + critical admin write-path unauthorized/post-clear convergence checks including report-export and rbac role/user mutation endpoints + admin settlement/refund full-lifecycle write-path unauthorized and convergence checks + admin case/maintenance/verifications-reject write-path unauthorized and convergence checks + admin config write-path unauthorized/custom-role/post-clear convergence checks across all 8 `PUT /admin/config/*` endpoints + admin industry-tags/regions/region-industry-tags/listing-featured/tech-manager/patent-map-import/rbac-role-create/order-issue-invoice unauthorized-custom-role-post-clear convergence checks + admin listings/demands/achievements/artworks full write-surface unauthorized-custom-role-post-clear convergence checks + admin announcements/patents/case-notes/rbac-mutation(custom-role) convergence checks + ai-parse-result update 404-compatible boundary checks + admin alerts ack/admin comments update write-surface (happy/missing/invalid + unauthorized/custom-role/post-clear + status transition read-back) checks + admin patent-map entry upsert and patents-normalize convergence checks + patent-map year integer strict-negatives + reports days strict-invalid negatives + ai-parse-feedback score strict-invalid negatives + file-temporary-access ttl strict-invalid negatives + contract-signed dealAmountFen strict-invalid negatives + search-listings numeric-filter strict-invalid negatives + listing-featured empty-rank strict-invalid negatives + maintenance-schedule year strict-invalid negatives + tech-manager featuredRank empty strict-invalid negatives + listing transferCount strict-invalid negatives + demand/artwork numeric decimal strict-invalid negatives + search demand/artwork empty numeric-filter strict-invalid negatives + patent-map import decimal strict-invalid negatives + demand empty-budget-string strict-invalid negatives + artwork empty-price/deposit-string strict-invalid negatives + listing empty-price/deposit-string strict-invalid negatives) + user-side critical write-path unauthorized checks (order create/payment-intent/refund/invoice-request + favorites add/remove + comments update/delete + address update/delete + conversation upsert/read + message send + listing create/update + listing consultations create + AI parse feedback create(existing-id) + file temporary-access create/missing + file upload + me patch + me verification + listing submit/off-shelf + demand/achievement/artwork create/update/submit/off-shelf + demand/achievement/artwork comment create/delete + contract upload) + me-verification strict enum negatives + pagination strict negatives for order/invoice/search/announcements/organizations/audit-logs/contracts/cases/alerts/notifications/comments/favorites/conversations/ai/maintenance/patents/user-verifications endpoints), plus 109 semantic read-back checks for state continuity/persistence integrity, with failure/idempotency assertions, targeted regression checks, mixed-race invoice consistency closure, randomized outcome-distribution assertions, percentile-based stability thresholding, and cross-run trend baseline support added.
- [ ] B03 Add frontend E2E for key H5/admin paths (excluding real login/payment).
  - Acceptance: homepage/search/detail/publish/order/audit flows are script-regressible.
- [x] B04 Upgrade `api-real-smoke` from read-heavy to read-write balanced.
  - Acceptance: write coverage raised from 1.48% to >=20% (phase 1). (done: >=132/135 unique operations covered, currently 1297 write assertions executed in smoke)
- [ ] B05 Define per-domain write coverage targets (`admin`, `listings`, `demands`, `achievements`, `artworks`, `me`, `orders`).
  - Acceptance: each domain has target + template assertions.

## C. API rigor and typing governance (P0-P2)
- [ ] C01 Introduce DTO validation (`class-validator` + `class-transformer` + global `ValidationPipe`).
  - Acceptance: all updated/new endpoints have DTOs; invalid input returns normalized 400.
- [ ] C02 Reduce Controller `any` (priority: orders/listing/audit/ticket modules).
  - Acceptance: `@Body() ...: any` drops from ~65 to <=20.
- [ ] C03 Reduce Service-layer `any` hotspots.
  - Acceptance: `apps/api/src` `any` count down >=40% (phase target).
- [ ] C04 Add state-machine assertions (order/refund/invoice transitions).
  - Acceptance: illegal transitions are blocked with automated positive/negative tests.

## D. WeApp bundle and frontend performance (P0)
- [x] D01 Identify WeApp style bloat root causes (NutUI imports/global injection/duplication).
  - Acceptance: root-cause report with file-level contribution and fix plan. (done: `docs/engineering/weapp-bundle-root-cause-2026-03-05.md`)
- [x] D02 Reduce `app-origin.wxss` and core page wxss size.
  - Acceptance: `app-origin.wxss` first target <500 KB, then continuous reduction. (done: 286,492 bytes)
- [x] D03 Add hard WeApp bundle budget gate (fail, not warning).
  - Acceptance: CI/local verify fails when threshold exceeded. (done: `scripts/check-weapp-bundle-budget.mjs` + CI/verify integration)
- [x] D04 Track bundle trend by day/PR.
  - Acceptance: trend section added to `docs/engineering/test-report.md`. (done)

## E. Security surface and authorization (P1)
- [ ] E01 Review `/uploads` direct exposure vs controlled download risk.
  - Acceptance: production policy defined + regression tests.
- [ ] E02 Upgrade memory rate-limit to scalable distributed limit (Redis).
  - Acceptance: consistent behavior across multi-instance with stress validation.
- [ ] E03 Add security tests for high-risk APIs (IDOR/guessing/replay/duplicate submit).
  - Acceptance: automated privilege/abuse tests for admin/order/file APIs.
  - Progress: RBAC privilege-boundary smoke matrix landed (`report.read` allow + forbidden checks on listings/audit-logs/cases/maintenance/user-verifications/config/rbac/report-export and admin order/case/maintenance/verification write endpoints, plus unauthorized 401 checks and post-role-clear 403 convergence checks on critical admin write paths including report-export, RBAC role/user mutation, settlement/refund full-lifecycle, and admin case/maintenance/verifications-reject write endpoints), `requirePermission` deny path is normalized to 403 (not 500), and `BearerAuthGuard` no longer elevates `isAdmin` by arbitrary custom role assignment.

## F. Data consistency and DB quality (P1)
- [ ] F01 Expand `db-preflight` to post-write validation scenarios.
  - Acceptance: flow tests auto-run consistency checks and archive results.
- [ ] F02 Build staging snapshot replay workflow.
  - Acceptance: migration + preflight + replay before release with unified report.
- [ ] F03 Add slow-query baseline and index review report.
  - Acceptance: top SQL latency baseline with optimization actions.

## G. CI/CD and observability (P1)
- [ ] G01 Add test stage in CI (API integration + minimal frontend e2e).
  - Acceptance: PR must pass business tests, not only lint/typecheck/build.
  - Progress: CI now includes API minimal test gate (`pnpm -C apps/api test`); frontend e2e gate remains pending.
- [ ] G02 Run smoke scripts in reproducible environment.
  - Acceptance: local and CI parity with actionable failure logs.
- [ ] G03 Improve metrics baseline (error rate, latency, key flow success).
  - Acceptance: metric catalog + alert thresholds + dashboard/config artifact.
- [ ] G04 Add vulnerability gate in CI (at least block critical).
  - Acceptance: new critical vulns fail PR/main build.

## H. Documentation and release governance (P0-P1)
- [x] H01 Update `docs/engineering/test-report.md` with latest results and risk notes.
  - Acceptance: commands/results/failures/fix-status traceable.
- [ ] H02 Sync `docs/engineering/overall-todo.md` with this file as single source.
  - Acceptance: no conflicting dual-track TODOs.
- [ ] H03 Maintain execution board fields (owner/plan/completion/blocker).
  - Acceptance: all active tasks have owner and timebox.

## I. Deferred (out of current execution)
- [ ] I01 Real WeChat login.
- [ ] I02 Real WeChat payment/refund/callback verification.
- [ ] I03 AI production integration.

## J. Page coverage and regression matrix completion (P0-P1)
- [x] J01 Build 3D matrix: page-api-test (client/admin).
  - Acceptance: each page maps to HTTP/render/e2e/manual coverage + owner. (done: `docs/engineering/page-api-test-matrix-2026-03-05.md`, generated by `scripts/build-page-api-test-matrix.mjs`)
- [x] J02 Extend `ui-render-smoke` to missing admin core routes.
  - Acceptance: admin coverage from 48% to >=80% (phase 1). (done: 25/25 = 100%)
- [x] J03 Extend `ui-render-smoke` to key client subpackage pages.
  - Acceptance: client coverage from 24.1% to >=60% (phase 1). (done: 58/58 = 100%)
- [x] J04 Upgrade UI smoke from status-only to DOM assertion based.
  - Acceptance: each page has 1-2 stable business DOM assertions. (done for core 11 pages via `scripts/ui-dom-smoke.ps1` + `scripts/dump-dom-cdp.mjs`)
- [x] J05 Execute gap closure in batches (>=8 pages per batch).
  - Acceptance: each batch updates coverage stats and remaining gap list. (done in 2026-03-05 batch: full-mode 83/83)
- [x] J06 Expand DOM assertions to first full-mode batch.
  - Acceptance: DOM assertions cover >=30 pages with stable pass. (done: `ui-dom-smoke -Mode full` -> 36/36, mode `full-batch1`)
- [x] J07 Continue DOM expansion from 36/83 to full 83/83.
  - Acceptance: each new batch adds >=8 pages and updates matrix/risk notes. (done: `ui-dom-smoke -Mode full` -> 83/83, mode `full-83`)
- [x] J08 Expand `ui-http-smoke` route reachability to full client/admin page set.
  - Acceptance: checks include all 83 configured client/admin page routes (plus mock endpoints), with script-generated client route list from app config. (done: `ui-http-smoke` 86/86)
- [x] J09 Add response-shape assertions to `ui-http-smoke`.
  - Acceptance: every HTTP smoke probe asserts expected content type and minimum response body length in addition to status code.

## K. Dependency security and version governance (P0-P2)
- [x] K01 Build vulnerability ledger (severity + business impact + upgradeability).
  - Acceptance: prioritized remediation strategy per issue. (done: `docs/engineering/vulnerability-ledger-2026-03-05.md`, generated by `scripts/audit-vulnerability-ledger.mjs`)
- [ ] K02 Resolve critical/high fixable issues first.
  - Acceptance: critical to 0, high significantly reduced.
- [ ] K03 Add vulnerability gate to CI.
  - Acceptance: new critical blocked in PR/mainline.
- [ ] K04 Define periodic dependency upgrade cadence (weekly/monthly) + regression scripts.
  - Acceptance: fixed upgrade windows and rollback/testing playbook.

## L. Engineering consistency and maintainability (P1)
- [ ] L01 Standardize encoding (remove BOM, enforce UTF-8 no-BOM policy).
  - Acceptance: tracked BOM count reduced from ~145 toward 0 (phase-based cleanup).
- [ ] L02 Standardize script output encoding (PowerShell/Python/Node).
  - Acceptance: logs and reports parse consistently cross-platform.
- [ ] L03 Script key quality metrics (coverage/any count/bundle trend/vuln count).
  - Acceptance: one-command weekly quality report generation.

## M. Feature-level usability closure (P0-P1)
- [ ] M01 Build client feature closure matrix (exclude real login/payment).
  - Acceptance: all major client areas have reproducible validation paths.
- [ ] M02 Build admin feature closure matrix.
  - Acceptance: each admin module has at least one happy-path + one failure-path check.
- [ ] M03 Validate demo auth/payment boundary behavior (dev enabled, staging/prod blocked).
  - Acceptance: script-enforced checks for both usability and safety boundaries.

## N. Script resilience and environment compatibility (P0-P1)
- [x] N01 Upgrade `verify` port selection (preferred + extended scan + random fallback).
  - Acceptance: stable execution on Windows excluded-port scenarios.
- [x] N02 Refactor smoke process cleanup (kill only spawned child processes).
  - Acceptance: no accidental termination of unrelated local services.
- [x] N03 Add collision self-healing and clear diagnostics to smoke scripts.
  - Acceptance: conflicts auto-resolve or fail with explicit retry guidance.
- [x] N04 Add memory/process hardening for long smoke/verify runs.
  - Acceptance: no leaked process trees after smoke runs; verify survives transient `client:build:h5` crashes with bounded retry.
- [x] N05 Add OpenAPI-to-smoke coverage gate in `verify` (excluding real login/payment integrations).
  - Acceptance: `verify` fails if any non-auth/non-WeChatPay webhook OpenAPI operation is missing from `api-real-smoke`.
- [x] N06 Add API smoke quality-floor gate in `verify`.
  - Acceptance: `verify` fails when API smoke total/write/read/negative-depth or key error-code distribution regresses below minimum baseline.

---

## 4. Milestones and exit criteria

### M1 (P0 close-out)
- Condition: P0 items in A + D + H + N completed.
- Exit:
  - full lint/typecheck/build/verify green;
  - WeApp bundle significantly reduced with hard gate;
  - test report and TODO synchronized.

### M2 (P1 hardening)
- Condition: first-stage B/C/E/F/G/J/M completed.
- Exit:
  - write API automation reaches phase target;
  - security/permission/data consistency have regression automation;
  - CI has business-test + vulnerability gates;
  - page automation coverage reaches phase target.

### M3 (P2 institutionalization)
- Condition: K/L and release process institutionalized.
- Exit:
  - rigor metrics stay stable;
  - release checks are templated and traceable;
  - supply-chain risk handled in recurring governance cadence.

---

## 5. Execution board (2026-03-07)

| ID | Status | Owner | Planned | Completed | Blocker / Notes |
| --- | --- | --- | --- | --- | --- |
| A01 | done | Codex | 2026-03-05 | 2026-03-05 | full repo lint passed in verify |
| A02 | done | Codex | 2026-03-05 | 2026-03-05 | full verify passed |
| A03 | done | Codex | 2026-03-05 | 2026-03-05 | fallback validated (`3200` unavailable -> `3302`) |
| A04 | done | Codex | 2026-03-06 | 2026-03-06 | `/admin/config/*` PUT now passes with UUID audit targets; smoke asserts audit-log increment |
| H01 | done | Codex | 2026-03-05 | 2026-03-06 | test report refreshed with latest run |
| N01 | done | Codex | 2026-03-05 | 2026-03-05 | preferred+range+random fallback implemented |
| N02 | done | Codex | 2026-03-06 | 2026-03-05 | `api-real`/`ui-http`/`ui-render`/`ui-dom` now clean up spawned process trees |
| N03 | done | Codex | 2026-03-06 | 2026-03-05 | dynamic fallback validated via forced collision (`4010` blocker) |
| N04 | done | Codex | 2026-03-06 | 2026-03-05 | verify heap/retry hardening + smoke leak cleanup validated in repeated runs |
| N05 | done | Codex | 2026-03-07 | 2026-03-07 | `verify` now runs `api-smoke-openapi-coverage`; non-auth/non-payment OpenAPI coverage is gated at 238/238 |
| N06 | done | Codex | 2026-03-07 | 2026-03-07 | `verify` now runs `api-smoke-quality-floor`; API smoke intensity is gated (total/write/read/negative depth + status distribution + admin density) |
| D01 | done | Codex | 2026-03-06 | 2026-03-05 | root cause report completed and documented |
| D02 | done | Codex | 2026-03-06 | 2026-03-05 | wxss size reduced below phase targets |
| D03 | done | Codex | 2026-03-06 | 2026-03-05 | hard budget gate enabled in verify + CI |
| D04 | done | Codex | 2026-03-06 | 2026-03-05 | trend section added to test report |
| J01 | done | Codex | 2026-03-06 | 2026-03-05 | page-api-test matrix completed for 83 pages |
| J02 | done | Codex | 2026-03-06 | 2026-03-05 | admin render coverage reached 25/25 (100%) |
| J03 | done | Codex | 2026-03-06 | 2026-03-05 | client render coverage reached 58/58 (100%) |
| J04 | done | Codex | 2026-03-06 | 2026-03-05 | core DOM assertions landed and green (11/11) |
| J05 | done | Codex | 2026-03-06 | 2026-03-05 | coverage gap closure batch executed (83/83 pass) |
| J06 | done | Codex | 2026-03-06 | 2026-03-05 | DOM full-mode batch-1 landed (36/36 pass, matrix 36/83) |
| J07 | done | Codex | 2026-03-06 | 2026-03-05 | DOM full-mode expanded to full 83/83 with matrix sync |
| J08 | done | Codex | 2026-03-07 | 2026-03-07 | `ui-http-smoke` now auto-parses client app routes and reaches 86/86 (83 page routes + app root + 2 mock checks) |
| J09 | done | Codex | 2026-03-07 | 2026-03-07 | `ui-http-smoke` now validates `content-type` + minimum body length for all 86 probes (not only status code) |
| K01 | done | Codex | 2026-03-06 | 2026-03-05 | vulnerability ledger + generator script completed |
| B04 | done | Codex | 2026-03-06 | 2026-03-11 | `api-real-smoke` expanded to 1725/1725 (writes 1290/1290 + reads 435/435 including 109 semantic read-back assertions), with unique write-operation coverage already near-full baseline |
| B02 | in_progress | Codex | 2026-03-06 | - | write batch now includes same-key replay invariants + semantic read-back checks (order/refund/case/maintenance/rbac/report/import/ai) + duplicate-maintenance-schedule 409 regression guard + concurrency race matrices (maintenance create, refund approve/reject, order payout pair/triple, transfer-vs-refund overlap, mixed payout/invoice/refund triple-write, repeated mixed bursts on same order aggregate, cross-order parallel payout, staggered mixed tail overlap, jittered repeated overlap loops, randomized seeded multi-iteration overlap harness with distribution assertions, randomized multi-order/multi-aggregate overlap harness, larger-seed chaos overlap harness with p50/p95/max percentile stats, `p95<=3000ms` stability threshold, and persisted cross-run trend baseline/threshold logic) + invoice consistency closure for mixed-race `invoice_without_file` risk + region/listing-featured strict validation/file-temporary-access deepening + search/public-read strictness and RBAC privilege-boundary regression checks (including role-clear immediate convergence checks) + read-detail closure for `GET /files/{id}`, `GET /files/{id}/preview`, `GET /patent-map/regions/{regionCode}?year=...`, `GET /conversations/{conversationId}/messages`; remaining depth is deeper transaction-isolation windows |

### Current execution batch (Batch-1)
- Scope: A01 / A02 / A03 / N01 / N02 / N03 / N04 / H01 / D01 / D02 / D03 / D04 (completed).
- Deliverables:
  1) resilient `scripts/verify.ps1` port fallback (done),
  2) fresh verify run result (done),
  3) TODO board status refresh (done),
  4) 2026-03-05 test report update (done),
  5) smoke scripts migrated to dynamic-port + child-process cleanup (done),
  6) WeApp bundle regression fixed and gated (done).

### Current execution batch (Batch-2)
- Scope: J01 / J02 / J03 / J04 / J05 / J06 / J07 / K01 (completed), K02 pending.
- Deliverables:
  1) full page coverage run (`ui-render-smoke -Mode full`) with 83/83 pass (done),
  2) page-api-test matrix for all pages (done),
  3) vulnerability ledger generated from latest audit report (done),
  4) core DOM assertion smoke completed and integrated into `verify` (done),
  5) DOM full-mode batch-1 completed (`ui-dom-smoke -Mode full`: 36/36; matrix DOM 36/83) (done),
  6) DOM full-mode expanded to all pages (`ui-dom-smoke -Mode full`: 83/83; matrix DOM 83/83) (done),
  7) remaining focus narrowed to critical/high dependency remediation + semantic-strengthening of DOM assertions (pending).

### Current execution batch (Batch-3)
- Scope: B04 close-out + B02 first write batch (in progress).
- Deliverables:
  1) `api-real-smoke` expanded from 17 to 1725 checks (done),
  2) write checks expanded from 2 to 1290 assertions and read-back checks expanded to 109 semantic assertions (favorites/comments/addresses/conversations/consultations/auth/admin-config/admin-order-refund-negative/order-admin-happy-path/file-dependent payout-invoice/refund lifecycle/admin-case workflows/admin case-create invalid-enum negatives/patent-maintenance workflows/rbac workflows/reports export/patent-map import/patent-map entry upsert/patents normalize/ai parse feedback/admin industry-tag create+list/public list/admin patents create+update happy+negative/admin announcements create+update+delete happy+negative/admin demands create+update+publish+off-shelf+approve+reject happy+negative/admin achievements create+update+publish+off-shelf+approve+reject happy+negative/admin artworks create+update+publish+off-shelf+approve+reject happy+negative/admin listings create+update+publish+off-shelf+approve+reject happy+negative/user listings create+update happy+negative/admin region create+update happy+negative/region industry-tags success+invalid-body+missing-field+invalid-code+missing/listing featured success+validation negatives+empty-rank strict negatives/listing+demand+achievement+artwork approve+reject missing-id 404/admin listing+demand+achievement+artwork+announcement publish+off-shelf missing-id 404/admin tech-manager update missing+invalid+empty-rank+happy-path assertions/admin order manual-payment+contract+transfer+payout+invoice invalid-datetime negatives/admin order contract-signed dealAmountFen strict-invalid negatives/admin maintenance schedule year strict-invalid negatives/search-listings numeric-filter strict-invalid negatives/file temporary-access/comment create+update empty-text strict-400 negatives/user-verification approve/reject missing-id 404 + reject-missing-reason 400 + same-key replay probes + concurrency race probes + larger-seed chaos percentile/trend harness + listing precondition hardening + patent strict negatives for sourcePrimary/legalStatus + maintenance strict negatives for create status + ai-query strict negatives for contentScope/contentType + order payment-intent strict negatives for payType + search enum strict negatives + public detail reads + RBAC privilege-boundary assertions + critical admin write-path unauthorized/post-clear convergence assertions (including report-export, rbac role/user mutation, settlement/refund full-lifecycle, case/maintenance/verifications-reject endpoints, all 8 admin-config PUT endpoints, industry-tags/regions/region-industry-tags/listing-featured/tech-manager/patent-map-import/rbac-role-create/order-issue-invoice endpoints, listings/demands/achievements/artworks full write endpoints, announcements/patents/case-notes/rbac-mutation+ai-parse-update-404-compatible endpoints, and newly closed admin alerts ack + admin comments update + admin patent-map entry upsert + patents normalize write surfaces + patent-map year decimal strict negatives + listing transferCount strict negatives + demand/artwork numeric decimal strict negatives + search demand/artwork empty numeric-filter strict negatives + patent-map import decimal strict negatives + demand empty-budget-string strict negatives + artwork empty-price/deposit-string strict negatives + listing empty-price/deposit-string strict negatives + listing empty-negotiable-range-percent strict negatives + orders empty-datetime-string strict negatives + patents empty-date-datetime-string strict negatives + artworks empty-creation-date-string strict negatives + listings empty-date-and-filter-string strict negatives + alerts/maintenance empty-date-filter-string strict negatives + cases empty-due-at-string strict negatives + reports empty-start/end-string strict negatives + listing-featured/tech-manager empty-featured-until-string strict negatives + maintenance-schedule empty-date-string strict negatives + listings empty-nullable-enum-string strict negatives + content user/admin nullable-enum empty-string + user invalid-enum strict negatives + patents empty legalStatus strict negatives + listings patent legalStatus/sourcePrimary strict negatives + patents upsert null legalStatus clear regression + patents upsert null filingDate clear regression + listings patent null filingDate clear regression + alerts/maintenance empty query-id strict regression + audit-logs empty query-id strict regression + listings regionCode null-clear/empty strict regression + demands/achievements/artworks regionCode null-clear/empty strict regression + me addresses regionCode null-clear/empty strict regression + me patch regionCode null-clear/empty strict regression + admin region update null/empty name-level strict regression + regions/patent-map empty-filter strict regression + ai-agent-query regionCode strict regression + search/recommendations empty regionCode strict regression + inventor/org/tech-manager empty regionCode strict regression + listing-search empty seller/cluster strict regression + content-create empty regionCode strict regression + listing-create-path empty regionCode strict regression + listing clusterId empty-string strict regression + case-create empty orderId/assigneeId strict regression + maintenance-task-create empty assignedCsUserId strict regression + maintenance-task-update empty assigned/evidence id strict regression + order-milestones empty evidenceFileId strict regression + content-cover-file-id empty-string strict regression + comment-create-empty-parent-comment-id strict regression + admin-listing-update-empty-seller-user-id strict regression + admin-content owner-user-id empty-string strict regression + admin-comments-empty-content-id strict regression + admin-listing-create-empty-seller-user-id strict regression + me-verification-empty-id-code-fields strict regression + me-verification-empty-evidence-file-id strict regression + admin-order-upsert-invoice-empty-invoice-no strict regression + admin-announcement-empty-issue-no strict regression + admin-patent-empty-application-no-display strict regression + admin-order-manual-payment-empty-trade-no strict regression + admin-case-create-empty-title-requester-name strict regression + listings-negotiable-note-empty-string strict regression + listings-encumbrance-note-empty-string strict regression + listings-title-empty-string strict regression + listings-summary-empty-string strict regression + demands-contact-name-empty-string strict regression + demands-contact-title-empty-string strict regression + demands-contact-phone-masked-empty-string strict regression + demands-summary-empty-string strict regression + demands-description-empty-string strict regression + achievements-summary-empty-string strict regression + achievements-description-empty-string strict regression + artworks-description-empty-string strict regression + artworks-certificate-no-empty-string strict regression + artworks-material-empty-string strict regression + artworks-size-empty-string strict regression + artworks-title-empty-string strict regression + artworks-creator-name-empty-string strict regression + artworks-update-omit-fields-preserve regression + artworks-update-null-title-creator-name strict regression + achievements-title-empty-null-update strict regression + demands-title-empty-null-update strict regression + demand-achievement-create-empty-title strict regression + announcement-patent-title strict regression + patents-upsert-abstract-omit-preserve strict regression + patents-upsert-application-no-display-omit-preserve strict regression + patents-upsert-source-meta-omit-preserve strict regression + listing-validation-no-patent-side-effect strict regression + case-evidence-invalid-file-id strict regression + case-note-empty-required strict regression + case-uuid-format strict regression + order-upsert-invoice-file-id-format strict regression + invoice-admin-order-id-format strict regression + user-invoice-order-id-format strict regression + order-detail-payment-intent-order-id-format strict regression + order-case-refund-order-id-format strict regression + admin-order-core-order-id-format strict regression + admin-refund-request-id-format strict regression + order-listing-id-and-payout-evidence-file-id-format strict regression + file-route-file-id-format strict regression + contract-upload-id-format strict regression + conversation route-id-format strict regressions for listing/demand/achievement/artwork/tech-manager upsert and conversation messages/send/read endpoints + comment route-id-format strict regressions for public listing/demand/achievement/artwork comment lists, listing/demand/achievement/artwork comment create routes, comment edit/delete/admin-update routes, parentCommentId body, and admin comments contentId filter + alert/notification id-format strict regressions for notification detail, admin alerts targetId filter, and admin alert ack route id + favorite route-id-format strict regressions for listing/demand/achievement/artwork favorite add/remove endpoints + address route-id-format strict regressions for me address update/delete endpoints + organization/tech-manager route-id-format strict regressions for public organization detail, public tech-manager detail, and admin tech-manager update route IDs + announcement route-id-format strict regressions for public announcement detail and admin announcement update/publish/off-shelf/delete route IDs + ai parse-result route-id-format strict regressions for admin parse-result get/update and user feedback routes (400/404 compatible in optional AI module paths)) + expanded user-side critical write unauthorized assertions (favorites add/remove, comments update/delete, address update/delete, conversation upsert/read, message send, listing create/update, listing consultations create, AI parse feedback create(existing-id), file temporary-access create/missing + file upload + me patch + me verification + listing submit/off-shelf + demand/achievement/artwork create/update/submit/off-shelf + demand/achievement/artwork comment create/delete + contract upload) + me-verification strict enum negatives + pagination strict negatives) (done),
  3) first failure-path/idempotency assertions added (duplicate favorites, invalid comment/message, missing-address delete) (done),
  4) `verify` rerun full green with new API smoke baseline (done),
  5) admin config write-path defect fixed (`PUT /admin/config/*` now passes with audit-log increment assertions) (done),
  6) admin issue-invoice missing-order defect fixed (`POST /admin/orders/:orderId/invoice` no longer returns Prisma 500, now 404) (done),
  7) order/admin happy-path state transitions added (order create, deposit/final paid, contract/transfer milestones, settlement) with conflict-path assertions (done),
  8) file-dependent happy paths (`invoiceFileId`, `payoutEvidenceFileId`) landed with `/files` upload + payout/invoice assertions (done),
  9) refund lifecycle deepening landed (approve/reject/complete happy+conflict paths) (done),
  10) admin case workflow deepening landed (`/admin/cases` create/assign/status/note/evidence/sla happy+negative paths) (done),
  11) patent-maintenance schedules/tasks deepening landed (happy+negative paths for create/update/detail/list) (done),
  12) RBAC role/user deepening landed (create/update/delete role + user role assignment happy+negative paths) (done),
  13) reports export/patent-map import checks landed (including invalid-range and missing-file guards) (done),
  14) semantic read-back/state continuity checks deepened (order/refund/case/maintenance/rbac/report/import) and single-failure summary counting edge fixed (done),
  15) same-idempotency replay invariants landed for order create/payment-intents/invoice-request/refund-create (done),
  16) duplicate maintenance-schedule create defect fixed (`patentId + yearNo` unique collision now returns 409 instead of 500) with regression assertion (done),
  17) first concurrency/state-race matrices landed (maintenance schedule duplicate create race + refund approve/reject race, both asserting one success + one conflict and converged final state) (done),
  18) cross-domain high-contention race landed for order settlement (`/admin/orders/:id/payouts/manual` concurrent writes -> one success + one conflict + post-state convergence assertions) (done),
  19) overlap race between settlement and refund landed (user refund-create vs admin transfer-completed on same order, with branch convergence assertions) (done),
  20) higher fan-out (3 concurrent writes) transaction race landed for order payout (one success + two conflicts + converged settlement/order state) (done),
  21) multi-endpoint 3+ fan-out race landed for same-order mixed writes (`payout + invoice-request + refund-request`) with convergence assertions and post-race conflict guards (done),
  22) mixed-race invoice consistency closure landed (`admin invoice upsert` with file link) to prevent `invoice_without_file` preflight drift (done),
  23) repeated mixed overlap bursts landed on same order aggregate (post-terminal repeated concurrent `payout + invoice-request + refund-request` with bounded outcomes and terminal-state re-checks) (done),
  24) mixed overlap expansion landed to multi-order and staggered-start bursts (cross-order parallel payout success guarantees + post-payout staggered invoice/refund overlap guarantees) (done),
  25) jittered repeated race loops landed (same aggregate sampled across delayed overlap bursts with per-loop bounded outcomes + terminal-state re-check) (done),
  26) randomized multi-iteration race harness landed (batched seeds + per-seed outcomes + aggregated distribution summary assertions) (done),
  27) randomized multi-order/multi-aggregate harness landed (seeded cross-order overlap bursts + per-run outcome capture + distribution assertions) (done),
  28) larger-seed chaos sampling + percentile-based stability thresholding landed (done: `chaos-randomized-outcome-distribution`, `p50/p95/max`, `p95<=3000ms` guard).
  29) expanded seed space + cross-run trend-threshold baseline landed (done: 30 seeded runs, persisted trend history `.tmp/api-real-smoke-chaos-history.json`, rolling baseline window, and activation validated at `priorSamples=6` with `trendThresholdP95=1893ms` and observed `p95=753ms`).
  30) AI/industry-tag/region/featured/file-temporary-access coverage landed (done: `POST /ai/agent/query`, `/admin/ai/parse-results` list/get/update branch coverage, `/ai/parse-results/:id/feedback` create + replay/missing checks, `POST /admin/industry-tags` create+duplicate-invalid checks + admin/public list visibility checks, `POST/PATCH /admin/regions` create+update success and strict input negatives, `PUT /admin/regions/:code/industry-tags` success + invalid-body/missing-field/invalid-code/missing-region checks, `PUT /admin/listings/:id/featured` CITY/NONE success + missing-id 404 + invalid-input guards, and `POST /files/:id/temporary-access` success + missing guards).
  31) featured endpoint validation hardening landed (done: backend now rejects invalid enum/missing region/invalid rank/invalid datetime with 400, matching OpenAPI contract intent).
  32) listings admin validation hardening + coverage landed (done: `POST/PATCH /admin/listings` now strictly validate `source/status/auditStatus/tradeMode/licenseMode/priceType/pledgeStatus/existingLicenseStatus/priceAmountFen/depositAmountFen`; smoke now covers create/update happy+negative + publish/off-shelf/approve/reject happy + update-missing 404).
  33) case create validation hardening + coverage landed (done: `POST /admin/cases` now strictly validates explicit `type/status/priority`; smoke adds invalid-enum create negatives and keeps default behavior when fields are absent).
  34) user listing validation hardening + coverage landed (done: `POST/PATCH /listings` now strictly validate `tradeMode/licenseMode/priceType/pledgeStatus/existingLicenseStatus/priceAmountFen/depositAmountFen`; smoke now covers create/update happy+negative + update-missing 404).
  35) patent strict-negative expansion + order precondition hardening landed (done: smoke adds `admin-patent-create-invalid-source-primary`, `admin-patent-create-invalid-legal-status`, `admin-patent-update-invalid-legal-status`; listing selection now gracefully falls back and auto-prepares audit status before order flow).
  36) maintenance create-status validation hardening + coverage landed (done: `POST /admin/patent-maintenance/schedules|tasks` now strictly reject explicit invalid `status` with 400; smoke adds create-invalid-status negatives for both resources).
  37) AI query enum validation hardening + coverage landed (done: `POST /ai/agent/query` now strictly rejects explicit invalid `contentScope/contentType` with 400; smoke adds invalid-scope and invalid-content-type negatives, while preserving environment-compatible 404 branch).
  38) payment-intent enum validation hardening + coverage landed (done: `POST /orders/:id/payment-intents` now strictly rejects explicit invalid `payType` with 400 and keeps missing-field default path to `DEPOSIT`; smoke adds `order-payment-intent-invalid-pay-type`).
  39) RBAC privilege matrix deepening + permission deny-path normalization landed (done: custom role `report.read` now has explicit allow assertion on finance summary, forbidden assertions on high-risk admin read/write endpoints, unauthenticated 401 boundary checks are now explicit for key protected endpoints, role-clear immediate-convergence checks now assert post-clear 403 on admin listings/report summary and critical write endpoints, and `requirePermission` now returns consistent 403 instead of internal 500 on deny path).
  40) user listing + AI/file unauthorized boundary deepening landed (done: added explicit 401 assertions for `POST /listings`, `PATCH /listings/:id`, `POST /listings/:id/consultations`, `POST /ai/parse-results/:id/feedback` on existing-id, and `POST /files/:id/temporary-access` create/missing paths).
  41) demand/achievement/artwork comment unauthorized boundary deepening landed (done: added explicit 401 assertions for `POST /demands/:id/comments`, `POST /achievements/:id/comments`, `POST /artworks/:id/comments`, and corresponding `DELETE /comments/:id` paths).
  42) user write unauthorized boundary deepening landed for profile/content lifecycle (done: added explicit 401 assertions for `PATCH /me`, `POST /me/verification`, `POST /files` upload, `POST /{listings|demands|achievements|artworks}/:id/{submit|off-shelf}`, and `POST/PATCH /{demands|achievements|artworks}` paths).
  43) contract upload unauthorized boundary landed (done: added explicit 401 assertion for `POST /contracts/:id/upload`).
  44) organizations detail defect fixed (done: `GET /public/organizations/:orgUserId` no longer triggers Prisma `uuid = text` 500; distinct patent count now uses Prisma query path, and smoke includes detail-read regression coverage).
  45) `ui-http-smoke` route coverage expanded and automated (done: client routes now parsed from `apps/client/src/app.config.ts`, HTTP smoke rose from 28 to 86 checks, and full verify remained green).
  46) OpenAPI alignment gate landed in verify (done: `scripts/check-api-smoke-openapi-coverage.mjs` enforces 238/238 non-auth/non-payment operation coverage and fails on any regression).
  47) API smoke quality-floor gate landed in verify (done: `scripts/check-api-smoke-quality-floor.mjs` enforces minimum depth floors for total/write/read/negative checks, key error-code counts, and admin negative density).
  48) `ui-http-smoke` semantic hardening landed (done: all 86 probes now include content-type + minimum body-length assertions, preventing status-only false positives on empty/wrong-type responses).
  49) patent-map year strict-integer hardening landed (done: `year` now rejects decimal input on public/admin patent-map endpoints with 400; regression checks added for summary/detail/admin get/admin upsert).
  50) patent-clusters query strictness hardening landed (done: `/public/patent-clusters` now returns 400 for invalid `page/pageSize` inputs instead of silent fallback; smoke adds `invalid-page` and `empty-page-size` regressions).
  51) reports days strict-parse hardening landed (done: `GET /admin/reports/finance/summary` and `POST /admin/reports/finance/export` now reject invalid/empty `days` with 400; smoke adds `summary-invalid-days`, `summary-empty-days`, `export-invalid-days`, `export-empty-days` regressions).
  52) AI parse feedback score strictness hardening landed (done: `POST /ai/parse-results/:id/feedback` now rejects non-integer/empty `score`; smoke adds `ai-parse-feedback-create-invalid-score-decimal` and `ai-parse-feedback-create-empty-score` regressions).
  53) file temporary-access ttl strictness hardening landed (done: `POST /files/:fileId/temporary-access` now strictly rejects non-integer/empty ttl params and preserves explicit `0` semantics; smoke adds `file-temporary-access-invalid-ttl-decimal` and `file-temporary-access-empty-ttl` regressions).
  54) admin order contract-signed `dealAmountFen` strictness hardening landed (done: `POST /admin/orders/:id/milestones/contract-signed` now rejects empty/non-finite/decimal/non-integer `dealAmountFen`; smoke adds `admin-order-contract-signed-invalid-deal-amount-decimal` regression).
  55) search-listings numeric filters strictness hardening landed (done: `GET /search/listings` now strictly rejects invalid/empty/decimal numeric filters for `priceMin/priceMax/depositMin/depositMax/transferCountMin/transferCountMax`; smoke adds `search-listings-invalid-price-min`, `search-listings-empty-deposit-max-fen`, and `search-listings-invalid-transfer-count-min-decimal` regressions).
  56) listing-featured empty-rank strictness hardening landed (done: `PUT /admin/listings/:id/featured` now rejects empty `featuredRank` instead of silently coercing to `0`; smoke adds `admin-listing-featured-set-city-empty-rank` regression).
  57) maintenance schedule `yearNo` strictness hardening landed (done: `POST /admin/patent-maintenance/schedules` now rejects decimal `yearNo`; smoke adds `admin-maintenance-schedule-create-invalid-year-decimal` regression).
  58) tech-manager update `featuredRank` strictness hardening landed (done: `PATCH /admin/tech-managers/:id` now rejects empty `featuredRank` instead of silently coercing to `0`; smoke adds `admin-tech-manager-update-empty-featured-rank` regression).
  59) listing patent `transferCount` strictness hardening landed (done: listing patent sync now rejects empty/non-finite/decimal/non-integer `transferCount`; smoke adds `listing-create-invalid-transfer-count-decimal`, `listing-create-empty-transfer-count`, `listing-update-invalid-transfer-count-decimal`, and `listing-update-empty-transfer-count` regressions).
  60) demands/artworks numeric-field strictness hardening landed (done: `DemandsService` and `ArtworksService` now reject decimal integer-fields instead of flooring; smoke adds `search-demands-invalid-budget-min-decimal`, `search-artworks-invalid-price-min-decimal`, `admin-demand-create-invalid-budget-min-decimal`, `admin-demand-update-invalid-budget-max-decimal`, `admin-artwork-create-invalid-price-amount-decimal`, and `admin-artwork-update-invalid-deposit-amount-decimal` regressions).
  61) search demand/artwork empty-filter strictness hardening landed (done: `GET /search/demands` and `GET /search/artworks` now reject explicit empty numeric filters with 400 instead of silently ignoring; smoke adds `search-demands-empty-budget-max-fen` and `search-artworks-empty-price-min` regressions).
  62) patent-map import integer strictness hardening landed (done: admin patent-map import now rejects decimal `regionCode/year/patentCount` and nested counts instead of truncating; smoke adds `admin-patent-map-import-dry-run-invalid-decimal` regression with explicit error assertion).
  63) next step: carry trend history across daily/CI runs and calibrate threshold factors using broader variance data (pending).
  64) demands empty-budget-string strictness hardening landed (done: `DemandsService` now rejects explicit empty-string `budgetMinFen/budgetMaxFen` with 400 while keeping `null` clear semantics; smoke adds `admin-demand-create-empty-budget-min-fen`, `admin-demand-update-empty-budget-max-fen`, `demand-create-empty-budget-min-fen`, and `demand-update-empty-budget-max-fen` regressions).
  65) artworks empty-price/deposit-string strictness hardening landed (done: `ArtworksService` now rejects explicit empty-string `priceAmountFen/depositAmountFen` with 400 while keeping `null`/omission semantics; smoke adds `admin-artwork-create-empty-price-amount-fen`, `admin-artwork-update-empty-deposit-amount-fen`, `artwork-create-empty-price-amount-fen`, and `artwork-update-empty-deposit-amount-fen` regressions).
  66) listings empty-price/deposit-string strictness hardening landed (done: `ListingsService` now rejects explicit empty-string `priceAmountFen/depositAmountFen` with 400 while keeping omission semantics and explicit min-checked paths; smoke adds `listing-create-empty-price-amount`, `listing-update-empty-deposit-amount`, `admin-listing-create-empty-price-amount`, and `admin-listing-update-empty-deposit-amount` regressions).
  67) listings empty-negotiable-range-percent strictness hardening landed (done: `ListingsService` now rejects explicit empty-string `negotiableRangePercent` with 400 while keeping omission/`null` semantics; smoke adds `listing-create-empty-negotiable-range-percent`, `listing-update-empty-negotiable-range-percent`, `admin-listing-create-empty-negotiable-range-percent`, and `admin-listing-update-empty-negotiable-range-percent` regressions).
  68) orders empty-datetime-string strictness hardening landed (done: `OrdersService` now rejects explicit empty-string datetime fields with 400 while keeping omission/`null` semantics for optional timestamps; smoke adds `admin-order-manual-payment-deposit-empty-paid-at`, `admin-order-contract-signed-empty-signed-at`, `admin-order-transfer-completed-empty-completed-at`, `admin-order-manual-payout-empty-payout-at`, and `admin-order-upsert-invoice-empty-issued-at` regressions).
  69) patents empty-date/datetime-string strictness hardening landed (done: `PatentsService` now rejects explicit empty-string `filingDate/publicationDate/grantDate/sourceUpdatedAt` with 400 while keeping omission/`null` semantics; smoke adds `admin-patent-create-empty-filing-date`, `admin-patent-create-empty-source-updated-at`, `admin-patent-update-empty-filing-date`, and `admin-patent-update-empty-source-updated-at` regressions).
  70) artworks empty-creation-date-string strictness hardening landed (done: `ArtworksService` now rejects explicit empty-string `creationDate` with 400 while keeping omission/`null` semantics; smoke adds `admin-artwork-create-empty-creation-date`, `admin-artwork-update-empty-creation-date`, `artwork-create-empty-creation-date`, and `artwork-update-empty-creation-date` regressions).
  71) listings empty-date-string strictness and search date-filter strictness landed (done: `ListingsService.parseDateValue` now rejects explicit empty-string date fields when `strict=true` while keeping omission/`null` semantics; smoke adds `listing-create-empty-filing-date`, `listing-create-empty-publication-date`, `listing-create-empty-grant-date`, `listing-update-empty-filing-date`, `listing-update-empty-publication-date`, `listing-update-empty-grant-date`, `admin-listing-create-empty-filing-date`, `admin-listing-create-empty-publication-date`, `admin-listing-create-empty-grant-date`, `admin-listing-update-empty-filing-date`, `admin-listing-update-empty-publication-date`, `admin-listing-update-empty-grant-date`, and search regressions `search-listings-empty-created-from`, `search-listings-empty-created-to`, `search-listings-empty-filing-date-from`, `search-listings-empty-filing-date-to`, `search-listings-empty-publication-date-from`, `search-listings-empty-publication-date-to`, `search-listings-empty-grant-date-from`, `search-listings-empty-grant-date-to`).
  72) alerts/maintenance empty-date-filter strictness hardening landed (done: `AlertsService.parseDateTime` and `PatentMaintenanceService.parseDate` now reject explicit empty-string query date filters in strict list-query paths while keeping omission/`null` semantics; smoke adds `admin-alert-list-empty-triggered-from`, `admin-alert-list-empty-triggered-to`, `admin-maintenance-schedules-list-empty-due-from`, and `admin-maintenance-schedules-list-empty-due-to` regressions).
  73) cases dueAt empty-string strictness hardening landed (done: `CasesService.parseDueAt` now rejects explicit empty-string `dueAt` in strict create/sla paths while keeping omission/`null` semantics for default scheduling behavior; smoke adds `admin-case-create-empty-due-at` and `admin-case-sla-empty-due-at` regressions).
  74) reports range start/end empty-string strictness hardening landed (done: `ReportsService.buildRange` now rejects explicit empty-string `start/end` with 400 while keeping omission/`null` semantics; smoke adds `admin-report-summary-empty-start`, `admin-report-summary-empty-end`, `admin-report-export-empty-start`, and `admin-report-export-empty-end` regressions).
  75) listing-featured/tech-manager featuredUntil empty-string strictness hardening landed (done: `ListingsService.updateFeatured` and `TechManagersService.update` now reject explicit empty-string `featuredUntil` with 400 while keeping `null` clear semantics; smoke adds `admin-listing-featured-set-city-empty-until` and `admin-tech-manager-update-empty-featured-until` regressions).
  76) maintenance-schedule dueDate/gracePeriodEnd empty-string strictness hardening landed (done: `PatentMaintenanceService` now rejects explicit empty-string `dueDate/gracePeriodEnd` in create/update paths with 400 while keeping omission/`null` semantics; smoke adds `admin-maintenance-schedule-create-empty-due-date`, `admin-maintenance-schedule-create-empty-grace-period-end`, `admin-maintenance-schedule-update-empty-due-date`, and `admin-maintenance-schedule-update-empty-grace-period-end` regressions).
  77) listings nullable enum empty-string strictness hardening landed (done: `ListingsService` now rejects explicit empty-string `licenseMode/pledgeStatus/existingLicenseStatus` with 400 while keeping `null` clear semantics; smoke adds user/admin create+update regressions: `listing-create-empty-license-mode`, `listing-create-empty-pledge-status`, `listing-create-empty-existing-license-status`, `listing-update-empty-license-mode`, `listing-update-empty-pledge-status`, `listing-update-empty-existing-license-status`, `admin-listing-create-empty-license-mode`, `admin-listing-create-empty-pledge-status`, `admin-listing-create-empty-existing-license-status`, `admin-listing-update-empty-license-mode`, `admin-listing-update-empty-pledge-status`, and `admin-listing-update-empty-existing-license-status`).
  78) demands/achievements/artworks nullable enum strictness hardening landed (done: `DemandsService/AchievementsService/ArtworksService` now reject explicit empty-string nullable enums with 400 while keeping `null` clear semantics, and user create/update paths now no longer silently swallow invalid optional enums; smoke adds admin/user create+update regressions for `budgetType/deliveryPeriod/maturity/calligraphyScript/paintingGenre` plus user update strict-invalid coverage for `artwork.category/priceType`).
  79) patents legalStatus empty-string strictness hardening landed (done: `PatentsService` now rejects explicit empty-string `legalStatus` with 400 in admin create/update while keeping omission/`null` clear semantics; smoke adds `admin-patent-create-empty-legal-status` and `admin-patent-update-empty-legal-status` regressions).
  80) listings patent legalStatus/sourcePrimary strictness hardening landed (done: `ListingsService` patent write paths now reject explicit invalid/empty `legalStatus/sourcePrimary` with 400 while keeping omission semantics and admin source fallback behavior; smoke adds user/admin create+update regressions `listing-create-invalid-legal-status`, `listing-create-empty-legal-status`, `listing-create-invalid-source-primary`, `listing-create-empty-source-primary`, `listing-update-invalid-legal-status`, `listing-update-empty-legal-status`, `listing-update-invalid-source-primary`, `listing-update-empty-source-primary`, `admin-listing-create-invalid-legal-status`, `admin-listing-create-empty-legal-status`, `admin-listing-create-invalid-source-primary`, `admin-listing-create-empty-source-primary`, `admin-listing-update-invalid-legal-status`, `admin-listing-update-empty-legal-status`, `admin-listing-update-invalid-source-primary`, and `admin-listing-update-empty-source-primary`).
  81) patents adminCreate-upsert legalStatus null-clear semantics hardening landed (done: `PatentsService.adminCreate` upsert update path now respects explicit `legalStatus: null` and actually clears persisted legal status instead of silently retaining previous value; smoke adds `admin-patent-upsert-clear-legal-status` regression with idempotent-id and missing-`legalStatus` assertions).
  82) patents adminCreate-upsert filingDate null-clear semantics hardening landed (done: `PatentsService.adminCreate` upsert update path now respects explicit `filingDate: null` and clears persisted filing date instead of retaining previous value; smoke adds `admin-patent-upsert-clear-filing-date` regression with idempotent-id and missing-`filingDate` assertions).
  83) listings patent date null-clear semantics hardening landed (done: `ListingsService.ensurePatent/updatePatentCore` now respects explicit `filingDate/publicationDate/grantDate: null` and clears persisted values instead of silently retaining previous dates; smoke adds user/admin regressions `listing-create-with-patent-for-null-filing-date-clear`, `listing-update-clear-null-filing-date`, `admin-listing-create-with-patent-for-null-filing-date-clear`, and `admin-listing-update-clear-null-filing-date` with admin patent read-back assertions).
  84) alerts and maintenance list query empty-id strictness hardening landed (done: `AlertsService` now rejects explicit empty `type/targetId`, and `PatentMaintenanceService` now rejects explicit empty `patentId/scheduleId/assignedCsUserId` on list queries with 400; smoke adds regressions `admin-alert-list-empty-type`, `admin-alert-list-empty-target-id`, `admin-maintenance-schedules-list-empty-patent-id`, `admin-maintenance-tasks-list-empty-schedule-id`, and `admin-maintenance-tasks-list-empty-assigned-cs-user-id`).
  85) audit-logs list query empty-id strictness hardening landed (done: `AuditLogsService` now rejects explicit empty `targetType/targetId/actorUserId/action` with 400 instead of silently ignoring; smoke adds regressions `admin-audit-logs-empty-target-type`, `admin-audit-logs-empty-target-id`, `admin-audit-logs-empty-actor-user-id`, and `admin-audit-logs-empty-action`).
  86) listings update regionCode null-clear and empty-string strictness hardening landed (done: `ListingsService` now treats explicit `regionCode: null` as clear and rejects explicit empty string with 400 for both user/admin update paths; smoke adds regressions `listing-update-clear-null-region-code`, `listing-update-empty-region-code`, `admin-listing-update-clear-null-region-code`, and `admin-listing-update-empty-region-code`).
  87) demands/achievements/artworks update regionCode null-clear and empty-string strictness hardening landed (done: `DemandsService` / `AchievementsService` / `ArtworksService` now treat explicit `regionCode: null` as clear and reject explicit empty string with 400 for both user/admin update paths; smoke adds regressions `demand-update-clear-null-region-code`, `demand-update-empty-region-code`, `achievement-update-clear-null-region-code`, `achievement-update-empty-region-code`, `artwork-update-clear-null-region-code`, `artwork-update-empty-region-code`, `admin-demand-update-clear-null-region-code`, `admin-demand-update-empty-region-code`, `admin-achievement-update-clear-null-region-code`, `admin-achievement-update-empty-region-code`, `admin-artwork-update-clear-null-region-code`, and `admin-artwork-update-empty-region-code`).
  88) me-addresses regionCode null-clear and empty-string strictness hardening landed (done: `AddressesService` now treats explicit `regionCode: null` as clear and rejects explicit empty string with 400 for both create/update semantics where field is explicitly provided; smoke adds regressions `me-address-create-empty-region-code`, `me-address-update-clear-null-region-code`, and `me-address-update-empty-region-code`).
  89) me profile patch regionCode null-clear and empty-string strictness hardening landed (done: `UsersService.updateUserProfile` now treats explicit `regionCode: null` as clear and rejects explicit empty string with 400 instead of storing `"null"`/`""`; smoke adds regressions `me-patch-region-code`, `me-patch-clear-null-region-code`, and `me-patch-empty-region-code`).
  90) admin region update null/empty strictness hardening landed (done: `RegionsService.updateRegion` now strictly rejects explicit null/empty `level` and null `name` with 400 instead of silently bypassing checks or writing invalid values; smoke adds regressions `admin-region-update-empty-level`, `admin-region-update-null-level`, and `admin-region-update-null-name`).
  91) regions/patent-map empty-filter strictness hardening landed (done: `RegionsService.listRegions` now rejects explicit empty `level` with 400 for both `/regions` and `/admin/regions`, and `PatentMapService.getSummary` now rejects explicit empty `parentCode` with 400 instead of silently dropping the filter; smoke adds regressions `regions-empty-level`, `admin-regions-empty-level`, and `patent-map-summary-empty-parent-code`).
  92) AI agent query `regionCode` strictness hardening landed (done: `AiService.createAgentQuery` now validates explicit `regionCode` as a strict 6-digit code and rejects empty/invalid values with 400 instead of silently ignoring; smoke adds regressions `ai-agent-query-empty-region-code` and `ai-agent-query-invalid-region-code`).
  93) search/recommendations empty `regionCode` strictness hardening landed (done: `ListingsService.listAdmin/searchPublic/getMyRecommendations` plus `DemandsService.search` / `AchievementsService.search` / `ArtworksService.search` now reject explicit empty `regionCode` with 400 instead of silently swallowing; smoke adds regressions `admin-listings-empty-region-code`, `search-listings-empty-region-code`, `search-demands-empty-region-code`, `search-achievements-empty-region-code`, `search-artworks-empty-region-code`, and `me-recommendations-listings-empty-region-code`).
  94) inventor/org/tech-manager query empty `regionCode` strictness hardening landed (done: `InventorsService.search`, `OrganizationsService.list`, and `TechManagersService.buildWhere` now reject explicit empty `regionCode` with 400 instead of silently ignoring; smoke adds regressions `search-inventors-empty-region-code`, `public-organizations-empty-region-code`, `search-tech-managers-empty-region-code`, and `admin-tech-managers-empty-region-code`).
  95) listing search empty seller/cluster filter strictness hardening landed (done: `ListingsService.searchPublic` now rejects explicit empty `sellerUserId` and `clusterId` with 400 instead of silently swallowing; smoke adds regressions `search-listings-empty-seller-user-id` and `search-listings-empty-cluster-id`).
  96) demand/achievement/artwork create empty `regionCode` strictness hardening landed (done: `DemandsService/AchievementsService/ArtworksService` user+admin create paths now use strict region parser and reject explicit empty `regionCode` with 400 instead of coercing to null; smoke adds regressions `admin-demand-create-empty-region-code`, `admin-achievement-create-empty-region-code`, `admin-artwork-create-empty-region-code`, `demand-create-empty-region-code`, `achievement-create-empty-region-code`, and `artwork-create-empty-region-code`).
  97) listing create empty `regionCode` strictness hardening landed (done: `ListingsService.createListing` and `ListingsService.adminCreate` now parse explicit `regionCode` via strict nullable parser and reject empty string with 400 instead of silently coercing to null; smoke adds regressions `listing-create-empty-region-code` and `admin-listing-create-empty-region-code`).
  98) listing create/update empty `clusterId` strictness hardening landed (done: `ListingsService.createListing/adminCreate/updateListing/adminUpdate` now parse explicit `clusterId` via strict nullable parser and reject empty string with 400 instead of silently coercing to null or writing empty string; smoke adds regressions `listing-create-empty-cluster-id`, `listing-update-empty-cluster-id`, `admin-listing-create-empty-cluster-id`, and `admin-listing-update-empty-cluster-id`).
  99) case create empty `orderId/assigneeId` strictness hardening landed (done: `CasesService.create` now uses has-own strict nullable parsing for explicit `orderId/assigneeId`, rejects empty string with 400 instead of silently coercing to null, while preserving omission/`null` semantics; smoke adds regressions `admin-case-create-empty-order-id` and `admin-case-create-empty-assignee-id`).
  100) maintenance task create empty `assignedCsUserId` strictness hardening landed (done: `PatentMaintenanceService.createTask` now uses has-own strict nullable parsing for explicit `assignedCsUserId`, rejects empty string with 400 instead of silently coercing to null, while preserving omission/`null` semantics; smoke adds regression `admin-maintenance-task-create-empty-assigned-cs-user-id`).
  101) maintenance task update empty `assignedCsUserId/evidenceFileId` strictness hardening landed (done: `PatentMaintenanceService.updateTask` now requires explicit clear-by-null and rejects empty string with 400 for `assignedCsUserId` and `evidenceFileId` instead of silently coercing to null; smoke adds regressions `admin-maintenance-task-update-empty-assigned-cs-user-id` and `admin-maintenance-task-update-empty-evidence-file-id`).
  102) order milestone empty `evidenceFileId` strictness hardening landed (done: `OrdersService.adminContractSigned` and `OrdersService.adminTransferCompleted` now use has-own strict nullable parsing for explicit `evidenceFileId`, rejecting empty string with 400 instead of silently ignoring; smoke adds regressions `admin-order-contract-signed-empty-evidence-file-id` and `admin-order-transfer-completed-empty-evidence-file-id`).
  103) content create/update empty `coverFileId` strictness hardening landed (done: `DemandsService/AchievementsService/ArtworksService` user+admin create/update paths now use strict nullable parser for explicit `coverFileId`, rejecting empty string with 400 instead of silently coercing to null; smoke adds regressions `admin-demand-create-empty-cover-file-id`, `admin-demand-update-empty-cover-file-id`, `demand-create-empty-cover-file-id`, `demand-update-empty-cover-file-id`, `admin-achievement-create-empty-cover-file-id`, `admin-achievement-update-empty-cover-file-id`, `achievement-create-empty-cover-file-id`, `achievement-update-empty-cover-file-id`, `admin-artwork-create-empty-cover-file-id`, `admin-artwork-update-empty-cover-file-id`, `artwork-create-empty-cover-file-id`, and `artwork-update-empty-cover-file-id`).
  104) comment create empty `parentCommentId` strictness hardening landed (done: `CommentsService.createComment` now uses has-own strict nullable parsing for explicit `parentCommentId`, rejects empty string with 400 instead of silently coercing to root comment, while preserving omission/`null` root semantics; smoke adds regression `listing-comment-create-empty-parent-comment-id`).
  105) admin listing update empty `sellerUserId` strictness hardening landed (done: `ListingsService.adminUpdate` now uses has-own strict non-empty parsing for explicit `sellerUserId`, rejects empty string with 400 instead of silently retaining original seller; smoke adds regression `admin-listing-update-empty-seller-user-id`).
  106) admin content owner user-id empty-string strictness hardening landed (done: `DemandsService/AchievementsService/ArtworksService` now apply strict alias-based owner parsing in admin create/update paths (`publisherUserId|ownerId` / `sellerUserId|publisherUserId|ownerId`), rejecting explicit empty strings with 400 instead of silently falling back; smoke adds regressions `admin-demand-create-empty-publisher-user-id`, `admin-demand-update-empty-publisher-user-id`, `admin-achievement-create-empty-publisher-user-id`, `admin-achievement-update-empty-publisher-user-id`, `admin-artwork-create-empty-seller-user-id`, and `admin-artwork-update-empty-seller-user-id`).
  107) admin comments list empty `contentId` strictness hardening landed (done: `CommentsService.adminList` now rejects explicit empty `contentId` with 400 instead of silently dropping filter; smoke adds regression `admin-comments-empty-content-id`).
  108) admin listing create empty `sellerUserId` strictness hardening landed (done: `ListingsService.adminCreate` now uses has-own strict non-empty parsing for explicit `sellerUserId`, rejects empty string with 400 instead of silently falling back to operator user; smoke adds regression `admin-listing-create-empty-seller-user-id`).
  109) me verification empty id/code field strictness hardening landed (done: `UsersService.submitMyVerification` now applies has-own strict parsing for explicit `regionCode/logoFileId/unifiedSocialCreditCode`, rejecting empty string with 400 instead of silently coercing to null; smoke adds regressions `me-verification-empty-region-code`, `me-verification-empty-logo-file-id`, and `me-verification-empty-unified-social-credit-code`).
  110) me verification empty `evidenceFileIds` item strictness hardening landed (done: `UsersService.submitMyVerification` now validates explicit `evidenceFileIds` as a strict non-empty string array and rejects empty items with 400 instead of silently accepting `[""]`; smoke adds regression `me-verification-empty-evidence-file-id`).
  111) admin order invoice upsert empty `invoiceNo` strictness hardening landed (done: `OrdersService.adminUpsertOrderInvoice` now uses has-own strict nullable parsing for explicit `invoiceNo`, rejects empty string with 400 instead of silently falling back to existing/generated invoice number; smoke adds regression `admin-order-upsert-invoice-empty-invoice-no`).
  112) admin announcement create/update empty `issueNo` strictness hardening landed (done: `AnnouncementsService.adminCreate/adminUpdate` now uses has-own strict nullable parsing for explicit `issueNo`, rejects empty string with 400 instead of silently coercing to null; smoke adds regressions `admin-announcement-create-empty-issue-no` and `admin-announcement-update-empty-issue-no`).
  113) admin patent create/update empty `applicationNoDisplay` strictness hardening landed (done: `PatentsService.adminCreate/adminUpdate` now use has-own strict nullable parsing for explicit `applicationNoDisplay`, reject empty string with 400 instead of silently falling back/clearing, while preserving omission fallback and explicit `null` clear semantics; smoke adds regressions `admin-patent-create-empty-application-no-display` and `admin-patent-update-empty-application-no-display`).
  114) admin manual payment empty `tradeNo` strictness hardening landed (done: `OrdersService.adminManualConfirmPayment` now uses has-own strict nullable parsing for explicit `tradeNo`, rejects empty string with 400 instead of silently falling back to existing/generated trade number, while preserving omission fallback semantics; smoke adds regressions `admin-order-manual-payment-deposit-empty-trade-no` and `admin-order-manual-payment-final-empty-trade-no`).
  115) admin case create empty `title/requesterName` strictness hardening landed (done: `CasesService.create` now uses has-own strict nullable parsing for explicit `title` and `requesterName`, rejects empty string with 400 instead of silently falling back to defaults, while preserving omission and explicit `null` default semantics; smoke adds regressions `admin-case-create-empty-title` and `admin-case-create-empty-requester-name`).
  116) listings create/update empty `negotiableNote` strictness hardening landed (done: `ListingsService` user+admin create/update paths now use has-own strict nullable parsing for explicit `negotiableNote`, rejecting empty string with 400 instead of silently coercing to null, while preserving omission and explicit `null` clear semantics; smoke adds regressions `listing-create-empty-negotiable-note`, `listing-update-empty-negotiable-note`, `admin-listing-create-empty-negotiable-note`, and `admin-listing-update-empty-negotiable-note`).
  117) listings create/update empty `encumbranceNote` strictness hardening landed (done: `ListingsService` user+admin create/update paths now use has-own strict nullable parsing for explicit `encumbranceNote`, rejecting empty string with 400 instead of silently coercing to null, while preserving omission and explicit `null` clear semantics; smoke adds regressions `listing-create-empty-encumbrance-note`, `listing-update-empty-encumbrance-note`, `admin-listing-create-empty-encumbrance-note`, and `admin-listing-update-empty-encumbrance-note`).
  118) listings create/update empty `title` strictness hardening landed (done: `ListingsService` user+admin create/update paths now use has-own strict nullable parsing for explicit `title`, rejecting empty string with 400 instead of silently falling back to patent/default title on create or preserving previous title on update, while preserving omission and explicit `null` fallback semantics; smoke adds regressions `listing-create-empty-title`, `listing-update-empty-title`, `admin-listing-create-empty-title`, and `admin-listing-update-empty-title`).
  119) listings create/update empty `summary` strictness hardening landed (done: `ListingsService` user+admin create/update paths now use has-own strict nullable parsing for explicit `summary`, rejecting empty string with 400 instead of silently coercing to null on create or preserving previous summary on update, while preserving omission and explicit `null` existing fallback semantics; smoke adds regressions `listing-create-empty-summary`, `listing-update-empty-summary`, `admin-listing-create-empty-summary`, and `admin-listing-update-empty-summary`).
  120) demands create/update empty `contactName` strictness hardening landed (done: `DemandsService` user+admin create/update paths now use strict nullable parsing for explicit `contactName`, rejecting empty string with 400 instead of silently coercing to null, while preserving omission and explicit `null` semantics; smoke adds regressions `demand-create-empty-contact-name`, `demand-update-empty-contact-name`, `admin-demand-create-empty-contact-name`, and `admin-demand-update-empty-contact-name`).
  121) demands create/update empty `contactTitle` strictness hardening landed (done: `DemandsService` user+admin create/update paths now use strict nullable parsing for explicit `contactTitle`, rejecting empty string with 400 instead of silently coercing to null, while preserving omission and explicit `null` semantics; smoke adds regressions `demand-create-empty-contact-title`, `demand-update-empty-contact-title`, `admin-demand-create-empty-contact-title`, and `admin-demand-update-empty-contact-title`).
  122) demands create/update empty `contactPhoneMasked` strictness hardening landed (done: `DemandsService` user+admin create/update paths now use strict nullable parsing for explicit `contactPhoneMasked`, rejecting empty string with 400 instead of silently coercing to null, while preserving omission and explicit `null` semantics; smoke adds regressions `demand-create-empty-contact-phone-masked`, `demand-update-empty-contact-phone-masked`, `admin-demand-create-empty-contact-phone-masked`, and `admin-demand-update-empty-contact-phone-masked`).
  123) demands create/update empty `summary` strictness hardening landed (done: `DemandsService` user+admin create/update paths now use strict nullable parsing for explicit `summary`, rejecting empty string with 400 instead of silently persisting empty text, while preserving omission and explicit `null` semantics; smoke adds regressions `demand-create-empty-summary`, `demand-update-empty-summary`, `admin-demand-create-empty-summary`, and `admin-demand-update-empty-summary`).
  124) demands create/update empty `description` strictness hardening landed (done: `DemandsService` user+admin create/update paths now use strict nullable parsing for explicit `description`, rejecting empty string with 400 instead of silently persisting empty text, while preserving omission and explicit `null` semantics; smoke adds regressions `demand-create-empty-description`, `demand-update-empty-description`, `admin-demand-create-empty-description`, and `admin-demand-update-empty-description`).
  125) achievements create/update empty `summary` strictness hardening landed (done: `AchievementsService` user+admin create/update paths now use strict nullable parsing for explicit `summary`, rejecting empty string with 400 instead of silently persisting empty text, while preserving omission and explicit `null` semantics; smoke adds regressions `achievement-create-empty-summary`, `achievement-update-empty-summary`, `admin-achievement-create-empty-summary`, and `admin-achievement-update-empty-summary`).
  126) achievements create/update empty `description` strictness hardening landed (done: `AchievementsService` user+admin create/update paths now use strict nullable parsing for explicit `description`, rejecting empty string with 400 instead of silently persisting empty text, while preserving omission and explicit `null` semantics; smoke adds regressions `achievement-create-empty-description`, `achievement-update-empty-description`, `admin-achievement-create-empty-description`, and `admin-achievement-update-empty-description`).
  127) artworks create/update empty `description` strictness hardening landed (done: `ArtworksService` user+admin create/update paths now use strict nullable parsing for explicit `description`, rejecting empty string with 400 instead of silently persisting empty text, while preserving omission and explicit `null` semantics; smoke adds regressions `artwork-create-empty-description`, `artwork-update-empty-description`, `admin-artwork-create-empty-description`, and `admin-artwork-update-empty-description`).
  128) artworks create/update empty `certificateNo` strictness hardening landed (done: `ArtworksService` user+admin create/update paths now use strict nullable parsing for explicit `certificateNo`, rejecting empty string with 400 instead of silently persisting empty text, while preserving omission and explicit `null` semantics; smoke adds regressions `artwork-create-empty-certificate-no`, `artwork-update-empty-certificate-no`, `admin-artwork-create-empty-certificate-no`, and `admin-artwork-update-empty-certificate-no`).
  129) artworks create/update empty `material` strictness hardening landed (done: `ArtworksService` user+admin create/update paths now use strict nullable parsing for explicit `material`, rejecting empty string with 400 instead of silently persisting empty text, while preserving omission and explicit `null` semantics; smoke adds regressions `artwork-create-empty-material`, `artwork-update-empty-material`, `admin-artwork-create-empty-material`, and `admin-artwork-update-empty-material`).
  130) artworks create/update empty `size` strictness hardening landed (done: `ArtworksService` user+admin create/update paths now use strict nullable parsing for explicit `size`, rejecting empty string with 400 instead of silently persisting empty text, while preserving omission and explicit `null` semantics; smoke adds regressions `artwork-create-empty-size`, `artwork-update-empty-size`, `admin-artwork-create-empty-size`, and `admin-artwork-update-empty-size`).
  131) artworks empty `title` strictness hardening landed (done: `ArtworksService` user/admin update paths now use explicit strict parsing for `title`, rejecting empty string with 400 instead of silently persisting empty text; create paths are also regression-guarded via `artwork-create-empty-title` and `admin-artwork-create-empty-title`, with update regressions `artwork-update-empty-title` and `admin-artwork-update-empty-title`).
  132) artworks empty `creatorName` strictness regression coverage landed (done: `ArtworksService` user/admin update paths already enforce strict non-empty parsing for explicit `creatorName`; smoke now adds regressions `artwork-create-empty-creator-name`, `artwork-update-empty-creator-name`, `admin-artwork-create-empty-creator-name`, and `admin-artwork-update-empty-creator-name` to lock create/update behavior).
  133) artworks patch omission-preserve regression hardening landed (done: `ArtworksService` user/admin update paths now update `creationDate/creationYear/priceAmountFen` only when explicitly present, preventing accidental null-clears on omitted fields; smoke adds preservation assertions on `artwork-update-clear-null-region-code` and `admin-artwork-update-clear-null-region-code`).
  134) artworks update explicit-null strictness hardening landed (done: `ArtworksService` now rejects explicit `null` for non-null fields `title` and `creatorName` in user/admin update paths with 400 instead of silently ignoring; smoke adds regressions `artwork-update-null-title`, `artwork-update-null-creator-name`, `admin-artwork-update-null-title`, and `admin-artwork-update-null-creator-name`).
  135) achievements update `title` strictness hardening landed (done: `AchievementsService` user/admin update paths now use explicit strict parsing for `title`, rejecting both empty string and explicit `null` with 400 instead of silently accepting/ignoring; smoke adds regressions `achievement-update-empty-title`, `achievement-update-null-title`, `admin-achievement-update-empty-title`, and `admin-achievement-update-null-title`).
  136) demands update `title` strictness hardening landed (done: `DemandsService` user/admin update paths now use explicit strict parsing for `title`, rejecting both empty string and explicit `null` with 400 instead of silently accepting/ignoring; smoke adds regressions `demand-update-empty-title`, `demand-update-null-title`, `admin-demand-update-empty-title`, and `admin-demand-update-null-title`).
  137) demand/achievement create empty `title` regression coverage landed (done: smoke now explicitly asserts `demand-create-empty-title`, `admin-demand-create-empty-title`, `achievement-create-empty-title`, and `admin-achievement-create-empty-title` all return 400, locking required-title behavior on both user/admin create paths).
  138) announcement/patent title strictness regression coverage landed (done: smoke now explicitly asserts `admin-announcement-create-empty-title`, `admin-patent-create-empty-title`, `admin-patent-update-empty-title`, and `admin-patent-update-null-title` all return 400, locking required-title behavior on admin create/update paths).
  139) patent upsert `abstract` omission-preserve regression hardening landed (done: `PatentsService.adminCreate` now preserves existing `abstract` on upsert update when `abstract` is omitted, while still supporting explicit `null` clear semantics; smoke adds assertions `admin-patent-upsert-abstract-seed`, `admin-patent-upsert-abstract-preserve-on-omit`, and `admin-patent-upsert-abstract-clear-null`).
  140) patent upsert `applicationNoDisplay` omission-preserve regression hardening landed (done: `PatentsService.adminCreate` now preserves existing `applicationNoDisplay` on upsert update when the field is omitted, while supporting explicit `null` clear semantics; smoke adds assertions `admin-patent-upsert-application-no-display-seed`, `admin-patent-upsert-application-no-display-preserve-on-omit`, and `admin-patent-upsert-application-no-display-clear-null`).
  141) patent upsert source metadata omission-preserve regression hardening landed (done: `PatentsService.adminCreate` now preserves existing `sourcePrimary/sourceUpdatedAt` on upsert update when these fields are omitted, preventing implicit metadata overwrite; smoke adds assertions `admin-patent-upsert-source-meta-seed` and `admin-patent-upsert-source-meta-preserve-on-omit`).
  142) listing validation no-side-effect hardening landed (done: `ListingsService` user/admin `create/update` paths now perform local field validation before `ensurePatent`, preventing invalid requests from preemptively creating/updating patent records; smoke adds regressions `listing-create-empty-title-with-patent-no-side-effect` and `admin-listing-update-empty-title-with-patent-no-side-effect` plus post-failure patent-lookup absence assertions).
  143) case evidence invalid file-id strictness hardening landed (done: `CasesService.addEvidence` now validates that explicit `fileId` exists and returns 400 on invalid IDs instead of leaking DB-level failures; smoke adds regression `admin-case-evidence-invalid-file-id`).
  144) case note empty-body strictness hardening landed (done: `CasesService.addNote` now enforces required non-empty `note` and returns 400 instead of silently no-op returning case detail; smoke updates `admin-case-note-empty` to assert 400, aligning with OpenAPI required-body contract).
  145) case UUID format strictness hardening landed (done: `CasesService` now applies strict UUID validation for `caseId/orderId/assigneeId/fileId` before Prisma lookups, preventing DB-level invalid-UUID failures and normalizing to 400; smoke adds regressions `admin-case-detail-invalid-id`, `admin-case-create-invalid-order-id-format`, `admin-case-create-invalid-assignee-id-format`, `admin-case-assign-invalid-assignee-format`, and `admin-case-evidence-invalid-file-id-format`).
  146) admin invoice upsert file-id format strictness hardening landed (done: `OrdersService.adminUpsertOrderInvoice` now strictly validates `invoiceFileId` as UUID and returns 400 on invalid format instead of leaking Prisma invalid-UUID errors; smoke adds regression `admin-order-upsert-invoice-invalid-file-id-format`).
  147) admin invoice endpoints order-id format strictness hardening landed (done: `OrdersService.adminIssueInvoice/adminUpsertOrderInvoice/adminDeleteOrderInvoice` now strictly validate `orderId` as UUID and return 400 on invalid path format; smoke adds regressions `admin-order-issue-invoice-invalid-order-id-format`, `admin-order-upsert-invoice-invalid-order-id-format`, and `admin-order-delete-invoice-invalid-order-id-format`).
  148) user invoice endpoints order-id format strictness hardening landed (done: `OrdersService.getOrderInvoice/requestInvoice` now strictly validate `orderId` as UUID and return 400 on invalid path format, preventing Prisma invalid-UUID failures; smoke adds regressions `order-invoice-get-invalid-order-id-format` and `order-invoice-request-invalid-order-id-format`).
  149) order detail/payment-intent order-id format strictness hardening landed (done: `OrdersService.getOrderDetail/createPaymentIntent` now strictly validate `orderId` as UUID and return 400 on invalid path format, preventing Prisma invalid-UUID failures; smoke adds regressions `order-detail-invalid-order-id-format` and `order-payment-intent-invalid-order-id-format`).
  150) order case/refund endpoints order-id format strictness hardening landed (done: `OrdersService.getOrderWithListing/getCaseWithMilestones/listRefundRequests/createRefundRequest` now strictly validate `orderId` as UUID and return 400 on invalid path format, preventing Prisma invalid-UUID failures; smoke adds regressions `order-case-invalid-order-id-format`, `order-refund-requests-list-invalid-order-id-format`, and `order-refund-request-invalid-order-id-format`).
  151) admin order core endpoints order-id format strictness hardening landed (done: `OrdersService.getAdminOrderDetail/adminManualConfirmPayment/adminContractSigned/adminTransferCompleted/getSettlement/adminManualPayout` now strictly validate `orderId` as UUID and return 400 on invalid path format, preventing Prisma invalid-UUID failures; smoke adds regressions `admin-order-detail-invalid-order-id-format`, `admin-order-manual-payment-invalid-order-id-format`, `admin-order-contract-signed-invalid-order-id-format`, `admin-order-transfer-completed-invalid-order-id-format`, `admin-order-settlement-invalid-order-id-format`, and `admin-order-manual-payout-invalid-order-id-format`).
  152) admin refund endpoints refund-request-id format strictness hardening landed (done: `OrdersService.adminApproveRefundRequest/adminRejectRefundRequest/adminCompleteRefundRequest` now strictly validate `refundRequestId` as UUID and return 400 on invalid path format, preventing Prisma invalid-UUID failures; smoke adds regressions `admin-refund-approve-invalid-refund-request-id-format`, `admin-refund-reject-invalid-refund-request-id-format`, and `admin-refund-complete-invalid-refund-request-id-format`).
  153) order listing-id and payout evidence file-id format strictness hardening landed (done: `OrdersService.createOrder` now strictly validates `listingId` as UUID, and `OrdersService.adminManualPayout` now strictly validates `payoutEvidenceFileId` as UUID before file lookup, both returning 400 on invalid format to prevent Prisma invalid-UUID failures; smoke adds regressions `order-create-invalid-listing-id-format` and `admin-order-manual-payout-invalid-payout-evidence-file-id-format`).
  154) file route file-id format strictness hardening landed (done: `FilesService.getFileById` now strictly validates `fileId` as UUID and returns 400 on invalid route format before Prisma lookup, preventing invalid-UUID failures on file routes; smoke adds regressions `file-download-invalid-file-id-format`, `file-preview-invalid-file-id-format`, and `file-temporary-access-invalid-file-id-format`).
  155) contract upload id-format strictness hardening landed (done: `ContractsService.parseOrderId` now strictly validates normalized `contractId` as UUID and `ContractsService.upload` now strictly validates `contractFileId` as UUID before lookup, both returning 400 on invalid format to prevent Prisma invalid-UUID failures; smoke adds regressions `contract-upload-invalid-contract-id-format` and `contract-upload-invalid-prefixed-contract-id-format`).
  156) conversation route id-format strictness hardening landed (done: `ConversationsService` now strictly validates UUID formats for listing/demand/achievement/artwork/tech-manager ids in conversation upsert paths and `conversationId` in list/send/read message paths, returning 400 on invalid format before Prisma lookups; smoke adds regressions `listing-conversation-upsert-invalid-listing-id-format`, `demand-conversation-upsert-invalid-demand-id-format`, `achievement-conversation-upsert-invalid-achievement-id-format`, `artwork-conversation-upsert-invalid-artwork-id-format`, `tech-manager-conversation-upsert-invalid-tech-manager-id-format`, `conversation-messages-list-invalid-conversation-id-format`, `conversation-message-send-invalid-conversation-id-format`, and `conversation-read-invalid-conversation-id-format`).
  157) comment route id-format strictness hardening landed (done: `CommentsService` now strictly validates UUID formats for `contentId` in public/admin list and create routes, `commentId` in user/admin update-delete routes, and `parentCommentId` in create body, returning 400 on invalid format before Prisma lookups; smoke adds regressions `admin-comments-invalid-content-id-format`, `public-listing-comments-invalid-listing-id-format`, `public-demand-comments-invalid-demand-id-format`, `public-achievement-comments-invalid-achievement-id-format`, `public-artwork-comments-invalid-artwork-id-format`, `listing-comment-create-invalid-listing-id-format`, `demand-comment-create-invalid-demand-id-format`, `achievement-comment-create-invalid-achievement-id-format`, `artwork-comment-create-invalid-artwork-id-format`, `listing-comment-create-invalid-parent-comment-id-format`, `listing-comment-update-invalid-comment-id-format`, `listing-comment-delete-invalid-comment-id-format`, and `admin-comment-update-invalid-comment-id-format`).
  158) alert and notification route id-format strictness hardening landed (done: `NotificationsService.getById` and `AlertsService.acknowledge/list` now strictly validate UUID formats for `notificationId`, `alertId`, and `targetId` filter, returning 400 on invalid format before Prisma lookups; smoke adds regressions `notifications-detail-invalid-notification-id-format`, `admin-alert-list-invalid-target-id-format`, and `admin-alert-ack-invalid-alert-id-format`).
  159) favorite route id-format strictness hardening landed (done: `FavoritesService` now strictly validates UUID formats for `listingId/demandId/achievementId/artworkId` in favorite add/remove routes, returning 400 on invalid format before Prisma lookups and count updates; smoke adds regressions `listing-favorite-post-invalid-listing-id-format`, `listing-favorite-delete-invalid-listing-id-format`, `demand-favorite-post-invalid-demand-id-format`, `demand-favorite-delete-invalid-demand-id-format`, `achievement-favorite-post-invalid-achievement-id-format`, `achievement-favorite-delete-invalid-achievement-id-format`, `artwork-favorite-post-invalid-artwork-id-format`, and `artwork-favorite-delete-invalid-artwork-id-format`).
  160) address route id-format strictness hardening landed (done: `AddressesService.update/remove` now strictly validate UUID format for `addressId`, returning 400 on invalid route format before Prisma lookups/deletes; smoke adds regressions `me-address-update-invalid-address-id-format` and `me-address-delete-invalid-address-id-format`).
  161) organization and tech-manager route id-format strictness hardening landed (done: `OrganizationsService.getById` and `TechManagersService.getPublic/updateAdmin` now strictly validate UUID formats for `orgUserId` and `techManagerId`, returning 400 on invalid route format before Prisma lookups/upsert; smoke adds regressions `public-organization-detail-invalid-org-user-id-format`, `public-tech-manager-detail-invalid-tech-manager-id-format`, and `admin-tech-manager-update-invalid-tech-manager-id-format`).
  162) announcement route id-format strictness hardening landed (done: `AnnouncementsService` now strictly validates UUID format for `announcementId` in public detail and admin update/publish/off-shelf/delete routes, returning 400 on invalid route format before Prisma lookups/updates/deletes; smoke adds regressions `public-announcement-detail-invalid-announcement-id-format`, `admin-announcement-update-invalid-announcement-id-format`, `admin-announcement-delete-invalid-announcement-id-format`, `admin-announcement-publish-invalid-announcement-id-format`, and `admin-announcement-off-shelf-invalid-announcement-id-format`).
  163) AI parse-result route id-format strictness hardening landed (done: `AiService.createFeedback/adminGetParseResult/adminUpdateParseResult` now strictly validate UUID format for `parseResultId`, returning 400 on invalid route format before Prisma lookups/updates; smoke adds regressions `admin-ai-parse-result-get-invalid-parse-result-id-format`, `admin-ai-parse-result-update-invalid-parse-result-id-format`, and `ai-parse-feedback-invalid-parse-result-id-format` with 400/404 compatibility for optional AI-module-not-ready branches).
  164) RBAC route id-format strictness hardening landed (done: `RbacService.updateRole/deleteRole/updateUserRoles` now validate route IDs strictly before lookups/writes, with role routes accepting UUID or known system role IDs and user route requiring UUID; smoke adds regressions `admin-rbac-role-update-invalid-role-id-format`, `admin-rbac-role-delete-invalid-role-id-format`, and `admin-rbac-user-update-invalid-user-id-format`, while preserving `admin-rbac-role-delete-system-forbidden` 403 behavior for `role-admin`).
  165) patent-maintenance route id-format strictness hardening landed (done: `PatentMaintenanceService.getSchedule/updateSchedule/updateTask` now strictly validate UUID format for `scheduleId` and `taskId`, returning 400 on invalid route format before Prisma lookups/updates; smoke adds regressions `admin-maintenance-schedule-detail-invalid-schedule-id-format`, `admin-maintenance-schedule-update-invalid-schedule-id-format`, and `admin-maintenance-task-update-invalid-task-id-format`).
  166) admin user-verification route id-format strictness hardening landed (done: `AdminUserVerificationsController` now strictly validates UUID format for `verificationId` in approve/reject/materials/audit-logs routes, returning 400 on invalid route format before service/audit lookups; smoke adds regressions `admin-user-verification-approve-invalid-verification-id-format`, `admin-user-verification-reject-invalid-verification-id-format`, `admin-user-verification-materials-invalid-verification-id-format`, and `admin-user-verification-audit-logs-invalid-verification-id-format`).
  167) patent route id-format strictness hardening landed (done: `PatentsService.adminGetById/adminUpdate/getPatentById` now strictly validate UUID format for `patentId`, returning 400 on invalid route format before Prisma lookups; smoke adds regressions `admin-patent-get-by-id-invalid-patent-id-format`, `admin-patent-update-invalid-patent-id-format`, and `patent-get-by-id-invalid-patent-id-format`).
  168) achievement route id-format strictness hardening landed (done: `AchievementsController` now centrally validates UUID format for all `achievementId` path routes across user/public/admin APIs before delegating to services/content-audit; smoke adds regressions `public-achievement-detail-invalid-achievement-id-format`, `achievement-get-mine-invalid-achievement-id-format`, `achievement-update-invalid-achievement-id-format`, `achievement-submit-invalid-achievement-id-format`, `achievement-off-shelf-invalid-achievement-id-format`, `admin-achievement-get-by-id-invalid-achievement-id-format`, `admin-achievement-materials-invalid-achievement-id-format`, `admin-achievement-audit-logs-invalid-achievement-id-format`, `admin-achievement-update-invalid-achievement-id-format`, `admin-achievement-off-shelf-invalid-achievement-id-format`, `admin-achievement-publish-invalid-achievement-id-format`, `admin-achievement-approve-invalid-achievement-id-format`, and `admin-achievement-reject-invalid-achievement-id-format`).
  169) artwork route id-format strictness hardening landed (done: `ArtworksController` now centrally validates UUID format for all `artworkId` path routes across user/public/admin APIs before delegating to services/content-audit; smoke adds regressions `public-artwork-detail-invalid-artwork-id-format`, `artwork-get-mine-invalid-artwork-id-format`, `artwork-get-by-id-invalid-artwork-id-format`, `artwork-update-invalid-artwork-id-format`, `artwork-submit-invalid-artwork-id-format`, `artwork-off-shelf-invalid-artwork-id-format`, `admin-artwork-get-by-id-invalid-artwork-id-format`, `admin-artwork-materials-invalid-artwork-id-format`, `admin-artwork-audit-logs-invalid-artwork-id-format`, `admin-artwork-update-invalid-artwork-id-format`, `admin-artwork-off-shelf-invalid-artwork-id-format`, `admin-artwork-publish-invalid-artwork-id-format`, `admin-artwork-approve-invalid-artwork-id-format`, and `admin-artwork-reject-invalid-artwork-id-format`).
  170) demand route id-format strictness hardening landed (done: `DemandsController` now centrally validates UUID format for all `demandId` path routes across user/public/admin APIs before delegating to services/content-audit; smoke adds regressions `public-demand-detail-invalid-demand-id-format`, `demand-get-mine-invalid-demand-id-format`, `demand-get-by-id-invalid-demand-id-format`, `demand-update-invalid-demand-id-format`, `demand-submit-invalid-demand-id-format`, `demand-off-shelf-invalid-demand-id-format`, `admin-demand-get-by-id-invalid-demand-id-format`, `admin-demand-materials-invalid-demand-id-format`, `admin-demand-audit-logs-invalid-demand-id-format`, `admin-demand-update-invalid-demand-id-format`, `admin-demand-off-shelf-invalid-demand-id-format`, `admin-demand-publish-invalid-demand-id-format`, `admin-demand-approve-invalid-demand-id-format`, and `admin-demand-reject-invalid-demand-id-format`).
  171) listing route id-format strictness hardening landed (done: `ListingsController` now centrally validates UUID format for all `listingId` path routes across user/public/admin APIs before delegating to service/content-audit; smoke adds regressions `public-listing-detail-invalid-listing-id-format`, `listing-get-mine-invalid-listing-id-format`, `listing-get-by-id-invalid-listing-id-format`, `listing-update-invalid-listing-id-format`, `listing-submit-invalid-listing-id-format`, `listing-off-shelf-invalid-listing-id-format`, `listing-consult-post-invalid-listing-id-format`, `admin-listing-get-by-id-invalid-listing-id-format`, `admin-listing-materials-invalid-listing-id-format`, `admin-listing-audit-logs-invalid-listing-id-format`, `admin-listing-update-invalid-listing-id-format`, `admin-listing-off-shelf-invalid-listing-id-format`, `admin-listing-publish-invalid-listing-id-format`, `admin-listing-approve-invalid-listing-id-format`, `admin-listing-reject-invalid-listing-id-format`, and `admin-listing-featured-set-invalid-listing-id-format`).
  172) case route id-format strictness hardening landed (done: `CasesController` now centrally validates UUID format for all `caseId` path routes before delegating to service methods; smoke adds regressions `admin-case-assign-invalid-case-id-format`, `admin-case-status-invalid-case-id-format`, `admin-case-note-invalid-case-id-format`, `admin-case-evidence-invalid-case-id-format`, and `admin-case-sla-invalid-case-id-format`, complementing existing `admin-case-detail-invalid-id` coverage).
  173) contract upload `contractFileId` format regression coverage landed (done: smoke adds `contract-upload-invalid-contract-file-id-format`, asserting `POST /contracts/:id/upload` returns 400 when body `contractFileId` is not UUID, complementing existing contract route-id format guards).
  174) contract upload empty `contractFileId` regression coverage landed (done: smoke adds `contract-upload-empty-contract-file-id`, asserting `POST /contracts/:id/upload` returns 400 when body `contractFileId` is explicit empty string, locking required-field behavior).
  175) contract upload missing `contractFileId` regression coverage landed (done: smoke adds `contract-upload-missing-contract-file-id`, asserting `POST /contracts/:id/upload` returns 400 when body omits `contractFileId`, locking required-field behavior for missing-key input).
  176) contract upload missing-file resource regression coverage landed (done: smoke adds `contract-upload-contract-file-id-not-found`, asserting `POST /contracts/:id/upload` returns 400 when `contractFileId` is a valid UUID but file record does not exist, locking not-found validation behavior).
  177) contract upload missing-contract-id regression coverage landed (done: smoke adds `contract-upload-missing-contract-id`, asserting authenticated `POST /contracts/:id/upload` returns 404 when `contractId` is a valid UUID but order does not exist, locking not-found behavior on path resource lookup).
  178) contract upload non-PDF file regression coverage landed (done: smoke adds `contract-upload-non-pdf-contract-file`, asserting `POST /contracts/:id/upload` returns 400 when `contractFileId` points to non-PDF file, locking MIME-type validation behavior).
  179) contract upload seller-boundary regression coverage landed (done: smoke adds `contract-upload-forbidden-not-seller`, asserting authenticated non-seller `POST /contracts/:id/upload` returns 403, locking seller-only authorization behavior).
  180) API smoke large-list stability hardening landed (done: `Normalize-ResultBody` truncation ceiling raised from 64 KB to 512 KB, preventing false `Result body is not valid JSON` on high-volume list payloads while preserving response capture safeguards).
  181) contract upload happy-path regression coverage landed (done: smoke adds `file-upload-contract-pdf` + `contract-upload` success path, asserting `POST /contracts/:id/upload` returns 200/201 with `status=WAIT_CONFIRM`, correct `orderId`, and non-empty `fileUrl`).
  182) contract wait-confirm list read-back coverage landed (done: smoke adds `contract-list-wait-confirm` + `contract-list-wait-confirm-status`, asserting uploaded order appears in `GET /contracts?status=WAIT_CONFIRM` with expected status, locking post-upload list filter semantics).
  183) contract list auth/page strictness coverage landed (done: smoke adds `contract-list-unauthorized` and `contract-list-invalid-page-zero`, asserting unauthenticated `GET /contracts` returns 401 and `page=0` strict validation returns 400).
  184) contract list `pageSize=0` strictness coverage landed (done: smoke adds `contract-list-invalid-page-size-zero`, asserting `GET /contracts?pageSize=0` returns 400 to lock positive-integer pagination semantics).
  185) contract prefixed upload-id and pageSize-format coverage landed (done: smoke adds `contract-upload-prefixed-contract-id` to verify `contract-<uuid>` path success parity, and `contract-list-invalid-page-size` to lock non-numeric `pageSize` strict 400 behavior).
  186) contract list `pageSize` cap and decimal strictness coverage landed (done: smoke adds `contract-list-invalid-page-size-decimal` (400) and `contract-list-page-size-capped` + `contract-list-page-size-capped-to-50`, locking integer-only pagination and max-50 cap semantics).
  187) contract list status case-normalization coverage landed (done: smoke adds `contract-list-wait-confirm-lowercase` + `contract-list-wait-confirm-lowercase-status`, asserting lowercase `status=wait_confirm` is normalized and still returns expected `WAIT_CONFIRM` records).
  188) contract list page negative/decimal strictness coverage landed (done: smoke adds `contract-list-invalid-page-negative` and `contract-list-invalid-page-decimal`, asserting `GET /contracts` rejects non-positive and non-integer `page` values with 400).
  189) contract list empty-status and negative-pageSize strictness coverage landed (done: smoke adds `contract-list-empty-status` and `contract-list-invalid-page-size-negative`, asserting `GET /contracts` returns 400 for empty `status` and negative `pageSize` to lock enum/positive-integer validation semantics).
  190) contract list empty-page strictness coverage landed (done: smoke adds `contract-list-empty-page`, asserting `GET /contracts?page=` returns 400 to lock page positive-integer validation semantics for empty query input).
  191) contract list near-cap pageSize normalization coverage landed (done: smoke adds `contract-list-page-size-slight-over-cap`, asserting `GET /contracts?pageSize=51` returns 200 and normalizes `page.pageSize` to 50 for max-cap consistency).
  192) contract list explicit first-page positive-path coverage landed (done: smoke adds `contract-list-page-one`, asserting authenticated `GET /contracts?page=1` returns 200 and preserves explicit valid pagination input behavior).
  193) contract list minimum pageSize positive-path coverage landed (done: smoke adds `contract-list-page-size-one`, asserting authenticated `GET /contracts?pageSize=1` returns 200 and reflects `page.pageSize=1` for lower-bound pagination consistency).
  194) contract list max pageSize exact-boundary coverage landed (done: smoke adds `contract-list-page-size-fifty`, asserting authenticated `GET /contracts?pageSize=50` returns 200 and reflects `page.pageSize=50` for upper-bound equality consistency).
  195) contract list combined page/pageSize boundary coverage landed (done: smoke adds `contract-list-page-one-page-size-fifty`, asserting authenticated `GET /contracts?page=1&pageSize=50` returns 200 and reflects `page.pageSize=50` for combined-parameter consistency).
  196) search-filter artifact pollution prevention landed (done: smoke adds `contract-list-unauthorized-with-pagination-query` to lock auth-priority behavior on paginated contract list requests, and `api-real-smoke` now auto-clears historical `smoke-tag-*` industry tags and `Smoke Region *` regions before/after runs, plus normalizes DB loopback host `127.0.0.1 -> localhost` for stable local connectivity).
  197) search filter anti-leak hardening landed (done: client `usePublicIndustryTags` now suppresses `smoke-tag-/e2e-tag-/qa-tag-` test artifacts from filter UI, and smoke adds internal `smoke-filter-artifact-preflight` assertion to fail fast if leaked smoke tags/regions are detected before run).
  198) public filter data-source anti-leak guard landed (done: backend `RegionsService` now filters test artifacts (`smoke/e2e/qa`) from public `/public/industry-tags` and `/regions` while preserving full visibility on admin endpoints, and smoke cleanup now removes leaked test prefixes from both `industry_tags` table and persisted region `industryTags` arrays).
  199) public regions query anti-leak regression landed (done: smoke adds `public-regions-query-smoke-region`, asserting `GET /regions?q=Smoke%20Region` does not expose freshly created smoke test region code in public results).
  200) public regions nested-tag anti-leak guard landed (done: backend `RegionsService` now strips `smoke-tag-/e2e-tag-/qa-tag-` from public `/regions` response `industryTags` payload while preserving admin visibility, smoke adds `public-regions-list-after-industry-tags-set` regression assertion, and client region-name cache now sanitizes stale `Smoke Region/E2E/QA` artifacts before read/write).
  201) search filter prefill/cache anti-leak guard landed (done: client now sanitizes `industryTags` for search prefill ingestion, filter picker selected/extras rendering, and outbound search params; hidden test tags `smoke-tag-/e2e-tag-/qa-tag-` are deduped/removed to prevent stale artifacts from resurfacing in search filters).
  202) publish edit/draft industry-tags anti-leak guard landed (done: client publish pages for patent/demand/achievement now sanitize `industryTags` on detail backfill, local draft restore/persist, picker onChange, and outbound create/update payloads, preventing stale `smoke-tag-/e2e-tag-/qa-tag-` artifacts from being re-saved and resurfacing downstream).
  203) industry-tags visible-surface anti-leak guard landed (done: client now sanitizes `industryTags` before rendering across listing/search/favorites/detail/cluster surfaces, including demand and achievement primary/secondary tags, so stale `smoke-tag-/e2e-tag-/qa-tag-` artifacts are no longer shown to users even if historical data still contains them).
  204) API public payload industry-tags anti-leak guard landed (done: backend now sanitizes `industryTags` in public listing summaries/details and public demand/achievement responses, and also in favorites payloads, ensuring `smoke-tag-/e2e-tag-/qa-tag-` artifacts are filtered at API source instead of relying only on client rendering).
  205) API public detail smoke anti-leak regression coverage landed (done: `api-real-smoke` now seeds `industryTags` with `smoke-tag-*` in admin demand/achievement/listing create+update flows, then asserts `public/*/detail` responses for approved content do not expose those tags via `public-demand-detail-admin-smoke-industry-tags-sanitized`, `public-achievement-detail-admin-smoke-industry-tags-sanitized`, and `public-listing-detail-admin-smoke-industry-tags-sanitized`).
  206) API smoke runtime dist-freshness guard landed (done: `api-real-smoke` now checks timestamps between `apps/api/src` and `apps/api/dist`, auto-runs `pnpm -C apps/api build` when dist is missing/stale, and supports opt-out via `-SkipApiBuild`, preventing false results caused by running outdated dist binaries).
  207) public search industry-tags smoke anti-leak regression coverage landed (done: `api-real-smoke` now searches newly approved admin demand/achievement/listing by unique title and asserts matched `search/*` list items do not expose `smoke-tag-*` in `industryTags`, via `public-demand-search-admin-smoke-industry-tags-sanitized`, `public-achievement-search-admin-smoke-industry-tags-sanitized`, and `public-listing-search-admin-smoke-industry-tags-sanitized`).
  208) hidden industry-tag filter strict sanitization landed (done: backend now sanitizes incoming `industryTags` filters for public search and AI agent query paths, cluster surfaces sanitize institution tags on both API and client, and smoke adds `public-*-search-hidden-industry-filter-ignored` + `ai-agent-query-hidden-industry-filter-sanitized` regressions to assert `smoke-tag-*` filters are ignored instead of narrowing public results).
  209) hidden industry-tag variant (`smoke_tag`) anti-leak hardening landed (done: backend/client hidden-tag recognizers now cover hyphen/underscore/space variants, public industry-tags and public regions reuse the shared visibility rule, and smoke adds underscore-variant create/list/search/AI regressions to ensure `smoke_tag*` artifacts are never exposed in public filters or responses).
  210) smoke artifact cleanup/preflight variant alignment landed (done: `api-real-smoke` cleanup and preflight now use the same hidden-tag variant matcher (`^(smoke|e2e|qa)[-_ ]?tag(?:[-_ ]|$)`), so leaked `smoke_tag*` / `smoke tag*` artifacts are detected and cleaned before assertions run).
  211) region hidden-variant anti-leak and smoke chaos warmup stability landed (done: API/client region hidden-name guards now cover hyphen/underscore/space variants for `smoke/e2e/qa region`, smoke cleanup+preflight now align region variant matching, smoke adds `public-regions-query-smoke-region-underscore` regression, and chaos p95 threshold now uses a relaxed warmup ceiling before trend baseline is established while preserving strict threshold once baseline exists).
  212) hidden industry-tag space-variant (`smoke tag`) anti-leak hardening landed (done: smoke now seeds and verifies space-variant industry tags across admin create/list, public industry-tags, public demand/achievement/listing detail+search payload sanitization, public search hidden-filter ignore behavior, public regions nested-tag sanitization, and AI parsed-filter sanitization).
  213) announcement tags anti-leak hardening landed (done: `AnnouncementsService` now sanitizes hidden test industry-tag variants on public `list/getById` while preserving admin visibility; client announcement list/detail adds defensive sanitize; smoke adds `public-announcement-detail-admin-smoke-tags-sanitized` and `public-announcements-list-admin-smoke-tags-sanitized` regressions plus admin-side visibility assertions).
  214) round170 gate status captured (done: `api-real-smoke` passed `1749/1749`, `check-api-smoke-openapi-coverage` remained `238/238`, and `check-api-smoke-quality-floor` returned `violations=[]`).
  215) chaos guard drift-adaptive hardening landed (done: `api-real-smoke` chaos p95 guard now adds a p50-relative ceiling and auto-bypasses stale trend baseline checks under large drift, preserving anomaly detection while avoiding false negatives on changed machine/runtime profiles).
  216) patent-map public industry-breakdown anti-leak hardening landed (done: `PatentMapService.getRegionDetail` now filters hidden test industry-tag variants from public `industryBreakdown` while preserving admin visibility, and smoke extends patent-map upsert/detail assertions to verify hidden variants are blocked but visible tags are preserved).
  217) patent-map top-assignee schema compatibility hardening landed (done: patent-map top-assignee parsing/output now accepts both `name` and legacy `assigneeName`, and returns both fields for compatibility across client/admin/openapi type differences).
  218) patent-map top-assignee smoke compatibility regression landed (done: smoke upsert now uses `topAssignees[].name` payload form and asserts both admin/public detail can read back top-assignee name correctly, preventing field-compatibility regressions).
  219) round173 gate status captured (done: `api-real-smoke` passed `1749/1749`, `check-api-smoke-openapi-coverage` remained `238/238`, and `check-api-smoke-quality-floor` returned `violations=[]`).
  220) hidden test slash-variant (`smoke/tag`, `e2e/tag`, `qa/tag`) anti-leak hardening landed (done: API/client hidden artifact recognizers now include slash-separated variants for industry tags, service tags, and region names; `api-real-smoke` cleanup/preflight matchers were aligned to the same slash-capable regex; smoke now seeds slash variants and asserts admin-visible/public-hidden behavior across industry-tags list, announcements, public demand/achievement/listing detail+search payloads, hidden-industry-filter ignore behavior, regions nested-tag output, patent-map region detail, and AI parsed-filter sanitization).
  221) round174 gate status captured (done: `api-real-smoke` passed `1754/1754`, `check-api-smoke-openapi-coverage` remained `238/238`, and `check-api-smoke-quality-floor` returned `violations=[]`).
  222) round175 gate status captured (done: post-hardening re-run remained green with `api-real-smoke` `1754/1754`, `check-api-smoke-openapi-coverage` `238/238`, and `check-api-smoke-quality-floor` `violations=[]`).
  223) round176 gate status captured (done: `scripts/verify.ps1 -ReportDate 2026-03-12-r176` stayed green with `api-real-smoke` `1754/1754`, OpenAPI coverage `238/238`, and quality floor `violations=[]`).
  224) UI smoke port-collision false-pass stability hardening landed (done: `ui-dom/ui-render/ui-http/verify/api-real-smoke` port availability checks now detect active listeners across all local bindings, and UI smoke scripts now assert spawned mock/client/admin process liveness with stderr/stdout tail diagnostics before and during probes, preventing stale-process false positives that previously caused mid-run `ERR_CONNECTION_REFUSED` cascades in `ui-dom-smoke(full)`).
  225) round178 full UI smoke status captured (done: `ui-dom-smoke(full)` recovered to `83/83`, `ui-render-smoke(full)` remained `83/83`, and `ui-http-smoke` completed green after stability hardening).
  226) round179 gate status captured (done: post-hardening `scripts/verify.ps1 -ReportDate 2026-03-12-r179` remained fully green with `api-real-smoke` `1754/1754`, OpenAPI coverage `238/238`, quality floor `violations=[]`, plus `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing).
  227) UI smoke client route drift correction landed (done: `ui-dom-smoke`/`ui-render-smoke` now normalize non-tabbar `#/pages/*` client fragments to `#/subpackages/*`, eliminating false-green checks where URL hash matched but real subpackage content was not loaded; this also aligns full smoke routing behavior with `apps/client/src/app.config.ts` actual route topology).
  228) DOM runtime diagnostics hardening landed (done: `dump-dom-cdp` runtime exception capture now prioritizes exception descriptions and object preview properties, turning opaque `Uncaught/Object` outputs into actionable messages for targeted remediation).
  229) checkout/about H5 runtime exception fixes landed (done: `MiniProgramPayGuide` now guards browser-side env access so missing `process` no longer crashes `/subpackages/checkout/{deposit-pay|final-pay}` in H5, and About page now gates `getAccountInfoSync` to WeApp environment to prevent unsupported-API unhandled rejection on `/subpackages/about/index`).
  230) round181 full UI smoke status captured (done: post-route/exception fixes `ui-dom-smoke(full)` passed `83/83`, `ui-render-smoke(full)` passed `83/83`, and `ui-http-smoke` remained green `86/86`).
  231) round182 gate status captured (done: `scripts/verify.ps1 -ReportDate 2026-03-12-r182` remained fully green with `api-real-smoke` `1754/1754`, OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing).
  232) weapp-route-smoke launch resilience and diagnostics landed (done: `scripts/weapp-route-smoke.js/.ps1` now support `--launch-retries`, `--launch-retry-delay-ms`, and optional `--kill-stale-devtools`; launch failures now record per-attempt DevTools process diagnostics into report JSON, and `scripts/weapp-route-smoke.ps1 -NoAuth -ReportDate 2026-03-12-r184` passed `11/11` routes with artifact `.tmp/weapp-route-smoke-2026-03-12-r184.json`).
  233) round185 gate status captured (done: `scripts/verify.ps1 -ReportDate 2026-03-12-r185` remained fully green with `api-real-smoke` `1754/1754`, OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing).
  234) weapp-route-smoke failure-path report persistence landed (done: script now writes report JSON even when post-launch runtime errors occur, and `scripts/weapp-route-smoke.ps1 -ReportDate 2026-03-12-r188 -LaunchRetries 1 -KillStaleDevtools` produced expected auth-missing failure artifact `.tmp/weapp-route-smoke-2026-03-12-r188.json`; no-auth regression remained green on `2026-03-12-r187` with `11/11` routes).
  235) round189/190/191 full UI smoke status captured (done: `ui-http-smoke` `86/86` on `r189`, `ui-render-smoke(full)` `83/83` on `r190`, and `ui-dom-smoke(full)` `83/83` on `r191`, confirming no regressions after latest weapp-smoke script hardening batch).
  236) search-filter anti-pollution regression subset recheck captured (done: parsed `.tmp/api-real-smoke-2026-03-12-r185.json` and revalidated 24 targeted anti-leak checks including `industry-tags-sanitized`, `hidden-industry-filter-*`, `public-regions-query-smoke-region*`, and public announcement tag sanitization; matched `24`, failed `0`).
  237) API minimal test framework bootstrap landed (done: `apps/api` adds Vitest+Supertest+Nest testing harness, first `health` e2e test (`2/2`) is runnable via `pnpm -C apps/api test`, and CI now includes `API minimal tests` step to gate regressions early).
  238) chaos trend-threshold anti-flake guard tuned (done: `api-real-smoke` trend threshold now applies `+250ms` grace and reports base/effective trend threshold values, reducing transient false negatives while retaining absolute p95 guard).
  239) round194 gate status captured (done: `scripts/verify.ps1 -ReportDate 2026-03-12-r194` remained fully green with `api-real-smoke` `1754/1754`, OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing).
  240) write-first Vitest coverage expansion landed (done: `apps/api/test/favorites.write.spec.ts` adds success/failure/idempotency/auth-boundary checks for listing/demand/achievement/artwork favorite+unfavorite writes (`29` assertions), and `apps/api/test/addresses.write.spec.ts` adds create/update/remove write-path strictness checks (`9` assertions); total `apps/api` Vitest suite now `40/40`).
  241) comments write-first Vitest suite landed (done: `apps/api/test/comments.write.spec.ts` adds create/edit/delete/adminUpdate strict-path tests across auth/id-format/not-found/owner-boundary/filter-normalization/status-transition branches (`28` assertions); total `apps/api` Vitest suite now `68/68`).
  242) orders write-first Vitest suite landed (done: `apps/api/test/orders.write.spec.ts` adds create-order/payment-intent/refund-request/invoice-request write-path coverage across auth/id-format/status-conflict/ownership/reason-code/idempotent-friendly branches (`16` assertions); total `apps/api` Vitest suite now `84/84`).
  243) orders admin write-path coverage expanded (done: `apps/api/test/orders.write.spec.ts` now additionally covers admin refund approve/reject/complete and invoice issue/upsert/delete strict flows (`+8` assertions); total `apps/api` Vitest suite now `92/92`).
  244) round195 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-12-r195` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-12-r195/`).
  245) audit-flow Vitest strictness suite landed (done: `apps/api/test/audit-logs.read.spec.ts` adds strict page/pageSize and query-filter validation plus list mapping assertions for `/admin/audit-logs` service path (`5` assertions); total `apps/api` Vitest suite now `97/97`).
  246) listings search-filter strictness suite landed (done: `apps/api/test/listings.search-filters.spec.ts` adds strict empty-filter rejection, numeric-filter strictness, pageSize cap, and hidden smoke/e2e/qa industry-tag sanitization assertions for `searchPublic` (`6` assertions); total `apps/api` Vitest suite now `103/103`).
  247) organizations/inventors filter strictness suites landed (done: `apps/api/test/org-inventor.filters.spec.ts` adds strict `regionCode/type/page/pageSize` validations and filter-shape assertions for `organizations.list` and `inventors.search` (`6` assertions); total `apps/api` Vitest suite now `109/109`).
  248) round196 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-12-r196` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-12-r196/`).
  249) regions filter/sanitization suite landed (done: `apps/api/test/regions.filters.spec.ts` adds strict `level/parentCode/coordinate` validation and hidden smoke-region/industry-tag sanitization assertions for `listRegions`/`listIndustryTags` plus create/update/set-tags error mapping (`6` assertions); total `apps/api` Vitest suite now `115/115`).
  250) shared tag-sanitizer suite landed (done: `apps/api/test/content-utils.sanitize.spec.ts` adds `industry/service` hidden smoke/e2e/qa tag filtering plus case-insensitive dedupe and string-array normalization assertions (`5` assertions); total `apps/api` Vitest suite now `120/120`).
  251) regionCode filter strictness convergence landed (done: `InventorsService` / `OrganizationsService` / `TechManagersService` now require `regionCode` to be 6 digits in filter paths, and `apps/api/test/org-inventor.filters.spec.ts` was expanded with invalid-format regression checks to prevent polluted query values from entering search filters).
  252) tech-manager and patent-map strictness suites landed (done: `apps/api/test/tech-managers.filters.spec.ts` adds approved-only/public sanitization and admin visibility boundary assertions for service-tag filters (`4` tests), and `apps/api/test/patent-map.filters.spec.ts` adds summary/detail strict-filter validation plus dry-run import duplicate/missing-region guards including full-width list-separator parsing regression (`5` tests); total `apps/api` Vitest suite now `129/129`).
  253) round197 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-12-r197` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-12-r197/`).
  254) ai query filter strictness suite landed (done: `apps/api/test/ai.query-filters.spec.ts` adds strict invalid `inputType/contentScope/contentType/regionCode` coverage, hidden `industryTags` sanitization and non-array ignore behavior in `createAgentQuery`, plus `adminListParseResults` filter validation and pageSize cap assertions; total `apps/api` Vitest suite now `133/133`).
  255) announcements filter/sanitization suite landed (done: `apps/api/test/announcements.filters.spec.ts` adds strict pagination/status validation for public/admin list paths, pageSize cap assertions, public hidden-tag sanitization assertions, and invalid-id guard on detail path; total `apps/api` Vitest suite now `137/137`).
  256) contracts list-filter strictness suite landed (done: `apps/api/test/contracts.filters.spec.ts` adds auth boundary, strict `page/pageSize/status` validation, `WAIT_UPLOAD` special where-clause convergence checks, pagination cap assertions, and response item mapping checks with `contract-*` id prefix; total `apps/api` Vitest suite now `141/141`).
  257) round198 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r198` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r198/`).
  258) reports range-filter strictness suite landed (done: `apps/api/test/reports.filters.spec.ts` adds auth boundary, strict invalid `days/start/end` handling, `start>end` rejection, default-range metric aggregation checks, and export-range validation/URL return assertions; total `apps/api` Vitest suite now `145/145`).
  259) cases/notifications/user-verifications filter strictness suites landed (done: `apps/api/test/cases.filters.spec.ts` adds list auth+permission boundary, strict `status/type/page/pageSize` validation, UUID-query where-shape convergence and case-mapping assertions; `apps/api/test/notifications.filters.spec.ts` adds list pagination cap/invalid checks plus strict notification-id format and not-found/success branches; `apps/api/test/user-verifications.filters.spec.ts` adds admin-list strict `type/status/page/pageSize` validation and filter/pagination-shape assertions; total `apps/api` Vitest suite now `155/155`).
  260) alerts list-filter strictness suite landed (done: `apps/api/test/alerts.filters.spec.ts` adds auth+permission boundary, strict `status/severity/channel/targetType/type/targetId/triggeredFrom/triggeredTo/page/pageSize` validation, pagination-cap and where-shape convergence checks, plus alert dto mapping assertions; total `apps/api` Vitest suite now `159/159`).
  261) patents admin-list filter strictness suite landed (done: `apps/api/test/patents.filters.spec.ts` adds admin boundary, strict `patentType/legalStatus/page/pageSize` validation, pagination cap (`100`) and query where-shape assertions, plus patent dto mapping assertions including parties/legal-status normalization; total `apps/api` Vitest suite now `163/163`).
  262) conversations pagination/id strictness suite landed (done: `apps/api/test/conversations.filters.spec.ts` adds list auth boundary, strict `page/pageSize` validation and pagination-cap where-shape assertions for `listMine`, plus strict `conversationId` format and participant-boundary checks for `listMessages`; total `apps/api` Vitest suite now `167/167`).
  263) round199 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r199` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r199/`).
  264) demands/achievements/artworks strict-filter Vitest suites landed (done: `apps/api/test/demands.filters.spec.ts`, `apps/api/test/achievements.filters.spec.ts`, and `apps/api/test/artworks.filters.spec.ts` now cover public-search/admin-list strict query validation, pagination cap behavior, hidden-test-tag sanitization, and normalized where-shape assertions; total `apps/api` Vitest suite now `182/182`).
  265) round200 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r200` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing).
  266) orders list/invoice filter strictness suite landed (done: `apps/api/test/orders.filters.spec.ts` adds auth boundary, strict `page/pageSize/asRole/status/statusGroup` and invoice `status` validation, pagination cap assertions, seller-role/status-group where-shape convergence, and invoice-status mapping checks for `listOrders/listInvoices`).
  267) patent-maintenance list filter strictness suite landed (done: `apps/api/test/patent-maintenance.filters.spec.ts` adds auth+permission boundary, strict `page/pageSize/patentId/status/dueFrom/dueTo/scheduleId/assignedCsUserId` validation, pagination cap assertions, and normalized where-shape convergence for `listSchedules/listTasks`; total `apps/api` Vitest suite now `192/192`).
  268) favorites list filter strictness suite landed (done: `apps/api/test/favorites.filters.spec.ts` adds auth boundary, strict `page/pageSize` validation across list endpoints, pagination cap assertions for listing/artwork favorites, and hidden test industry-tag sanitization regression checks in listing favorites payload; total `apps/api` Vitest suite now `197/197`).
  269) rbac read/update strictness suite landed (done: `apps/api/test/rbac.read.spec.ts` adds auth+permission boundary for role listing, seed-path read mapping checks, list-users fallback role convergence, strict `userId/roleIds` validation in `updateUserRoles`, unknown-role rejection, and deduplicated role assignment checks; total `apps/api` Vitest suite now `202/202`).
  270) round201 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r201` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing).
  271) patent-clusters public filter strictness suite landed (done: `apps/api/test/patent-clusters.filters.spec.ts` adds strict `page/pageSize` validation, pagination cap behavior checks, and hidden test tag sanitization+dedupe assertions for public cluster/institution tags (`3` tests)).
  272) files temporary-access strictness suite landed (done: `apps/api/test/files.temporary-access.spec.ts` adds auth/existence/access-boundary checks plus strict `scope` and `expiresInSeconds/ttlSeconds` validation and preview URL issuance assertions (`4` tests); total `apps/api` Vitest suite now `209/209`).
  273) users profile+verification strictness suite landed (done: `apps/api/test/users.profile-verification.spec.ts` adds strict patch-field validation (`regionCode`, nickname length), `P2025 -> UNAUTHORIZED` mapping, nullable avatar/region normalization checks, verification submit strict payload checks (type/displayName/evidence/logo), duplicate-pending conflict guard, and auto-approve-personal audit-path assertions (`6` tests); total `apps/api` Vitest suite now `215/215`).
  274) round202 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r202` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing).
  275) admin-user-verifications controller strictness suite landed (done: `apps/api/test/admin-user-verifications.controller.spec.ts` adds permission-boundary checks across list/approve/reject/materials/audit-log endpoints, strict UUID param validation, normalized id forwarding, and delegation assertions for review/material/audit flows (`5` tests); total `apps/api` Vitest suite now `220/220`).
  276) admin-config controller permission/audit suite landed (done: `apps/api/test/admin-config.controller.spec.ts` adds `config.manage` permission boundary checks across all admin config endpoints, getter delegation checks, and update-path audit action/target-id assertions for all 8 config update routes (`3` tests); total `apps/api` Vitest suite now `223/223`).
  277) config-service behavior suite landed (done: `apps/api/test/config.service.spec.ts` adds default/invalid-json fallback checks for trade-rules and alert-config, version-bump persistence checks for trade-rules updates, and partial-merge persistence checks for alert-config updates (`5` tests)).
  278) public-config controller suite landed (done: `apps/api/test/public-config.controller.spec.ts` adds delegation checks for `/public/config/trade-rules` and `/public/config/customer-service` read paths (`2` tests); total `apps/api` Vitest suite now `230/230`).
  279) files-service temp-token strictness suite landed (done: `apps/api/test/files.service.spec.ts` adds TTL clamp checks (`60..3600`), token verification success-path checks, and strict rejections for mismatched fileId, tampered signature, and expired token (`5` tests); test bootstrap now seeds `FILE_TEMP_TOKEN_SECRET` in `apps/api/test/setup.ts`; total `apps/api` Vitest suite now `235/235`).
  280) round203 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r203` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r203/`).
  281) auth/webhooks strictness suites landed (done: `apps/api/test/auth.service.spec.ts` adds demo auth validation/login/bind strictness (`8` tests), `apps/api/test/auth.controller.spec.ts` adds delegation checks (`4` tests), `apps/api/test/webhooks.service.spec.ts` adds event normalization/payment+refund transition/failed-event-idempotency assertions (`5` tests), and `apps/api/test/webhooks.controller.spec.ts` adds webhook delegation check (`1` test); total `apps/api` Vitest suite now `253/253`).
  282) round204 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r204` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r204/`).
  283) demo-auth and webhook idempotency hardening suites landed (done: `apps/api/test/demo.config.spec.ts` adds release-env disable and strict flag parsing checks (`4` tests), `apps/api/test/auth.service.spec.ts` expands with demo-disabled and missing-secret rejection plus phoneCode required checks (`+3` tests, total `11`), and `apps/api/test/webhooks.service.spec.ts` expands with hash-key fallback and duplicate-idempotency safe-noop assertions (`+2` tests, total `7`); total `apps/api` Vitest suite now `262/262`).
  284) round205 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r205` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r205/`).
  285) addresses delegation and list strictness suites landed (done: `apps/api/test/addresses.controller.spec.ts` adds delegation/fallback-body checks for list/create/update/remove (`4` tests), and `apps/api/test/addresses.list.spec.ts` adds auth-boundary, query-ordering, and DTO mapping strictness checks (`3` tests); total `apps/api` Vitest suite now `269/269`).
  286) round206 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r206` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r206/`).
  287) webhook payment/refund branch-depth suite expansion landed (done: `apps/api/test/webhooks.service.spec.ts` adds `FINAL` payType derivation from `WAIT_FINAL_PAYMENT`, finalAmount backfill on escrow transition, existing `PENDING` payment update path, already-paid idempotent no-op branch, refund fallback via latest order-scoped request, and unresolved-payType safe-ignore branch (`+5` tests, total `12`); total `apps/api` Vitest suite now `274/274`).
  288) round207 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r207` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r207/`).

  289) cases/comments/conversations controller delegation suites landed (done: `apps/api/test/cases.controller.spec.ts` adds UUID normalization + invalid-id guard + fallback-body delegation checks (`6` tests), `apps/api/test/comments.controller.spec.ts` adds multi-content list/create + edit/delete/admin delegation checks with fallback-body coverage (`3` tests), and `apps/api/test/conversations.controller.spec.ts` adds list/create/message/read delegation and fallback-body checks (`4` tests); total `apps/api` Vitest suite now `396/396`).
  290) round219 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r219` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r219/`).

  291) my-verification/industry-tags/organizations/inventors/notifications/regions controller delegation suites landed (done: `apps/api/test/my-verification.controller.spec.ts` adds user-id extraction + submit/get delegation checks (`3` tests), `apps/api/test/industry-tags.controller.spec.ts` adds `config.manage` permission boundary and create/list delegation checks (`3` tests), `apps/api/test/organizations.controller.spec.ts` adds public list/detail delegation checks (`2` tests), `apps/api/test/inventors.controller.spec.ts` adds search delegation check (`1` test), `apps/api/test/notifications.controller.spec.ts` adds list/detail delegation checks (`2` tests), and `apps/api/test/regions.controller.spec.ts` adds public regions/industry-tags delegation checks (`2` tests); total `apps/api` Vitest suite now `409/409`).
  292) round220 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r220` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r220/`).

  293) ai/admin-ai/tech-managers/patent-map controller delegation suites landed (done: `apps/api/test/ai.controller.spec.ts` adds agent-query/feedback fallback-body delegation checks (`2` tests), `apps/api/test/admin-ai.controller.spec.ts` adds admin parse-result list/detail/update delegation checks (`3` tests), `apps/api/test/tech-managers.controller.spec.ts` adds public/admin delegation and permission-boundary checks (`3` tests), and `apps/api/test/patent-map.controller.spec.ts` adds year numeric conversion + summary/detail delegation checks (`3` tests); total `apps/api` Vitest suite now `420/420`).
  294) round222 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r222` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r222/`).
  295) contracts/patents/patent-maintenance/users/patent-clusters controller delegation suites landed (done: `apps/api/test/contracts.controller.spec.ts` adds list/detail delegation + fallback-body checks (`2` tests), `apps/api/test/patents.controller.spec.ts` adds list/detail/upsert/delete delegation checks (`4` tests), `apps/api/test/patent-maintenance.controller.spec.ts` adds list detail delegation checks (`2` tests), `apps/api/test/users.controller.spec.ts` adds profile/verification delegation checks (`2` tests), and `apps/api/test/patent-clusters.controller.spec.ts` adds list/detail delegation checks (`3` tests); total `apps/api` Vitest suite now `433/433`).
  296) files controller delegation suite landed (done: `apps/api/test/files.controller.spec.ts` adds upload/download/temporary-access delegation checks with fallback-body coverage (`4` tests); total `apps/api` Vitest suite now `437/437`).
  297) round223 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r223` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r223/`).
  298) notifications write-path and inventors ranking-query suites landed (done: `apps/api/test/notifications.write.spec.ts` adds `create/createMany` validation, default-field mapping, dedupe semantics, and no-op branches (`4` tests), and `apps/api/test/inventors.query.spec.ts` adds `queryRankings` zero-total short-circuit plus positive ranking branch checks (`2` tests); total `apps/api` Vitest suite now `443/443`).
  299) round224 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r224` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r224/`).
  300) alerts acknowledge and organizations detail service suites landed (done: `apps/api/test/alerts.acknowledge.spec.ts` adds auth/permission boundary, UUID strictness, missing-alert `404`, and successful acknowledge + audit payload checks (`4` tests), and `apps/api/test/organizations.detail.spec.ts` adds org-user-id strictness, missing-org `404`, and detail stats aggregation checks (`3` tests); total `apps/api` Vitest suite now `450/450`).
  301) round225 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r225` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r225/`).
  302) contracts upload service suite landed (done: `apps/api/test/contracts.upload.spec.ts` adds auth boundary, contract-id + contract-file-id strictness, missing-order `404`, non-seller forbidden, non-pdf rejection, prefixed contract-id normalization, and successful upsert mapping checks (`7` tests); total `apps/api` Vitest suite now `457/457`).
  303) round226 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r226` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r226/`).
  304) tech-managers public detail/admin update service suite landed (done: `apps/api/test/tech-managers.update-admin.spec.ts` adds auth boundary + UUID strictness, missing-target `404` branches for `getPublic/updateAdmin`, hidden test service-tag sanitization + evidence-id normalization assertions on public detail, strict payload validation on `intro/serviceTags/featuredRank/featuredUntil`, and successful admin-update/audit payload assertions (`5` tests); total `apps/api` Vitest suite now `462/462`).
  305) round227 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r227` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r227/`).
  306) ai feedback + parse-update service suite landed (done: `apps/api/test/ai.parse-feedback-update.spec.ts` adds feedback auth/id/score strictness, missing-parse-result `404`, reasonTags/comment normalization + actor-type mapping, admin parse-result id/not-found guards, admin update strict status validation, normalized payload upsert shape assertions, and audit-log write assertions (`6` tests); total `apps/api` Vitest suite now `468/468`).
  307) round228 full verify rerun captured (done: `scripts/verify.ps1 -ReportDate 2026-03-13-r228` remained fully green with `api-real-smoke` `1754/1754` (`writes=1297`,`reads=457`), OpenAPI coverage `238/238`, quality floor `violations=[]`, and `db-preflight/ui-http-smoke/ui-render-smoke(core)/ui-dom-smoke(core)` all passing; UI render artifacts updated at `docs/demo/rendered/ui-smoke-2026-03-13-r228/`).
