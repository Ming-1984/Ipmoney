# Ipmoney 专利交易小程序需求文档（优化补充版）

> 目标：让搜索列表的默认排序更贴近用户兴趣与区域产业特色；同时保持可控、可解释、可调参。
> 本文在现有需求基础上补充：业务边界、关键流程、检索字段规范、专利类型定义标准（发明/实用新型/外观设计）等。

## 0. 术语与范围（补充）

- **平台定位：** 居间/撮合服务，不直接替代专利权属转移的法定流程（过户/备案仍需按主管部门要求办理）。
- **品牌与视觉：** 前端主色建议为**橙色**（寓意成功/成交）；平台理念为“**专利点金台**”（把专利价值转化为可交易、可量化的收益）。
- **多端形态：** P0 支持 **微信小程序（买家/卖家） + 用户 H5（Taro；电脑端可用） + PC Web 管理后台（运营/客服/财务）**。
  - **电脑端 H5（P0）：** 支持浏览/搜索/详情/收藏/咨询；**下单与支付引导回小程序**（二维码/链接），避免 H5 支付/合规与渠道成本。
- **交易对象：** 以专利为主（后续可扩展商标/版权，但不在本期范围）。
- **交易方式：** 转让为主、许可为辅（P0 支持许可：独占/排他/普通；前台默认转让）。
- **订金：** 用于表达交易意向并触发客服介入；是否可退、退还条件、违约责任必须在协议与流程中明确。
- **上架信息来源：** 用户发布为主（P0 以用户上传/后台录入为准）；P1 可选接入官方/第三方检索源做自动回填与校验（减少手填、降低造假）。

## 1. 角色与权限（建议补充）

- **游客：** 可浏览公开信息与搜索；敏感字段（联系方式、证件等）默认不展示。
- **已注册未认证：** 可收藏/咨询；发布与支付前需完成实名认证/主体认证。
- **身份/主体认证（P0）：**
  - **个人：** 可直接授权信息完成注册（无需后台审核）；发布/交易前如需更严格实名可作为 P1 追加。
  - **企业：** 提交主体信息与材料，后台审核通过后可发布/交易；**通过后可在小程序“机构展示”中被检索/展示**。
  - **科研院校：** 提交主体信息与材料，后台审核通过后可发布/交易；**通过后可在小程序“机构展示”中被检索/展示**。
  - **政府：** 提交主体信息与材料，后台审核通过后可发布/交易（是否在前台展示可配置）。
  - **行业协会/学会：** 提交主体信息与材料，后台审核通过后可发布/交易（是否在前台展示可配置）。
  - **技术经理人：** 提交个人资质信息与材料，后台审核通过后可发布/交易（可作为“服务方/撮合方”展示）。
- **平台运营：** 内容审核、用户认证审核、类目/标签/敏感词配置。
- **客服：** 跟单、聊天监管、线下流程记录、发起退款/争议处理流转。
- **财务：** 对账、退款、佣金结算、发票。

### 1.1 后台 RBAC 权限矩阵（P0 默认，可配置）

> 说明：以下为 **P0 默认角色权限**（可在后台配置/扩展）；所有“审核/退款/放款/配置修改”等关键操作必须记录审计日志（见 6.3/ER）。

| 功能/操作（权限点） | 运营 | 客服 | 财务 | 管理员 |
|---|---|---|---|---|
| 用户与认证：查看/审核（`verification.read/review`） | 查看/审核 | 查看 | 查看 | 全部 |
| 上架：查看/审核（`listing.read/audit`） | 查看/审核 | 查看 | - | 全部 |
| 订单：查看（`order.read`） | 查看 | 查看 | 查看 | 全部 |
| 工单：创建/分配/跟进（`case.manage`） | 查看/分配 | 创建/跟进 | 查看 | 全部 |
| 里程碑：合同签署确认（`milestone.contractSigned.confirm`） | - | 操作 | - | 全部 |
| 里程碑：变更完成确认（`milestone.transferCompleted.confirm`） | - | 操作 | - | 全部 |
| 退款：买家申请处理（`refund.read/approve/reject`） | 审批 | 发起/建议 | 审批/执行 | 全部 |
| 结算：台账查看（`settlement.read`） | 查看 | 查看 | 查看 | 全部 |
| 放款：人工放款确认（`payout.manual.confirm`） | - | - | 操作 | 全部 |
| 发票：上传/下载电子发票（`invoice.manage`） | 查看 | - | 操作 | 全部 |
| 系统配置：交易规则/类目/敏感词（`config.manage`） | 操作 | - | - | 全部 |
| 报表/对账：查看/导出（`report.read/export`） | 查看/导出 | - | 查看/导出 | 全部 |
| 审计日志：查看（`auditLog.read`） | 查看 | 查看 | 查看 | 全部 |

## 2. 核心业务流程（端到端补充）

1. **卖家认证 → 发布专利商品 → 平台审核 → 上架**
2. **买家搜索/筛选 → 在线咨询 → 支付订金 → 系统分配客服**
3. **客服介入：** 资料核验/尽调清单 → 线下签约（转让/许可合同）→ 权属变更/备案（如适用）
4. **尾款与结算：** 线下合同签署确认 → 线上支付尾款（平台托管）→ 权属变更完成 → 结算佣金并放款 → 订单完成/归档
5. **退款/争议：** 按“可退/不可退”规则、证据上传、客服/运营处理

## 3. 检索功能与专利类型分类标准（用于搜索/筛选）

### 3.1 三种专利类型的法定定义（《专利法》第二条）

- **发明：** 对产品、方法或者其改进所提出的新的技术方案。
- **实用新型：** 对产品的形状、构造或者其结合所提出的适于实用的新的技术方案。
- **外观设计：** 对产品的整体或者局部的形状、图案或者其结合以及色彩与形状、图案的结合所作出的富有美感并适于工业应用的新设计。

### 3.2 存续期提示（《专利法》第四十二条）

- **发明：** 20 年；**实用新型：** 10 年；**外观设计：** 15 年（均自申请日起算）。

### 3.3 检索字段规范（建议最小集合）

- **关键标识：** 专利号/申请号/公开号（支持多格式输入，统一规范化存储）。
- **专利类型：** 发明/实用新型/外观设计（必填，支持自动识别后可改）。
- **分类号：** 发明/实用新型用 IPC；外观设计用 Locarno（LOC）分类（用于“行业/领域”精细筛选）。
- **法律状态：** 申请中、已授权、终止/失效、无效（以数据源为准）。
- **权利人/申请人、发明人/设计人、申请日、公开/公告日**。
- **交易字段：** 交易方式（转让/许可）、标价/面议、订金、地区、行业标签、发布时间、更新时间。

### 3.4 类型自动识别（可选增强）

- **若输入为公开号/公告号：** 末尾常见标记 A/B → 发明，U → 实用新型，S → 外观设计（以各国文献种类代码为准，存在例外需人工确认）。
- **若输入为中国申请号：** 常见格式 `YYYYTxxxxxxx.x`，其中 `T` 通常为 1/2/3 分别对应发明/实用新型/外观设计（PCT 等特殊号段需额外规则）。

### 3.5 参考来源（联网查询）

- WIPO Lex：《Patent Law of the People's Republic of China》Article 2 / Article 42：<https://www.wipo.int/wipolex/en/legislation/details/21027>
- WIPO IPC：<https://www.wipo.int/en/web/classification-ipc>
- WIPO Locarno：<https://www.wipo.int/en/web/classification-locarno>
- WIPO Standard ST.13（申请号编号结构建议）：<https://www.wipo.int/documents/d/standards/docs-en-03-13-01.pdf>
- WIPO Standard ST.16（文献种类代码 Kind code）：<https://www.wipo.int/documents/d/standards/docs-en-03-16-01.pdf>

### 3.6 专利号输入正则与规范化规则（P0 必做）

> 目标：让搜索列表的默认排序更贴近用户兴趣与区域产业特色；同时保持可控、可解释、可调参。

#### 3.6.1 支持的输入类型（P0：CN）

- **中国申请号（Application No.）**
  - **较新常见格式：** `YYYYTXXXXXXX.C`（含点号）或 `YYYYTXXXXXXXC`（纯数字）
    - `YYYY`=申请年（4 位），`T`=类型位（`1`/`2`/`3`），`XXXXXXX`=流水号（7 位），`C`=校验/控制位（1 位数字）
    - 例：`2023113409720` / `202311340972.0`
  - **历史常见格式（兼容）：** `YYTXXXXX.C`（含点号）或 `YYTXXXXX C`（纯数字/含分隔符）
    - `YY`=申请年（2 位），`T`=类型位（`1`/`2`/`3`），`XXXXX`=流水号（常见 5 位），`C`=校验/控制位（1 位数字）
    - 例（示意）：`96123456.1`
- **中国专利号（Patent No.）**：常见写法为 `ZL` + 申请号（本质上仍以申请号为主键）。
  - 例：`ZL202311340972.0`
- **中国公开号/公告号（Publication No. / Grant No.）**：`CN` + 数字 + **文献种类代码（Kind code）**
  - 例：`CN114123456A`、`CN114123456B`、`CN214123456U`、`CN314123456S`（Kind code 也可能包含数字，如 `A1/A8` 等，按数据源为准）

#### 3.6.2 规范化存储（建议输出字段）

- `jurisdiction`：国家/地区代码（WIPO ST.3），P0 默认 `CN`
- `identifier_type`：`CN_APPLICATION` / `CN_PUBLICATION` / `CN_PATENT(ZL)`
- `application_no_norm`：去掉点号与前缀后的纯数字（CN 常见长度 9 或 13 位），如 `2023113409720`
- `application_no_display`：展示格式（默认在末位前插入点号），如 `202311340972.0`
- `publication_no_norm`：去空格、全大写，如 `CN114123456A8`
- `patent_type`：`INVENTION` / `UTILITY_MODEL` / `DESIGN`
- `kind_code`：如 `A`/`B`/`U`/`S`/`A1`/`A8`…
- `parse_status`：`OK` / `FORMAT_ERROR` / `AMBIGUOUS`

#### 3.6.3 规范化清洗流程（先清洗再匹配）

1. **trim**：去首尾空白
2. **统一字符集**：全角转半角（数字、字母、冒号、括号等）；字母转大写
3. **删除噪声前缀/标签**：如 `专利号`/`申请号`/`公开号`/`公告号`/`：` 等（保留核心内容）
4. **移除分隔符**：空格、`-`、`_`、中文顿号/逗号等；**保留点号 `.`**（用于识别申请号校验位）
5. **模式匹配**：按优先级尝试 `CN_APPLICATION` → `CN_PUBLICATION` → 其他（P1 扩展）
6. **二次规范化**：输出 `*_norm` 与 `*_display`，并填充 `patent_type/kind_code`

#### 3.6.4 正则规则（建议“宽松识别 + 严格校验分层”）

> 说明：小程序/前端建议用“宽松识别”提升容错；后端入库前再做“严格校验 + 外部数据源核验”。

- **CN 申请号/专利号（宽松识别，允许 CN/ZL 前缀、允许有无点号；兼容新旧格式）**

```regex
^(?:CN)?(?:ZL)?(?<year>19\\d{2}|20\\d{2})(?<type>[123])(?<serial>\\d{7})\\.?((?<check>\\d))$
```

```regex
^(?:CN)?(?:ZL)?(?<year>\\d{2})(?<type>[123])(?<serial>\\d{5})\\.?((?<check>\\d))$
```

- **CN 公开号/公告号（宽松识别，允许 7~9 位数字 + Kind code，Kind code 允许带 1 位数字）**

```regex
^(?:CN)?(?<number>\\d{7,9})(?<kind>[A-Z]\\d?)$
```

- **类型映射（CN）**
  - 申请号 `type=1/2/3` → `发明/实用新型/外观设计`
  - 公开号 `kind=U` → `实用新型`；`kind=S` → `外观设计`；`kind=A/B` 通常为 `发明`（以数据源为准）

#### 3.6.5 校验策略（P0/P1）

- **P0：** 仅做“结构校验”（位数、字符集、前缀、Kind code 形态）；不强制校验位计算，避免误判导致用户无法发布。
- **P1：** 若确定数据源（CNIPA/第三方）提供校验位算法或校验接口，可增加“校验位/权属一致性/法律状态一致性”校验，并对异常给出可解释提示（例如“号码格式正确但未能在数据源检索到”）。

## 4. 筛选字段枚举与数据库字段设计（建议）

### 4.1 筛选字段枚举（面向前端/UI 与 API）

> 建议所有枚举值在接口中使用稳定的英文 code，避免 UI 文案变更导致兼容问题。

| 字段 | 类型 | 枚举/格式 | 说明 |
|---|---|---|---|
| `q` | string | 任意 | 搜索关键词（专利号/申请号/公开号/标题/摘要/申请人等） |
| `qType` | enum | `AUTO`/`NUMBER`/`KEYWORD`/`APPLICANT`/`INVENTOR` | 搜索类型（`AUTO` 自动判断） |
| `patentType` | enum | `INVENTION`/`UTILITY_MODEL`/`DESIGN` | 三种专利类型 |
| `legalStatus` | enum | `PENDING`/`GRANTED`/`EXPIRED`/`INVALIDATED`/`UNKNOWN` | 供筛选的“归一化状态”（可从事件流映射） |
| `ipc` | string[] | IPC 代码数组 | 发明/实用新型过滤；支持主分类/全部分类 |
| `locarno` | string[] | Locarno(LOC) 代码数组 | 外观过滤；支持主类/子类 |
| `tradeMode` | enum | `ASSIGNMENT`/`LICENSE` | 转让/许可 |
| `licenseMode` | enum | `EXCLUSIVE`/`SOLE`/`NON_EXCLUSIVE` | 许可方式（仅 `LICENSE` 时出现） |
| `priceType` | enum | `FIXED`/`NEGOTIABLE` | 一口价/面议 |
| `priceMin/priceMax` | number | >=0 | 总价区间（单位分/元统一） |
| `depositMin/depositMax` | number | >=0 | 订金区间 |
| `regionCode` | string | 行政区划码 | 发布地区/标的地区（建议统一国家标准或高德 adcode） |
| `createdFrom/createdTo` | date | `YYYY-MM-DD` | 发布时间区间 |
| `sortBy` | enum | `NEWEST`/`PRICE_ASC`/`PRICE_DESC`/`POPULAR` | 排序 |
| `page/pageSize` | number | 1.. | 分页 |

### 4.2 数据库字段设计（核心表，关系型数据库）

> 目标：让搜索列表的默认排序更贴近用户兴趣与区域产业特色；同时保持可控、可解释、可调参。

**A. 专利主数据（建议）**

- `patents`
  - `id`（PK）
  - `jurisdiction`（`CN`）
  - `application_no_norm`（唯一，建议与 `jurisdiction` 组成唯一键）
  - `application_no_display`
  - `patent_type`（`INVENTION/UTILITY_MODEL/DESIGN`）
  - `title`、`abstract`
  - `filing_date`、`publication_date`、`grant_date`（可空）
  - `legal_status`（归一化枚举）+ `legal_status_raw`（保留原始文本/代码）
  - `source_primary`（主数据来源：`USER/ADMIN/PROVIDER`）+ `source_updated_at`
  - `created_at`、`updated_at`
- `patent_identifiers`（一专利多号码）
  - `id`（PK）、`patent_id`（FK）
  - `id_type`（`APPLICATION/PATENT(ZL)/PUBLICATION`）
  - `id_value_norm`（唯一，便于反查）
  - `kind_code`（公开号/公告号才有）
  - `date`（公开/公告日等，可空）
- `patent_classifications`
  - `id`（PK）、`patent_id`（FK）
  - `system`（`IPC/LOC/CPC`…）、`code`、`is_main`（主分类标记）
- `patent_parties`
  - `id`（PK）、`patent_id`（FK）
  - `role`（`APPLICANT/INVENTOR/ASSIGNEE`）、`name`、`country_code`（可空）
- `patent_legal_events`（可选：事件流，用于生成法律状态）
  - `id`（PK）、`patent_id`（FK）
  - `event_date`、`event_code`、`event_text_raw`、`source`

**B. 上架商品（专利交易）**

- `listings`
  - `id`（PK）
  - `seller_user_id`（FK）
  - `patent_id`（FK，可空：P0 允许先发后补齐专利主数据）
  - `title`（可覆盖专利标题）、`summary`（卖点）
  - `trade_mode`（`ASSIGNMENT/LICENSE`）+ `license_mode`（可空）
  - `price_type`（`FIXED/NEGOTIABLE`）+ `price_amount`（可空）
  - `deposit_amount`
  - `region_code`
  - `audit_status`（`PENDING/APPROVED/REJECTED`）
  - `status`（`DRAFT/ACTIVE/OFF_SHELF/SOLD`）
  - `created_at`、`updated_at`
- `files`（对象存储文件元数据）
  - `id`（PK）、`url`、`mime_type`、`size_bytes`
  - `owner_scope`（`LISTING/CASE/REFUND_REQUEST/USER`）、`owner_id`
  - `created_at`
- `listing_media`（图片/附件）
  - `id`、`listing_id`、`file_id`、`type`、`sort`
- `listing_audit_logs`
  - `id`、`listing_id`、`reviewer_id`、`action`、`reason`、`created_at`
- `audit_logs`（关键操作审计日志，跨域）
  - `id`、`actor_user_id`、`action`（如 `LISTING_APPROVE/REFUND_APPROVE/PAYOUT_CONFIRM/CONFIG_UPDATE`）
  - `target_type`、`target_id`（多态指向：`LISTING/ORDER/REFUND_REQUEST/SYSTEM_CONFIG`…）
  - `before_json`、`after_json`（JSON，记录修改前后）、`request_id`、`ip`、`user_agent`、`created_at`

**C. 订单与支付（订金/尾款）**

- `orders`
  - `id`（PK）、`listing_id`（FK）、`buyer_user_id`（FK）
  - `status`（`DEPOSIT_PENDING/DEPOSIT_PAID/WAIT_FINAL_PAYMENT/FINAL_PAID_ESCROW/READY_TO_SETTLE/COMPLETED/CANCELLED/REFUNDING/REFUNDED`）
  - `deal_amount`（成交总价，可空：合同签署确认后写入）
  - `deposit_amount`、`final_amount`（可空：合同签署确认后写入）、`commission_amount`（可空：结算时写入）
  - `assigned_cs_user_id`（跟单客服，可空）
  - `created_at`、`updated_at`
- `payments`
  - `id`、`order_id`、`pay_type`（`DEPOSIT/FINAL/REFUND/PAYOUT`）
  - `channel`（`WECHAT/ALIPAY`…）、`trade_no`、`amount`、`status`、`paid_at`

**C2. 退款/工单/结算（建议最小表）**

> 说明：P0 以“可追溯、可对账、可仲裁”为目标，至少需要退款申请、客服工单与结算台账 3 类数据承载。

- `refund_requests`
  - `id`、`order_id`
  - `reason_code`、`reason_text`（可空）
  - `status`（`PENDING/APPROVED/REJECTED/REFUNDING/REFUNDED`）
  - `created_at`、`updated_at`
- `cs_cases`（跟单/退款/争议）
  - `id`、`order_id`、`cs_user_id`
  - `type`（`FOLLOWUP/REFUND/DISPUTE`）、`status`
  - `created_at`、`updated_at`
- `cs_milestones`（里程碑+证据）
  - `id`、`case_id`
  - `name`（`CONTRACT_SIGNED/TRANSFER_SUBMITTED/TRANSFER_COMPLETED`）
  - `status`（`PENDING/DONE`）
  - `evidence_file_id`（可空）、`created_at`
- `settlements`（结算台账）
  - `id`、`order_id`
  - `gross_amount`（成交总额）、`commission_amount`、`payout_amount`
  - `payout_method`（`MANUAL`/`WECHAT`…，P0 默认 `MANUAL`）
  - `payout_status`（`PENDING/SUCCEEDED/FAILED`）
  - `payout_ref`（可空：银行流水号/转账单号等）、`payout_evidence_file_id`（可空）
  - `status`、`created_at`、`updated_at`

**C3. 系统配置（承载 8.4，可后台配置）**

- `system_configs`
  - `id`、`key`（唯一）、`value_type`（`INT/DECIMAL/STRING/JSON/BOOL`）、`value`
  - `scope`（`GLOBAL` 或 `TENANT` 预留）、`version`
  - `updated_by`、`created_at`、`updated_at`

**D. 检索性能与索引建议（关键）**

- `patents(jurisdiction, application_no_norm)` 唯一索引
- `patent_identifiers(id_value_norm)` 唯一索引（支持任意号码反查专利）
- `listings(status, audit_status, created_at)` 组合索引（列表页）
- 标题/摘要/申请人：建议接入全文检索（PostgreSQL FTS / Elasticsearch / OpenSearch）

## 5. 数据来源与录入方案（已确认：用户上传 + 后台录入/导入）

> 本期不接入 CNIPA/第三方检索接口：专利信息以“用户发布时上传/填写 + 后台补录/导入”为准，并通过审核与材料校验控制风险。

### 5.1 本期数据来源

1. **用户上传（主）**
   - 发布专利商品时：输入专利号/申请号/公开号（按 3.6 规范化）+ 填写交易信息 + 上传权属/授权材料。
2. **后台录入/导入（辅）**
   - 运营可后台创建 `patents` 主数据、创建/代发 `listings`，并支持 Excel 批量导入（用于“精选交易/平台招商内容”）。

### 5.2 数据质量与风控（取代“外部权威核验”）

- **结构校验：** 号码格式与类型映射（3.6），字段必填校验。
- **材料校验：** 上架前必须上传权属/授权链路材料（7.3），审核通过后方可展示“可交易”。
- **重复与冒用：** 允许同一 `application_no_norm` 多卖家上架，但每条 listing 必须独立通过材料审核与风控（7.4）。
- **数据标识：** 专利详情页展示“数据来源=用户上传/后台录入”与更新时间，避免用户误解为官方数据。

### 5.3 可选扩展（P1）：外部数据源接入

> 若后续需要“自动回填/法律状态自动刷新/权威核验”，再接入外部数据源，并通过适配层隔离差异。

1. **官方渠道（推荐）**：CNIPA 公共服务平台/数据服务（接口需签约授权）。
2. **第三方商业数据源**：incoPat、PatSnap、Questel/Orbit、Derwent 等（以采购与接口文档为准）。
3. **公开网页抓取/非公开接口（不建议生产）**：稳定性与合规风险高。

### 5.4 统一适配层（Provider Adapter）（P1）

- 抽象接口（示例）：
  - `lookupByApplicationNo(appNo_norm) -> PatentSnapshot`
  - `lookupByPublicationNo(pubNo_norm) -> PatentSnapshot`
  - `search(q, filters, page) -> PatentSnapshot[]`
  - `getLegalEvents(appNo_norm) -> LegalEvent[]`
- `PatentSnapshot` 建议包含：号码集合、标题摘要、类型、分类号、当事人、关键日期、法律状态、来源与更新时间

### 5.5 合规与安全要点

- 使用任何第三方/官方数据前，确认：**授权范围、可展示字段、可缓存期限、商业用途限制**。
- API Key/密钥：后端托管、分环境、定期轮换；日志脱敏（不记录完整密钥与敏感个人信息）。
- 平台内上传的证据材料（证件/合同等）按隐私与商业秘密做访问控制与水印（6.3/6.4）。

## 6. 非功能性需求（NFR）与合规清单（建议补充）

> 当前文档以“功能需求”为主，若要进入可开发阶段，建议把以下 NFR 与合规条款补齐并固化（否则后期会反复返工）。

### 6.1 性能与容量（示例口径，需结合预算确认）

- **搜索/列表页：** P95 响应 ≤ 800ms（缓存命中），≤ 2s（需外部数据源回填时走异步）。
- **下单/支付：** 支付回调与订单状态一致性必须可追溯（幂等、可重放）。
- **并发与限流：** 对搜索与数据源查询做用户级/全局限流，避免被供应商封禁。

### 6.2 可用性与容灾

- **数据备份：** 数据库日备 + 关键表增量备；支持按 `patent_id/listing_id/order_id` 快速恢复。
- **高可用：** 关键服务（支付回调、订单状态机）优先保障；支持灰度发布与回滚。

### 6.3 安全与隐私（合规最小要求）

- **账号与权限：** 后台必须 RBAC；客服与运营分权；所有敏感操作记录审计日志。
- **敏感信息：** 证件照/合同/联系方式默认不公开；下载与查看需权限 + 水印（可选）。
- **风控：** 防刷（短信/验证码/接口限流）、黑名单、异常交易报警（高金额、频繁退款、疑似导流）。
- **隐私合规：** 按《个人信息保护法》等要求提供隐私政策、删除/更正通道、数据最小化、访问留痕。

### 6.4 内容与交易合规

- **内容审核：** 文本敏感词 + 图片 OCR/涉黄涉政识别（可选），支持用户举报与下架。
- **交易风险提示：** 明确平台为居间撮合；展示“数据来源与更新时间”；提示线下签约与权属变更风险。

### 6.5 可观测性与运营

- **日志/指标/告警：** 订单状态异常、支付回调失败、数据源失败率、审核积压量、客服 SLA。
- **埋点指标：** 搜索→咨询→支付订金→成交 的转化漏斗；成交金额、客单价、退款率。

### 6.6 合规提示文案与用户协议清单（P0 占位，需产品/法务确认）

> 目标：让搜索列表的默认排序更贴近用户兴趣与区域产业特色；同时保持可控、可解释、可调参。

**建议最少协议/规则页（P0）**

1. 《用户协议》
2. 《隐私政策》
3. 《专利交易居间服务协议》（平台服务性质、收费与发票、免责与争议处理）
4. 《订金支付与退款规则》（自动秒退窗口、人工审核条件、不可退条件、证据要求）
5. 《交易与结算规则》（尾款托管、佣金卖家承担、放款条件=变更完成确认、争议处理）
6. 《卖家上架与权属承诺》（权属真实性、授权/转让链路、质押/许可现状声明、虚假信息处置）
7. 《争议处理与证据规则》（证据类型、提交方式、平台裁定流程、升级法务/仲裁占位）
8. 《内容规范与举报规则》（重复上架/冒用权属/导流等）
9. 《发票与税务说明》

**强制勾选/触达点位（P0 建议）**

- 注册/登录页：勾选《用户协议》《隐私政策》
- 支付订金前：勾选《专利交易居间服务协议》《订金支付与退款规则》
- 支付尾款前：再次展示《交易与结算规则》（强调“线下签约确认后再付尾款”“尾款托管”“放款条件”）
- 卖家提交上架审核前：勾选《卖家上架与权属承诺》

## 7. 关键业务规则缺口（必须定稿，否则无法落库/联调）

### 7.1 订金与退款规则（已确认，按规范落地）

> 关键词：**订金线上支付、资金平台托管、线下签合同后再付尾款、退款有规则可追溯**。

**订金金额（可配置参数）**

- 平台支持 3 种策略：**固定值 / 阶梯 / 按标的总价比例（含上限/下限）**。
- **P0 默认值（后台可配置）：** 若为一口价（`priceType=FIXED`），订金按 `比例` 计算；若为面议（`priceType=NEGOTIABLE`），订金按 `固定值`。
  - `deposit_rate = 5%`
  - `deposit_min = ¥1,000`
  - `deposit_max = ¥20,000`
  - `deposit_fixed_for_negotiable = ¥2,000`
- **计算规则（P0）：**
  - `FIXED`：`deposit_amount = clamp(round(price_amount * deposit_rate), deposit_min, deposit_max)`（且不得超过 `price_amount`）
  - `NEGOTIABLE`：`deposit_amount = deposit_fixed_for_negotiable`

**资金流与抵扣**

- 订金由买家支付进入平台托管账户，**冻结**至“退款完成”或“交易完成放款”。
- 订金计入交易总价：尾款 = 成交价 - 订金（尾款同样线上支付并托管）。

**退款条件（按订单阶段）**

- **自动可退（系统秒退，P0 默认 30 分钟）：** 买家支付订金后 **30 分钟内**，且**客服未开始线下处理**时，买家可无理由申请退订金并系统原路秒退。
  - “未开始线下处理”定义（P0）：客服未点击“开始处理/开始跟单”，且工单未产生任何需双方配合的线下动作记录（如：未发起材料清单、未上传证据/合同页、未记录已寄送/已签署等）。
- **人工审核可退：** 已分配客服、但**线下合同未签署**时，满足以下任一条件可申请退订金：
  - 卖家未在规定时限内补齐权属/授权材料或材料明显不符
  - 双方协商一致取消（需双方线上确认或客服记录）
  - 平台风控/审核判定信息虚假或涉嫌侵权
- **原则不可退：** 线下合同已签署且无卖家违约/重大瑕疵情形时，买家单方反悔订金不退。
- **争议处理：** 发生纠纷（例如合同争议、权属争议）进入“争议工单”，由客服/运营按证据裁定（可升级法务）。

**退款流程（状态机）**

1. 买家发起退款 → 系统判断是否满足“自动可退”
2. 自动可退：直接原路退回并关闭订单
3. 人工审核：生成退款工单 → 客服收集证据/双方确认 → 运营审批 → 原路退款
4. 全链路记录：退款原因、证据附件、审批人、时间戳、支付平台退款单号（用于对账与审计）

### 7.2 平台收费与结算

**佣金（已确认：卖家承担）**

- 佣金从卖家应收款中扣除：卖家收款 =（订金 + 尾款）- 佣金。
- 佣金计费方式（可配置）：
  - **P0 默认：按成交价比例**（卖家承担），并提供下限/上限控制：`commission = clamp(round(deal_amount * 5%), ¥1,000, ¥50,000)`。
  - P1 可扩展：固定服务费 / 分档（可按类目/金额分段）。

**结算点（已确认：尾款线上支付）**

- 线下签署合同后，由客服/运营在后台完成“合同签署确认”里程碑，系统解锁买家“支付尾款”入口。
- 买家线上支付尾款进入平台托管账户；**P0 默认仅在“权属变更完成确认”后放款与结算**（超时自动放款默认关闭，仅支持人工特批）。
  - **放款方式（P0 默认）：** 财务线下放款（银行转账/企业网银等）→ 后台回传放款凭证与流水号 → 系统记账完成并关闭订单。

**发票（P0：线下开票，平台仅提供上传/下载）**

> 定位：平台仅对“平台服务费/佣金”开具发票；**不对专利转让/许可标的价款开票**（标的发票由卖家与买家线下另行约定）。

- **开票范围（P0 默认）：** 仅开具“平台服务费/佣金”发票；开票金额默认为 `commission_amount`（见结算台账）。
- **处理方式（P0）：** 卖家如需开票，线下联系平台（客服/财务）收集抬头信息 → 财务线下开票 → 后台选择**已完成订单**上传电子发票文件（PDF/图片）并可填写发票号码/开票日期 → 系统通知卖家在平台内下载。
- **系统不做（P0）：** 不做“开票申请单/状态流转/邮箱投递”；抬头与税号由财务线下收集（如需线上申请与专票作为 P1 扩展）。
- **留痕与权限（P0）：** 发票附件属于敏感资料；仅财务/管理员可上传/替换；下载需鉴权；上传/替换/下载写审计日志（who/when/what）。

### 7.3 线下签约/权属变更的“交付清单”

> 已确认：合同线下签署完成后，再回到平台支付尾款。

- 合同模板：转让/许可（独占/排他/普通）模板与必填条款清单（平台提供模板下载，线下签署后需回传关键页）。
- 权属材料：证书、权利人主体证明、授权/转让链路（如有）、质押/许可现状声明。
- 关键里程碑：合同签署、缴费、提交变更、变更完成；每一步的责任人与证据归档。

### 7.4 同一专利多次上架的规则

> 已确认：允许多个卖家上架同一 `application_no_norm`，由后台审核与风控把关。

- **上架策略：** 同一专利可存在多个有效 `listings`（不同卖家/不同价格/不同交易方式）。
- **审核要求：** 每个 listing 必须单独通过权属/授权材料审核；支持“疑似冒用”标记与拒绝上架。
- **前台展示：** 在专利聚合页展示“该专利有 N 个报价/供给”，默认排序：价格（可选）/信誉分/更新时间。
- **举报与处置：** 买家可举报；客服生成工单并可临时下架；重复违规卖家可封禁并保留证据链。

## 8. 下一步建议：架构图 + 产出物（建议按此顺序推进）

### 8.1 先做哪些“定稿决策”

1. **确认 MVP（P0）范围**：
   - 标的范围：**P0 仅做 CN 专利**
   - 交易方式：**转让为主、许可为辅（P0 支持许可）**
   - 资金路径：**尾款线上支付（已确认）**
2. **定稿关键业务规则**：订金/退款、佣金/结算、审核标准、客服 SLA。
3. **确定数据来源路线**：**用户上传 + 后台录入/导入（已确认）**；外部权威数据源接入作为 P1 可选。

### 8.2 建议产出物

- **系统架构图（C4 口径）**：Context/Container/Component（见 `docs/architecture/`）
- **关键时序图**：支付订金/退款/结算（见 `docs/architecture/`）
- **ER 图**：见 `docs/architecture/er-diagram.mmd`
- **API 文档（OpenAPI）**：见 `docs/api/openapi.yaml`
- **原型与页面流**：围绕 P0 主链路（搜索→详情→咨询→订金→跟单→完成）

### 8.3 架构/时序/ER/OpenAPI 文件清单（本仓库输出）

- C4-Context：`docs/architecture/c4-context.mmd`
- C4-Container（可拆分微服务）：`docs/architecture/c4-container.mmd`
- C4-Component（以订单域为例）：`docs/architecture/c4-component-order.mmd`
- 时序-支付订金：`docs/architecture/sequence-deposit-payment.mmd`
- 时序-退款：`docs/architecture/sequence-refund.mmd`
- 时序-结算放款：`docs/architecture/sequence-settlement.mmd`
- 业务流程-发布上架：`docs/architecture/flow-listing-publish.mmd`
- 业务流程-交易主链路：`docs/architecture/flow-trade-end2end.mmd`
- 业务流程-退款/争议：`docs/architecture/flow-refund-dispute.mmd`
- 状态机-订单：`docs/architecture/state-order.mmd`
- ER 图：`docs/architecture/er-diagram.mmd`
- OpenAPI：`docs/api/openapi.yaml`

### 8.4 P0 默认参数（可后台配置）

> 说明：以下为**P0 默认值**（已可用于研发落地），后续可在后台“系统设置”调整；涉及争议风险的“超时自动放款/自动退款”默认关闭或降级为人工处理。

- **订金参数（已定稿）：**
  - `deposit_rate = 5%`
  - `deposit_min = ¥1,000`
  - `deposit_max = ¥20,000`
  - `deposit_fixed_for_negotiable = ¥2,000`
- **自动退款时间窗（已定稿）：** `auto_refund_window_minutes = 30`（见 7.1“自动可退”定义）
- **线下推进时限（已定稿，超时默认不自动放款）：**
  - `seller_material_deadline_business_days = 3`（卖家补齐权属/授权材料；超时后买家发起退款可按规则自动通过/优先通过）
  - `contract_signed_deadline_business_days = 10`（线下合同签署确认；超时后买家发起退款可按规则自动通过/优先通过）
  - `transfer_completed_sla_days = 90`（权属变更完成预期；超时触发预警 + 升级人工跟进/争议工单）
    - 参考：公开法律问答普遍认为专利权转让登记通常需 **1–3 个月**（示例：<https://www.64365.com/zs/1103443.aspx>）。
- **佣金参数（已定稿，卖家承担）：**
  - `commission_rate = 5%`
  - `commission_min = ¥1,000`
  - `commission_max = ¥50,000`
- **托管放款条件（已定稿）：**
  - `payout_condition = TRANSFER_COMPLETED_CONFIRMED`（必须“权属变更完成确认”才放款）
  - `auto_payout_on_timeout = false`（P0 关闭；如遇极端情况仅走人工特批并留痕）
  - `payout_method_default = MANUAL`（P0 默认财务人工放款并回传凭证；P1 可升级自动放款能力）
- **发票参数（P0 默认）：**
  - `invoice_enabled = true`
  - `invoice_scope = COMMISSION_ONLY`（仅对平台服务费/佣金开票）
  - `invoice_type_default = ELECTRONIC_GENERAL`（电子普通发票）
  - `invoice_item_name_default = 居间服务费`（发票项目名称/服务内容展示）
  - `invoice_delivery = IN_APP_DOWNLOAD_ONLY`（平台内下载，不做邮箱投递）
- **推荐参数（新增，P0 默认）：**
  - `recommend_enabled = true`
  - `recommend_time_decay_half_life_hours = 72`（时间衰减半衰期；用于新鲜度）
  - `recommend_weights = {"time":0.35,"view":0.15,"favorite":0.25,"consult":0.20,"region":0.20,"user":0.15}`（后台可调；权重调整后立即生效）
  - `recommend_featured_boost = {"province":2.0,"city":1.5}`（省/市级特色产业专利置顶加成系数）
  - `recommend_dedupe_window_hours = 24`（同用户/设备对同信息的浏览/咨询去重窗口）

---

### 第一部分：移动端（用户前台）功能明细

#### 1. 登录/注册与角色引导页

- **页面目标：** 明确用户身份，建立信任基础。
- **核心功能：**
  - **手机号一键登录/验证码登录**。
  - **身份选择引导（首次登录弹出）：**
    - **个人：** 独立发明人、个人中介等（可直接授权信息注册成功）。
    - **企业：** 适合寻找技术或出售公司资产（需提交主体信息与证明材料）。
    - **科研院校：** 高校/科研院所（需提交主体信息与证明材料）。
    - **政府：** 政府部门/园区管委会等（需提交主体信息与证明材料）。
    - **行业协会/学会：** 行业组织（需提交主体信息与证明材料）。
    - **技术经理人：** 技术经纪/技术经理人（需提交资质信息与证明材料）。
- **交互逻辑：**
  - 个人：授权信息 → 注册成功 → 进入首页。
  - 其他类型：填写信息 + 上传材料 → 提交审核 → 显示“审核中”；后台通过后解锁发布/交易能力。

#### 2. 首页 (Home)

- **页面风格：** 橙色主调（寓意成功/成交），视觉上可用“金豆/矿脉/挖矿”做轻量点缀（避免低俗化）。
- **核心模块：**
  - **全局搜索栏：** 占位符“输入专利号、技术关键词、企业或发明人”。
  - **轮播图 (Banner)：** 后台配置，展示平台活动、政策解读。
  - **快捷入口 (Grid)：** 沉睡专利（直达搜索）、发明人榜、专利地图、机构展示。
  - **数据快报 (Marquee)：** 滚动文字：“恭喜某企业刚刚成交一项发明专利”、“今日新增专利信息 102 条”。
  - **入驻机构展示（新增，P0）：**
    - 展示对象：**已审核通过的企业/科研院校**（默认展示；政府/协会/技术经理人是否展示可后台开关）。
    - 展示形态：Logo/名称横滑卡片 + “更多”进入机构列表（可搜索/按地区筛选）。
    - 点击机构：进入“机构聚合页”（展示该机构的上架信息/成果/需求等）。
  - **最新专利推荐区：**
    - **最新专利：** 默认按发布时间排序，展示最新上架专利。

##### 2.1 智能推荐算法 + 地域特色推荐（新增，P0）

> 目标：让搜索列表的默认排序更贴近用户兴趣与区域产业特色；同时保持可控、可解释、可调参。

**智能推荐（多维度权重，实时计算）**

- **推荐信号（P0）：**
  - 信息发布时间（新鲜度/时间衰减）
  - 详情点击量（`view_count`）
  - 收藏热度（`favorite_count`）
  - 咨询热度（`consult_count`，以“点击咨询/留资/联系客服”事件计）
- **推荐评分（示例公式，可配置权重）：**
  - `score = w_time*decay(age) + w_view*log1p(view) + w_fav*log1p(fav) + w_consult*log1p(consult) + w_region*regionMatch + w_featured*featuredBoost + w_user*tagSimilarity`
  - 说明：
    - `decay(age)`：时间衰减（如半衰期 `recommend_time_decay_half_life_hours`）
    - `log1p(x)`：抑制刷量（热度增长递减）
    - `tagSimilarity`：基于用户历史行为（收藏/咨询/成交）形成的“兴趣标签向量”，与信息的“产业标签”相似度（冷启动为 0）
- **实时性（P0 方案）：**
  - 统计字段在服务端增量更新（浏览/收藏/咨询事件落库），推荐排序在查询时按最新计数+当前权重实时计算（权重调整后立即生效）。
  - 反作弊（最低成本版）：对同一用户/设备对同一信息的浏览/咨询计数做时间窗去重（如 24h 只记 1 次）。

**地域特色推荐（LBS + 区域产业标签）**

- **区域优先：** 用户授权定位（LBS）或手动选择地区后，系统优先展示用户所在区域（同市 > 同省 > 其他）的专利成果/交易信息。
- **特色产业专利置顶：**
  - 后台支持把某条信息标记为“**省级/市级特色产业专利**”（并可设置置顶优先级/排序权重）。
  - 在首页推荐与搜索列表中对符合区域的“特色产业专利”给予高权重加成，必要时置顶展示（同时保留“为什么推荐/置顶”的解释文案）。
- **搜索与推荐融合：**
  - 搜索有关键词时：在文本相关性（relevance）基础上叠加 `score` 与 `region/featured` 加成，避免出现“相关但不本地/不热门”长期排前的问题。
- 无关键词（空搜索）时：默认按 `score` 输出推荐排序；首页展示最新专利。

#### 3. 区域产业专利地图页 (Patent Map)

- **数据逻辑：** 数据由后台手动录入，前端负责可视化渲染。
- **页面元素：**
  - **顶部筛选：** 切换“年份”（2024/2025）、“省份/区域”。
  - **地图可视化区：** 中国地图或省份地图。颜色深浅代表专利数量多少。点击某区域弹出浮窗显示该区域核心数据。
  - **产业饼图/柱状图：** 展示该区域下的产业占比（如：新材料 40%，AI 20%）。
  - **重点机构列表：** 该区域内专利数 Top 10 的企业/高校名单（点击可跳转其机构主页/聚合页）。

#### 4. 搜索结果页 (Search Results)

用户可以在此浏览三类信息，作为搜索结果页。

- **分类 Tab：** 专利交易 | 产学研需求 | 成果展示 | **机构展示（企业/科研院校）**。
- **列表项样式 (Card)：**
  - **专利交易卡片：** 专利名称 + 法律状态标签（已授权/实审）+ **价格（或“面议”）** + 缩略图。
  - **需求信息卡片：** 需求标题 + 预算范围 + 合作方式标签（技术转让/入股）。
  - **成果展示卡片：** 成果图 + 应用领域 + 所属高校/单位。
- **筛选功能（专利交易）：** 专利类型（发明/实用新型/外观）、**发明人（可选）**、行业/分类号、法律状态、价格区间、发布时间、地区。
- **排序（新增）：** 默认「推荐」（可切换：最新/热门/价格升序/价格降序/**发明人影响力**）。
  - 地域特色规则生效时：同城/同省优先；被后台标记为“省级/市级特色产业专利”的信息在对应区域内高权重置顶。

##### 4.1 发明人排行榜（新增，P0）

> 目标：让搜索列表的默认排序更贴近用户兴趣与区域产业特色；同时保持可控、可解释、可调参。

- **排名口径（P0 默认）：** 按发明人关联的专利数量（去重按 `patent_id`）降序；并可展示其关联的上架数量（`ACTIVE/SOLD`）作为参考。
- **入口建议：** 首页快捷入口「发明人榜」（检索页不放入口，避免干扰检索主链路）；支持关键词搜索（发明人姓名）与地区过滤（按上架 `region_code`）。
- **点击发明人：** 跳转到“专利交易列表”并自动带入 `inventor` 条件（聚合其相关专利上架信息）。

#### 5. 详情页 (Detail Page)

所有交易转化的核心页面。

- **专利交易详情页：**
  - **基本信息：** 专利号/申请号/公开号、名称、摘要、专利类型、分类号（IPC/LOC）、**权利人/申请人/发明人**、申请日/公开(公告)日、有效期、法律状态。
  - **价格区域：** 订金金额（如 ¥2000）、预估总价。
  - **卖家信息：** 头像、昵称、认证标识（企业/科研院校/政府/协会/技术经理人等）。
  - **留言区（新增，P0）：** 页面底部展示公开留言列表；登录且审核通过可留言/回复/编辑/删除（本人）；后台可隐藏/恢复/删除。
  - **底部操作栏：**
    - **收藏**。
    - **在线咨询：** 点击跳转聊天窗口。
    - **立即支付订金：** 点击唤起支付（小程序默认微信支付；**电脑端 H5 不做支付**，展示“去小程序支付”的二维码/链接）。
- **需求/成果详情页：**
  - 展示详细图文描述。
  - 页面底部增加留言区，支持留言/回复/编辑/删除（同上）；后台可管理。
  - 底部只有“在线咨询”和“收藏”，无支付功能。

#### 6. 聊天与咨询页 (Message)

- **消息列表：** 显示所有对话记录。
- **聊天窗口：**
  - 支持发送文本、图片、文件（文件能力按小程序平台能力与合规要求裁剪）。
  - **顶部悬浮卡片：** 显示当前沟通的专利/需求信息，方便双方确认。
  - **系统提示：** “请勿私下转账，谨防诈骗”的安全提示。

#### 7. 支付与交易流程页 (Checkout)

- **订金支付页：**
  - 显示订单摘要：专利名称、订金金额。
  - 服务协议勾选：“我已阅读并同意《专利交易居间服务协议》”。
  - 支付方式选择。
- **支付成功结果页：**
  - 提示语：“订金支付成功！专属客服将在 15 分钟内联系您，引导线下签约；合同签署确认后可在平台支付尾款。”
  - 按钮：“查看订单”、“联系客服”。
- **尾款支付页（合同线下签署后解锁）：**
  - 显示订单摘要：专利名称、成交价、已付订金、待付尾款。
  - 展示“合同签署确认”信息与回传材料（可选查看）。
  - 支付方式：小程序微信支付（电脑端 H5 引导回小程序）。
- **尾款支付成功结果页：**
  - 提示语：“尾款支付成功，资金平台托管。完成权属变更后将自动结算并放款给卖家。”
  - 按钮：“查看进度”、“联系跟单客服”。

#### 8. 我的 (User Center)

- **头部：** 头像、昵称、身份标签（如：认证企业）、账户余额（若有）。
- **订单管理面板：**
  - **我买到的：** 待付订金、已付订金/待签合同、待付尾款、权属变更中、已完成、退款中/已退款、已取消。
  - **我卖出的：** 待确认/待补材料、待签合同、待收尾款（托管）、权属变更中、已完成。
- **发布管理：** 编辑、下架、刷新自己发布的三类信息。
- **常用工具：** **企业/实名认证入口**。
  - **客服中心**（电话/在线）。
  - **收货地址/合同寄送地址管理**。
  - **发票/电子发票下载（新增）：** 订单完成后，如财务已上传电子发票，可在订单详情查看并下载。

#### 9. 发布与编辑 (Publish)

- **入口：** 首页快捷入口“发布”/悬浮按钮/「我的-发布管理」。
- **发布类型：** 专利交易（商品）、产学研需求、成果展示。
- **专利交易发布（建议字段）：**
  - 基础：专利号/申请号/公开号（支持粘贴识别）→ 自动回填（名称、类型、分类号、申请日等）→ 手工确认。
  - 权属：当前权利人/申请人信息、权属证明材料上传（证书/合同链路等）、是否质押/许可现状声明。
  - 交易：交易方式（转让/许可）、标价/面议、订金、可谈空间、可交付资料清单、预计周期。
  - 风险提示：平台仅撮合，最终以合同及主管部门登记结果为准。
- **需求发布（建议字段）：** 需求标题、技术领域/关键词、预算范围、合作方式（转让/许可/入股等）、期望地区/行业、联系人（脱敏展示）。
- **成果展示发布（建议字段）：** 成果标题、图片/视频、应用场景、成熟度、所属单位、可合作方式。
- **审核：** 提交后进入“待审核”池；审核通过上架，驳回需给出原因并支持修改后再提交。

------

### 第二部分：管理后台（PC Web 端）功能明细

这是运营人员使用的核心系统。

#### 1. 工作台 / 数据看板 (Dashboard)

- **核心数据：** 今日新增用户、今日发布信息数、今日订单数、今日流水（订金/尾款）、待结算金额、已结算佣金。
- **待办事项提醒：** 待审核发布 X 条、待审核认证 X 条、**新增待介入交易 X 单**。

#### 2. 专利地图数据管理 (Map Data CMS)

- **功能目标：** 解决手动上传数据的需求。
- **操作界面：**
  - **区域管理：** 添加/编辑省份或城市节点。
  - **数据录入：** 针对每个区域，录入以下字段：
    - 年份。
    - 专利总量。
    - 产业分布数据（例如：输入“电子信息: 500条”, “生物医药: 300条”）。
    - 重点企业名单及专利数。
  - **支持 Excel 导入：** 提供模板下载，批量导入数据生成地图。
  - **地域产业标签管理（新增）：** 为省/市配置“特色产业标签”（用于推荐与搜索加权）。
  - **特色产业专利标记（新增）：** 在省/市维度维护“省级/市级特色产业专利”名单，配置置顶优先级/权重，突出区域特色。

#### 3. 交易订单管理 (Order System)

- **列表展示：** 订单号、专利名、买卖双方、交易金额、当前状态、**跟单客服**。
- **状态流转操作（关键）：**
  - **状态：已付订金。** -> 操作：**分配客服**（系统可自动或手动指派）。
  - **状态：线下合同签署中。** -> 客服在后台记录里程碑（合同已寄送/已签署/回传材料齐全等）。
  - **状态：待付尾款。** -> 里程碑“合同签署确认”完成后，系统通知买家支付尾款（线上支付）。
  - **状态：尾款已付（托管）/权属变更中。** -> 客服持续记录进度（提交变更/补正/变更完成）。
  - **状态：待结算/放款。** -> 变更完成并确认无争议后，操作“确认结算并放款”（系统扣除佣金，向卖家放款）。
  - **退款处理：** 支持订金退款/尾款退款（按 7.1 规则自动或人工审批），全链路留痕与对账。

#### 4. 信息发布审核 (Content Audit)

- **审核列表：** 每一条用户发布的信息（专利、需求、成果）都需要先进入“待审核”池。
- **审核详情：** 查看图片是否违规、文字是否含敏感词、专利号是否格式正确；专利类型/分类号是否合理；权属材料是否齐全（如适用）。
- **操作：** 通过（上架） / 驳回（填写驳回原因，用户端会收到通知）。

#### 5. 用户与认证管理 (User & Auth)

- **用户列表：** 查看所有注册用户，支持封禁/解封。
- **认证审核：**
  - 查看用户上传的身份证/营业执照/事业单位法人证书等材料。
  - 人工核对信息是否一致，点击“通过认证”或“驳回”。
  - 认证通过后，前端显示“V”标或“企业认证”标签。

#### 6. 财务管理 (Finance)

- **资金流水：** 查看订金/尾款入账、退款、放款、佣金入账等流水记录。
- **结算与放款：** 订单结算审核（如需）、卖家放款记录与失败重试。
- **发票管理（新增）：** 已完成订单发票上传/替换（PDF/图片）+ 发票号码/日期（可选）+ 通知下载 + 审计留痕。
- **报表导出：** 导出月度交易报表，统计成交额、佣金收入、退款率、放款成功率。

#### 7. 系统设置

- **Banner 配置：** 上传首页轮播图图片和跳转链接。
- **协议管理：** 编辑《用户协议》、《交易服务条款》的富文本内容。
- **客服设置：** 配置前端显示的客服电话、默认自动回复语。
- **类目与标签：** 配置行业领域、分类号映射、热门搜索词、敏感词库。
- **推荐配置（新增）：** 调节推荐权重系数（发布时间/点击/收藏/咨询/地域/特色置顶/兴趣标签）、时间衰减参数与刷量去重策略；权重调整后立即影响推荐排序。
