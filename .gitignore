node_modules
dist
build
.turbo
.taro
.next
coverage
.swc
.env
.env.*
!.env.example
*.log
.DS_Store

# Docs rendered artifacts
# Rendered diagrams/images are part of deliverables, keep them tracked.
# Ignore only local export/temp outputs if needed.
# Local IPC classification source files (CNIPA/WIPO)
docs/ipc/
